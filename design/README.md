# RentEase — Design

Thư mục chứa file thiết kế (Figma export, mockup, asset UI/UX) cho project RentEase.

Nằm trong repo, tách riêng khỏi `src/` và `backend/` để dễ phân biệt asset thiết kế với source code.
