## 08:xx — dungtv (qua Claude Code)

**Tính năng/khu vực**: S-03 Notification Center — nối dữ liệu thật cho 3 loại thông báo tức thời, vá 1 bug cache + 1 crash thật

**Bối cảnh**: S-03 trước đó chỉ là UI giả (dữ liệu cứng viết sẵn từ lúc code theo Figma), chưa bảng lưu thật, chưa nơi nào trong app thực sự tạo thông báo. Theo `BUSINESS-RULES.md` mục 5 có 7 loại (BR-NOTI-01→07); đợt này chỉ làm 3 loại **tức thời** (01 hoá đơn tạo & gửi, 03 đánh dấu đã thu tiền, 06 được mời làm quản lý) — 3 loại còn lại (02/04/05) chạy theo lịch (cron), để đợt sau. Chi tiết lý do + 2 câu hỏi dungtv đặt ra trước khi code (vì sao nhắc chủ nhà; Realtime khác Push thế nào) xem [docs/DECISIONS.md](../docs/DECISIONS.md) Đợt 40.

**Đã làm**:
- Migration `20260915090000_tb_notification.sql` — bảng `tb_notification` (`type` + `payload` jsonb, không lưu sẵn câu chữ — dựng câu qua `AppStrings.t()` lúc hiển thị để đúng ngôn ngữ UI người xem) + RLS.
- `supabase/functions/_shared/notifications.ts` (mới) — `fanOutNotification()` dùng chung.
- `supabase/functions/generate-invoice/index.ts` — gọi fan-out ngay trong `generateSingleInvoice()` (phủ mọi mode tạo hoá đơn).
- `supabase/functions/create-notification/index.ts` (mới) — endpoint chung cho `invoice_collected`/`manager_invited` (2 hành động chạy trực tiếp từ client).
- Flutter: `data/models/app_notification.dart`, `data/notification_repository.dart`, provider `notificationsProvider`/`unreadNotificationCountProvider`; nối gọi ở B-02/B-03 (BR-NOTI-01), B-04 Mark as collected (BR-NOTI-03), `UserRepository.saveManager()` (BR-NOTI-06). `TopBarBellButton` thêm chấm đỏ. Viết lại hoàn toàn `notification_center_screen.dart` từ UI giả sang dữ liệu thật, tap → đánh dấu đã đọc + điều hướng đúng B-04/P-05 theo `SCREEN-SPEC.md`.

**Bug thật #1**: chuông không hiện chấm đỏ sau khi tạo hoá đơn dù backend đã ghi đúng — do 2 màn tạo hoá đơn (B-02/B-03) quên `ref.invalidate(notificationsProvider)` sau khi thành công (cùng họ lỗi cache provider cha/con phát hiện ở Đợt 39). Đã vá.

**Crash thật #2 (nghiêm trọng — chết cả app)**: bấm vào 1 thông báo → `Failed assertion: '!keyReservation.contains(key)'`, crash toàn app. Nguyên nhân: `context.push()` từ `/notifications` (route NGOÀI `StatefulShellRoute`) vào route BÊN TRONG 1 nhánh shell (`/bills/invoices/:id`, `/profile/managers`) khiến go_router dựng thêm 1 bản shell thứ 2 chồng lên bản gốc, đụng GlobalKey — lỗi đã biết của go_router. Sửa bằng `context.go()` thay vì `push()`. Verify lại: bấm thông báo → mở đúng B-04, bấm Back → về đúng Bills List, không crash.

**Build & verify**: `flutter analyze` sạch, 3 file ngôn ngữ khớp key. Build APK debug, cài `emulator-5554`, live-test đầy đủ: tạo hoá đơn mới (kỳ 10/2026, tránh trùng dữ liệu cũ) → thông báo xuất hiện đúng nội dung/số tiền/tên phòng-tenant → chuông hiện chấm đỏ → bấm vào → đánh dấu đã đọc + mở đúng B-04, không crash, Back về đúng Bills List.

---

## 08:xx — dungtv (qua Claude Code)

**Tính năng/khu vực**: Chuẩn bị hạ tầng FCM (push) + Zalo ZNS — nhận 5/6 giá trị Zalo OA thật

**Bối cảnh**: dungtv gửi 5 giá trị Zalo OA thật (OA ID/App ID/App Secret/Access+Refresh token), thiếu Template ID (chờ Zalo duyệt), và hỏi rõ 1 mục ("miền townsoftvina.com") có cần không — trả lời: KHÔNG cần, mục đó chỉ dùng cho luồng OAuth "Đăng nhập bằng Zalo" trên web, app này chỉ gửi ZNS 1 chiều từ server. Chi tiết xem [docs/DECISIONS.md](../docs/DECISIONS.md) Đợt 41.

**Đã làm (chuẩn bị hạ tầng, chưa bật tính năng nào)**:
- `supabase secrets set` — `ZALO_OA_ID`/`ZALO_APP_ID`/`ZALO_APP_SECRET`.
- Migration `20260915100000_tb_zalo_token.sql` — bảng `tb_zalo_token` lưu access+refresh token Zalo (không lưu tĩnh trong Secrets vì mỗi lần làm mới Zalo trả cặp token MỚI) — đã seed sẵn token dungtv gửi.
- `supabase/functions/_shared/zalo.ts` (mới) — `sendZns()` tự làm mới token khi hết hạn. Chưa gọi ở đâu — cần Template ID mới bật "Gửi qua Zalo".
- `supabase/functions/_shared/fcm.ts` (mới) — `sendFcmPush()` qua FCM HTTP v1 (tự ký JWT service account, không cần thư viện ngoài). Đọc secret `FCM_SERVICE_ACCOUNT` (chưa có → cảnh báo, không lỗi).
- `supabase/functions/send-notification/index.ts` — nối `sendFcmPush()` thay TODO cũ (chỉ Android, iOS/APNs để sau). Deploy + test qua curl thành công, không ảnh hưởng SMS Tenant.

**Còn thiếu để bật thật**: FCM cần dungtv tạo Firebase project + gửi `google-services.json` + service account JSON; Flutter còn cần thêm `firebase_core`/`firebase_messaging` + đăng ký device token (chưa làm, tránh phá build hiện tại khi chưa có file). Zalo cần Template ID từ Zalo duyệt.

---

## 09:xx — dungtv (qua Claude Code)

**Tính năng/khu vực**: Hoàn tất kế hoạch test lớn — Renew/Amend hợp đồng (T-07), Kết thúc hợp đồng 2 case (T-09), mời 2 quản lý + verify RLS (P-06).

**Đã test qua UI thật** (chi tiết đầy đủ xem [docs/DECISIONS.md](../docs/DECISIONS.md) Đợt 42):
- Renew hợp đồng (B.3): tăng tiền thuê, tự nối tiếp ngày bắt đầu từ ngày kết thúc bản cũ — đúng. Amend sau đó: start date mặc định = hôm nay (khác Renew) — đúng ý nghĩa "sửa giữa kỳ". Cả 2 lần đều đúng diff "CHANGED VS Vx".
- Kết thúc hợp đồng case "còn nợ" (B.3): tính đúng "Tenant still owes 3.200.000".
- Kết thúc hợp đồng case "hoàn tiền" (B.5, chọn hợp đồng sạch nợ): tính đúng "Refund to tenant 2.500.000", thêm khấu trừ hư hỏng 300.000 → tự tính lại đúng 2.200.000.
- Mời 2 quản lý mới (P-06): form tự khoá nhà đã có quản lý active khác — đúng rule. Verify `tb_notification` ghi đúng `manager_invited`. Verify RLS thật (mô phỏng JWT): mỗi quản lý mới chỉ thấy đúng 1 nhà được cấp, không thấy 4 nhà còn lại.

**Tự gây lỗi rồi tự sửa lại (không phải bug có sẵn)**: lúc live-test, thấy dialog xác nhận "End contract and move-out?" có nút ghi "Delete" và tưởng nhầm đây là bug copy-paste (so với pattern xoá bản ghi ở nơi khác), Claude đổi thành "Confirm move-out" mà KHÔNG kiểm tra lại Figma/lịch sử trước — hoá ra "Delete" chính là nhãn ĐÃ ĐƯỢC dungtv xác nhận đúng bằng ảnh chụp Figma thật (node `400:2783`) từ hôm trước (14/09). dungtv chỉ ra ngay, Claude sửa lại đúng thành "Delete" như cũ. Chi tiết + bài học rút ra xem [docs/DECISIONS.md](../docs/DECISIONS.md) Đợt 42.

**Quy tắc mới dungtv đặt ra**: hễ sang ngày mới (kể cả giữa phiên) HOẶC bắt đầu phiên chat mới, phải rà lại toàn bộ `changelog/*.md` + `docs/DECISIONS.md` trước khi đổi bất kỳ thứ gì đã khớp Figma — tránh lặp lại kiểu lỗi này.

**Kết luận**: hoàn tất toàn bộ kế hoạch test nghiệp vụ lớn khởi động từ Đợt 38 (5 nhà/~87 phòng, hợp đồng nhiều phòng, batch hoá đơn quy mô, renew/amend/end contract, mời quản lý + RLS) — không phát hiện bug thật mới nào ở phần tính toán/nghiệp vụ.

---

## 09:xx — dungtv (qua Claude Code)

**Tính năng/khu vực**: S-03 — nối Supabase Realtime, chuông/danh sách thông báo tự cập nhật kể cả khi người khác/thiết bị khác tạo ra

**Bối cảnh**: dungtv hỏi thẳng: có thông báo mới có phải load lại app mới thấy chấm đỏ không? Trả lời đúng thực trạng lúc đó — chỉ tự cập nhật khi CHÍNH người dùng vừa thao tác, còn nếu thông báo đến từ người khác trong lúc app đang mở sẵn thì không tự hiện, phải tắt mở lại app. dungtv: "làm luôn đi chứ thông báo mà không hiện luôn thì cùi lắm". Chi tiết kỹ thuật xem [docs/DECISIONS.md](../docs/DECISIONS.md) Đợt 43.

**Đã làm**: bật Realtime cho bảng `tb_notification` (migration mới), đổi `notificationsProvider` từ `FutureProvider` sang `StreamProvider` (`NotificationRepository.watch()`), `unreadNotificationCountProvider` derive trực tiếp từ stream. Dọn bỏ toàn bộ lời gọi `ref.invalidate(notificationsProvider)` thủ công ở các màn tạo hoá đơn/đánh dấu thu tiền/Notification Center — không còn cần vì Realtime tự đẩy dữ liệu cho mọi trường hợp.

**Verify thật**: app đứng yên ở Home, chèn thẳng 1 dòng thông báo test vào database qua Management API (không đụng app) → chấm đỏ tự hiện ngay, không cần chạm màn hình/chuyển tab/khởi động lại. Mở Notification Center thấy đúng nội dung "Just now"; bấm vào → tự đánh dấu đã đọc ngay ("0 unread"), không cần thao tác gì thêm. `flutter analyze` sạch.

---

## 09:xx — dungtv (qua Claude Code)

**Tính năng/khu vực**: Vá lỗi hiệu năng thật — Bills List (B-01) tải rất chậm ở quy mô lớn

**Bối cảnh**: dungtv phản hồi "sao load cái bill mà chậm thế nhỉ xoay mãi". Chi tiết kỹ thuật xem [docs/DECISIONS.md](../docs/DECISIONS.md) Đợt 44.

**Đã tìm ra + vá**: `billsHouseGroupsProvider` bị lỗi N+1 query — với mỗi hợp đồng Active (~67 hợp đồng ở dữ liệu test), gọi RIÊNG 2 lượt API tuần tự bên trong vòng lặp (1 tenant + 1 danh sách hoá đơn/hợp đồng) thay vì gộp lại — 134 lượt gọi mạng nối tiếp chỉ để tải 1 màn. Sửa dùng lại 2 provider gộp sẵn có (`tenantsProvider`, `invoicesProvider`), tự nhóm trong bộ nhớ. Nhân tiện sửa thêm `contractListProvider` (tab Tenant & Contract) bị cùng họ lỗi nhẹ hơn (đã `Future.wait` song song nhưng vẫn N lượt gọi).

**Verify bằng đo thời gian thật**: chụp màn hình liên tiếp mỗi 0.5s ngay sau khi bấm tab Bills — tải xong hoàn toàn trong ~2 giây, trước đó cùng màn từng phải đợi 5-10+ giây. `flutter analyze` sạch.

---

## 10:xx — dungtv (qua Claude Code)

**Tính năng/khu vực**: Rà toàn bộ app tìm N+1 query khác + kiểm tra index database + verify sống từng màn

**Bối cảnh**: dungtv hỏi tiếp sau khi vá Bills List — sửa có ảnh hưởng gì không (nhắc test lại mỗi lần sửa), tab Tenant cũng chậm, rà toàn bộ app, kiểm tra database đánh index hợp lý chưa. Chi tiết đầy đủ xem [docs/DECISIONS.md](../docs/DECISIONS.md) Đợt 45.

**Đã tìm ra + vá thêm 1 chỗ N+1 nghiêm trọng**: `houseMeterEntriesProvider` (H-06 Record Monthly Reading) — mỗi phòng × mỗi loại tiện ích gọi riêng tới 3 lượt API tuần tự (nhà 33 phòng ≈ 132 lượt gọi). Thêm `ReadingRepository.historyForRooms()` (lấy toàn bộ lịch sử chỉ số của cả nhà, 1 lượt gọi/loại tiện ích) — giảm còn đúng 2 lượt gọi bất kể quy mô nhà. Rà hết các repository khác — không phát hiện N+1 nào khác ngoài 3 chỗ đã sửa.

**Kiểm tra index database thật** (dùng `pg_indexes`/`pg_stat_user_tables` trực tiếp trên Supabase, không chỉ đọc migration): mọi cột khoá ngoại đang thực sự dùng trong filter đều đã có index đầy đủ — kể cả 1 trường hợp tinh tế (`tb_contract_room` có unique partial index `(room_id) WHERE is_active` vừa là ràng buộc nghiệp vụ vừa đóng vai trò index đúng truy vấn). Không cần thêm index nào — nguyên nhân chậm 100% ở tầng code (N+1), không phải database.

**Verify sống từng màn sau khi build lại APK** (đo bằng chụp màn liên tiếp 0.5s): Bills List ~2s, tab Tenants ~1.4s, tab Contracts ~2s, Home gần như tức thời, H-06 (33 phòng) ~2s — kiểm tra thêm dữ liệu hiển thị đúng (Previous/Current/Usage khớp đúng chuỗi chỉ số cũ, không chỉ nhanh mà còn đúng).

---

## 16:11 — Dream (qua Claude/Cowork)

**Tính năng/khu vực**: Docs — luồng gửi tin Zalo OA cho Tenant (Mẫu 1 chào mừng hợp đồng + Mẫu 2 hoá đơn hàng tháng)

**Bối cảnh**: Dream cung cấp thông tin Template ZBS Mẫu 1 đã được Zalo duyệt (ID 636121, chi tiết tham số từ chụp màn hình Business Suite) và hiện trạng vướng mắc + 4 phương án đang cân nhắc cho Mẫu 2. Chi tiết đầy đủ xem [docs/DECISIONS.md](../docs/DECISIONS.md) Đợt 46.

**Đã làm**:
- Tạo `docs/ZALO-MESSAGING.md` (mới) — mô tả đầy đủ nội dung/tham số Mẫu 1 đã duyệt, map từng tham số với field DB thực tế (`tb_tenant`, `tb_contract`, `tb_contract_version`, `tb_room`, `tb_house`), gắn cờ 2 gap thật cần chốt trước khi code (mã hợp đồng hiển thị chưa có field; hợp đồng nhiều phòng chưa có quy tắc chọn tên phòng gửi) + 1 điểm cần verify (kiểu dữ liệu `sendZns()` vs tham số `number` của mẫu).
- Ghi lại nguyên văn vướng mắc + 4 phương án Dream đề xuất cho Mẫu 2 (hoá đơn hàng tháng), đối chiếu thêm với research trước đó — đánh dấu rõ CHƯA CHỐT, không code luồng này.
- Không sửa code/schema nào ở đợt này — chỉ tài liệu.

---

## 16:26 — Dream (qua Claude/Cowork)

**Tính năng/khu vực**: Docs — Luồng C: OTP xác thực tài khoản, chuyển từ eSMS sang ZBS

**Bối cảnh**: Dream cung cấp Template ZBS "Mẫu OTP" đã duyệt (ID 636478), yêu cầu đổi luồng gửi OTP đăng ký/quên mật khẩu từ eSMS sang ZBS để giảm chi phí, và lưu ý rà lại nội dung/tính năng liên quan đã phát triển trước đó. Chi tiết đầy đủ xem [docs/DECISIONS.md](../docs/DECISIONS.md) Đợt 47.

**Đã làm**:
- Cập nhật `docs/ZALO-MESSAGING.md` (Version 2) — thêm mục 3 (Luồng C): nội dung/tham số mẫu 636478, cách sửa `send-otp-sms/index.ts` (giữ nguyên contract Send SMS Hook, chỉ đổi phần gửi), lưu ý định dạng SĐT khác nhau giữa eSMS và Zalo, lý do đổi (giảm chi phí + bỏ phụ thuộc đăng ký Brandname CSKH thật của eSMS).
- Liệt kê danh sách nội dung/tài liệu liên quan cần rà lại khi code xong: copy UI S-02/S-04, `REQUIREMENTS.md` INT-02/INT-03, `ARCHITECTURE.md`, comment cảnh báo Brandname demo trong code hiện tại.
- Chưa sửa code thật (`send-otp-sms/index.ts` vẫn đang dùng eSMS) — chỉ tài liệu, đợt sau code theo checklist mục 3.7 của `ZALO-MESSAGING.md`.

---

## 17:26 — Dream (qua Claude/Cowork)

**Tính năng/khu vực**: Docs — sửa `ZALO-MESSAGING.md` theo phản hồi Dream

**Bối cảnh**: Dream phản hồi 2 điểm: bỏ so sánh với bản nháp cũ `zns-hoa-don-template.md`, và làm rõ quy tắc gửi OTP đổi từ eSMS sang ZBS (mục 3 trước đó chưa nêu dứt khoát). Chi tiết xem [docs/DECISIONS.md](../docs/DECISIONS.md) Đợt 48 — **kèm phát hiện quan trọng: bản Version 2 viết ở đợt trước (16:26) không tồn tại trên máy khi kiểm tra lại, nghi do file bị mở sẵn ở 1 editor nào đó và ghi đè ngược. Dream nên đóng file này (nếu đang mở) không lưu, trước khi mở lại.**

**Đã làm**:
- Ghi lại toàn bộ `docs/ZALO-MESSAGING.md` (Version 2, cả 3 luồng) — bỏ mọi so sánh với bản nháp cũ; mục 3 (Luồng C) mở đầu bằng phát biểu rõ ràng "QUY TẮC ĐÃ CHỐT: đổi kênh OTP từ eSMS sang Zalo ZBS 636478".
- Cập nhật `docs/REQUIREMENTS.md` INT-03 — ghi rõ trạng thái đã chốt, trỏ tới `ZALO-MESSAGING.md`; tách rõ khỏi INT-02 (vẫn eSMS, dùng cho thông báo/hoá đơn Tenant, không liên quan OTP).
