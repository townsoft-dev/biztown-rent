# 2026-09-19

## 00:30 — Claude · Xử lý 8 lỗi/việc trong danh sách Task-Bug 18/9

dungtv gửi danh sách 8 việc (đánh số 3→10) kèm ảnh Figma đối chiếu. Đã hỏi lại 3 chỗ chưa rõ trước khi làm, dungtv trả lời rồi mới bắt tay.

### 3. Ghi nhớ đăng nhập

Thêm ô tick "Ghi nhớ đăng nhập" ở màn S-01, đúng vị trí Figma (node `664:2646`: ô 20×20, chữ cách 7px, nằm dưới ô mật khẩu và trên dòng "Quên mật khẩu?").

**Chỉ nhớ SỐ ĐIỆN THOẠI, cố ý không lưu mật khẩu.** Lưu mật khẩu dạng thường xuống máy là rủi ro thật — ai mượn máy hoặc bản sao lưu bị lộ là đọc được. Phiên đăng nhập vốn đã được Supabase giữ nên người dùng bình thường không phải gõ lại gì; ô này chỉ có tác dụng sau khi họ chủ động Đăng xuất.

### 4. Splash theo thiết kế mới

Bỏ dòng mô tả (Figma để `hidden="true"`), icon 100×100, khoảng hở 12px, wordmark 248px — số đo lấy từ node `220:2153`.

**Thêm đổ bóng cho icon**: nền splash và nền ô vuông icon cùng là `#23305E` nên không có bóng thì icon chìm hẳn. Thông số đo pixel trên bản render Figma: chỉ đổ xuống dưới, lan ~9px, chỗ đậm nhất `#1B2548` trên nền `#23305E` — quy ra **đen 23%**.

### 5. Logo màn Đăng nhập

Figma node `220:2176`: icon 60×60 **đứng cạnh** wordmark "RENT MANAGER" bản chữ đậm màu navy, không phải icon đứng một mình.

**Một lỗi tự phát hiện khi chụp màn kiểm tra**: đặt wordmark theo `height` làm chữ bé hơn hẳn thiết kế, vì file PNG 1784×431 nhưng phần chữ thật chỉ 1448×96 — phần còn lại là lề trống. Phải đặt theo **bề ngang 235** như Figma thì chữ mới đúng cỡ.

### 6. Nút ẩn/hiện mật khẩu ở TẤT CẢ ô mật khẩu

8 ô ở 4 màn. Gom thành 2 chỗ dùng chung thay vì sửa rải rác:
- `PasswordField` mới — dùng ở S-01, S-02, S-04 (5 ô, trước đây là `TextFormField` thô).
- `AppTextField` chuyển thành `StatefulWidget`, ô nào `obscureText: true` thì **tự mọc** nút — dùng ở P-04 (3 ô).

### 7. Nhập sai OTP báo nhầm là "hết hạn"

**Nguyên nhân**: Supabase trả **cùng mã lỗi `otp_expired`** cho cả "sai mã" lẫn "hết hạn" — ghi chú cũ trong code nói GoTrue tách riêng 2 trường hợp là **sai**.

**Cách phân biệt**: dùng đồng hồ đếm ngược của chính màn đó. Còn đếm ngược thì mã chưa thể hết hạn ⇒ người dùng gõ sai. Sửa câu chữ thành "Mã OTP không chính xác. Vui lòng kiểm tra lại." ở cả 3 ngôn ngữ.

### 8. Viền ô OTP bị nứt khi xoá số

Hiệu ứng mặc định của `pin_code_fields` cho số **trượt xuống đè qua viền**. Đổi sang `AnimationType.fade` — số mờ dần tại chỗ, viền nguyên vẹn.

### 9. Hai nút gửi lại mã, nút đúng chỗ thì chết

Nút "Gửi mã OTP" ở trên khoá **vĩnh viễn** sau lần gửi đầu, còn bên dưới lại mọc thêm một nút "Gửi lại mã" khác. Bỏ nút dưới, và mở lại nút trên khi đồng hồ về 0.

### 10. Quy tắc mật khẩu ở màn đặt lại mật khẩu

Thêm `PasswordRequirements` vào S-04 bước đặt mật khẩu mới, giống P-04 Đổi mật khẩu — trước đây chỉ báo lỗi sau khi bấm Lưu, người dùng phải tự đoán thiếu gì.

### Kiểm chứng

Chạy thật trên `emulator-5554`, chụp từng màn: Splash (bóng đổ + không còn dòng mô tả), Login (logo ngang + ô tick + nút con mắt bật/tắt được), OTP (nút trên mờ khi đếm ngược, không còn nút dưới, nhập sai báo đúng câu, xoá số viền không nứt).

**Riêng việc 10 chưa chụp được màn** — muốn tới bước đặt mật khẩu phải nhập đúng mã OTP thật, mà mã nằm trên điện thoại dungtv. Đã kiểm ở mức mã nguồn.

`flutter analyze` sạch, 3 file ngôn ngữ khớp key.
