## 10:xx — Dream (qua Claude/Cowork)

**Tính năng/khu vực**: Docs — hạ độ ưu tiên "Nhắc thanh toán tự động" từ Must xuống Could

**Bối cảnh**: Dream xác nhận nhắc trễ hẹn (nhắc thanh toán trước/đúng/sau hạn) chỉ là Could, không phải Must trong phase này — đảo lại quyết định cũ (kế thừa nguyên vẹn từ Version 2, ghi Must ở `BR-PAY-04`/`BR-NOTI-02`/`FR-BILL-09`). Chi tiết xem [docs/DECISIONS.md](../docs/DECISIONS.md) Đợt 49.

**Đã làm**:
- `docs/BUSINESS-RULES.md` — `BR-PAY-04`, `BR-NOTI-02`: đổi Must → Could, ghi ngày + tham chiếu Đợt 49.
- `docs/REQUIREMENTS.md` — `FR-BILL-09`: Must → Could. `FR-NOTI-02`: tách rõ priority theo từng phần trong dòng (hoá đơn mới vẫn Must, nhắc thanh toán hạ Could).
- Không sửa code — tính năng nhắc thanh toán tự động hiện chưa có implementation nào trong `supabase/functions`/`src/lib`, đây thuần là điều chỉnh tài liệu kế hoạch.

## 11:xx — Dream (qua Claude/Cowork)

**Tính năng/khu vực**: Docs — đảo lại quyết định "OTP đổi sang Zalo ZBS" (Đợt 47/48), tạm quay về eSMS

**Bối cảnh**: Dream yêu cầu revert/ghi đè quyết định 2026-09-15 "đổi kênh gửi OTP từ eSMS sang Zalo ZBS" — lý do: ZBS rẻ hơn nhưng đang cân nhắc lại có tiếp tục dùng Zalo OA cho hệ thống hay không. Cho tới khi chốt việc OA: OTP tạo tài khoản/đổi mật khẩu tiếp tục ưu tiên eSMS. Chi tiết xem [docs/DECISIONS.md](../docs/DECISIONS.md) Đợt 50.

**Đã làm**:
- `docs/ZALO-MESSAGING.md` — Luồng C (mục 3): đổi trạng thái "✅ ĐÃ CHỐT" → "⏸️ TẠM HOÃN"; header, mục 3.6, mục 4 cập nhật theo. Nội dung kỹ thuật (template 636478, mapping, checklist) giữ nguyên làm tham khảo, không xoá.
- `docs/REQUIREMENTS.md` — `INT-03`: đổi từ "ĐÃ CHỐT: eSMS → ZBS" thành "Tạm hoãn quyết định đổi sang ZBS — chờ chốt việc dùng OA, tiếp tục ưu tiên eSMS".
- **Không sửa code**: xác nhận lại `supabase/functions/send-otp-sms/index.ts` chưa từng thực sự đổi sang Zalo (chỉ có `sendViaEsms()`, không có nhánh Zalo nào) — quyết định Đợt 47/48 trước đó chỉ dừng ở tài liệu. Vì vậy code hiện tại đã khớp sẵn với quyết định mới, không cần rollback gì. `_shared/zalo.ts`, migration `tb_zalo_token`, secrets Zalo giữ nguyên (vẫn cần cho Luồng A hoá đơn).
