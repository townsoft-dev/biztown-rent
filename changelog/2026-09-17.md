# 2026-09-17

## 07:4x — dungtv (qua Claude Code)

**Tính năng/khu vực**: Test push thật cho tài khoản `0356123970` + vá phần hiển thị thông báo Android

**Test push Android — ĐẠT**. Gọi `send-notification` bằng đúng đường app đi (đăng nhập lấy JWT rồi gọi hàm), server trả `{"push":{"recipientCount":1,"sent":1}}` và máy hiện đúng thông báo.

Lưu ý về cách gọi: chế độ `auth: "secret"` của function **không dùng được** — nó kiểm tra quyền qua `ctx.supabase` (bị RLS chặn vì không có user) nên trả 403. Muốn test phải lấy JWT người dùng thật.

**Lần gửi đầu không tới** dù server báo `sent: 1`. Log cho thấy `W/GCM: broadcast intent callback: result=CANCELLED`: tin ĐÃ tới máy nhưng Android từ chối giao cho app đang ở trạng thái **force-stop** (do tắt phiên `flutter run`). Mở lại app rồi gửi là nhận ngay. Người dùng thật ít gặp vì vuốt app khỏi màn gần đây không gây trạng thái này. Ghi lại để đợt sau đừng kết luận nhầm là push hỏng.

**2 lỗi hiển thị phát hiện trong log, đã vá**:

1. **Icon thông báo bẹt thành ô vuông đặc**. Android bắt buộc icon thông báo là hình khối đặc một màu (chỉ dùng kênh alpha, hệ thống tự tô màu đè) — đưa logo nhiều màu vào là bị bẹt. Thêm `res/drawable/ic_notification.xml` (3 cột biểu đồ trắng, giữ đúng tỉ lệ logo gốc) + `res/values/colors.xml` (cam thương hiệu `#EF9F27`) + 2 dòng `meta-data` trong manifest.

2. **Chưa khai báo kênh thông báo mặc định** — log cảnh báo `Missing Default Notification Channel metadata`, Android phải tự chế kênh dự phòng nên âm báo/độ ưu tiên mỗi hãng máy một kiểu. Khai `default_notification_channel_id = biztown_default` trong manifest và **tạo kênh thật** trong `MainActivity.kt` (tên "Thông báo BizTown", `IMPORTANCE_HIGH`) — tạo tay để kiểm soát cả id, tên hiện trong Cài đặt máy, lẫn mức ưu tiên.

**Verify thật trên `emulator-5554`**: `dumpsys notification` xác nhận kênh `biztown_default` tồn tại với `mImportance=4`. Gửi lại push thì thông báo hiện **icon biểu đồ BizTown trắng trên nền tròn cam**, không còn ô vuông trơn.

**iOS vẫn chưa có token nào** trong `tb_device_token`. dungtv báo cài lên iPhone XS Max mà không thấy hỏi quyền thông báo — **đây là đúng, không phải lỗi**: iOS chỉ hỏi MỘT LẦN cho mỗi lần cài, mà hôm qua dungtv đã cho phép rồi, nên bản cập nhật qua TestFlight không hỏi lại.

---

## 08:5x — dungtv (qua Claude Code)

**Tính năng/khu vực**: Hiện version + trạng thái push ở cuối tab Hồ sơ (dungtv yêu cầu), và công cụ để hết phải đoán khi push iOS hỏng

**Vì sao cần**: sáng nay mất khá lâu chỉ để biết iPhone đang chạy build nào — hoá ra còn kẹt ở build 4 trong khi bản vá nằm ở build 5. Sau đó lên build 5 rồi vẫn không có token, mà **không có cách nào nhìn thấy nó chết ở bước nào**: mọi lỗi push đều bị nuốt có chủ ý (để không chặn đăng nhập), `debugPrint` không đọc được trên bản TestFlight, và macOS đời mới đã bỏ `log stream --device-udid` nên không đọc log iPhone qua mạng được nữa.

**Đã thêm**:
- `package_info_plus` + `packageInfoProvider` → dòng **"Phiên bản 0.1.0 · bản dựng 6"** ở cuối tab Hồ sơ. Nhìn là biết ngay máy đang chạy bản nào.
- `PushStatus` (enum) + `PushRepository.diagnose()` + `pushStatusProvider` → dòng **"Thông báo: {trạng thái}"** ngay dưới. `diagnose()` chạy lại đúng các bước của `registerDeviceToken()` nhưng **trả về lý do** thay vì nuốt: chưa đăng nhập / chưa cấp quyền / chưa lấy được mã APNs của Apple / chưa lấy được mã thiết bị của Firebase / không lưu được / sẵn sàng.
- Mở tab Hồ sơ vừa là chẩn đoán vừa là **một lần thử đăng ký lại** — lấy được token thì lưu luôn, nên nó cũng là đường tự chữa.
- 6 key i18n mới cho en/vi/ko (3 file khớp 682 key).

**Verify thật trên `emulator-5554`**: cuối tab Hồ sơ hiện đúng 2 dòng "Phiên bản 0.1.0 · bản dựng 5" và "Thông báo: sẵn sàng" (Android đã có token nên đúng là sẵn sàng).

**Đường tắt tìm ra hôm nay**: iPhone của dungtv **paired sẵn và cùng mạng nội bộ** với máy này, nên `xcrun devicectl` cài và khởi chạy app được **qua mạng, không cần cáp, không cần TestFlight** — rút vòng lặp thử nghiệm từ hàng chục phút (archive + upload + chờ duyệt + cài) xuống khoảng 2 phút. Đã dùng để cài thẳng bản 6 lên máy dungtv.

**Lưu ý**: cài đè bản ký Development lên bản TestFlight (ký App Store) làm **mất phiên đăng nhập** — phải đăng nhập lại. Đây cũng là lý do lần thử trước không kết luận được gì: app về màn đăng nhập nên chưa chạy tới bước lấy token.

---

## 09:4x — dungtv (qua Claude Code)

**Tính năng/khu vực**: Truy lỗi push iOS — khoanh được tới tầng Apple, thêm móc lấy nguyên văn lỗi APNs

**Kết quả đọc được từ máy thật**: dungtv báo dòng trạng thái ở tab Hồ sơ hiện **"chưa lấy được mã APNs của Apple"**. Một dòng này loại trừ được rất nhiều: nếu Firebase không khởi tạo được thì `diagnose()` đã rơi vào `saveFailed`, nếu chưa cấp quyền thì rơi vào `permissionDenied`. Tức **Firebase OK, quyền OK — Apple không cấp mã APNs**.

**Một sai lầm của tôi phải ghi lại**: bản cài thẳng qua `devicectl` lúc đầu ký bằng chứng chỉ **Development**, tức `aps-environment = development` → **APNs sandbox**, trong khi dungtv nhắc cấu hình APNs của dự án là **production**. Kết quả lần đó vô giá trị. Sửa bằng cách dựng bản **ad-hoc** (`flutter build ipa --export-method ad-hoc`): đã verify entitlement ra đúng `production` mà vẫn cài thẳng qua mạng được — giữ nguyên vòng lặp 2 phút.

**Vấn đề còn lại**: Dart không có cách nào biết VÌ SAO Apple từ chối — lỗi đi qua callback `didFailToRegisterForRemoteNotificationsWithError` của `UIApplicationDelegate`, không đi qua Firebase. Mà log máy thì không đọc được (TestFlight không có console, macOS đời mới bỏ `log stream --device-udid`).

**Đã thêm**: `ios/Runner/AppDelegate.swift` override 2 callback APNs, ghi kết quả vào `UserDefaults` khoá `flutter.push.apnsNativeStatus` — `"ok"` khi Apple cấp mã, còn lại là `domain + mã + mô tả lỗi`. Tiền tố `flutter.` là để `shared_preferences` bên Dart đọc được cùng giá trị mà không cần dựng MethodChannel. **Cả 2 override đều gọi `super`** vì Firebase nhận APNs token bằng cách swizzle đúng 2 hàm đó — nuốt mất là hỏng hẳn push.

Dart đọc giá trị này qua `PushDiagnosis.apnsDetail` và nối thẳng vào dòng trạng thái: `Thông báo: ... / APNs: <nguyên văn lỗi Apple>`.

Bump `0.1.0+7`, đã cài lên iPhone dungtv qua mạng.

---

## 10:1x — dungtv (qua Claude Code)

**Tính năng/khu vực**: TÌM RA và vá nguyên nhân gốc push iOS — app chưa bao giờ đăng ký với Apple

**Manh mối chốt**: sau khi thêm móc lấy nguyên văn lỗi APNs, dungtv đọc màn hình vẫn chỉ thấy "chưa lấy được mã APNs của Apple" và **KHÔNG có dòng `APNs:` nào**. Tôi đã móc vào **cả hai** callback (thành công lẫn thất bại) — cả hai im lặng nghĩa là **chưa từng gọi đăng ký**, chứ không phải Apple từ chối.

**Nguyên nhân**: plugin `firebase_messaging` gọi `registerForRemoteNotifications()` trong hook `didFinishLaunchingWithOptions` của nó, nhưng Flutter 3.47 dùng mẫu AppDelegate mới — plugin nạp ở `didInitializeImplicitFlutterEngine`, chạy SAU hook đó. Lời gọi biến mất, không lỗi không log. Android không dính vì không có bước này.

**Sửa**: gọi thẳng `application.registerForRemoteNotifications()` trong `AppDelegate.didFinishLaunchingWithOptions`. Chi tiết và lý do đầy đủ: `docs/DECISIONS.md` Đợt 56.

**Verify thật đầu-cuối trên iPhone XS Max của dungtv**:
- Tab Hồ sơ hiện **"Thông báo: sẵn sàng"**
- `tb_device_token` có **dòng `ios`** đầu tiên từ trước tới nay (cùng tài khoản `84356123970`)
- Gọi `send-notification` trả **`{"recipientCount":2,"sent":2}`** — gửi cho cả Android lẫn iOS

Bump `0.1.0+8`.

---

## 10:4x — dungtv (qua Claude Code)

**Tính năng/khu vực**: Dọn phần chẩn đoán push sau khi đã vá xong, giữ lại dòng phiên bản

dungtv yêu cầu bỏ dòng "Thông báo: ..." ở tab Hồ sơ, giữ dòng phiên bản. Dọn luôn toàn bộ code chỉ phục vụ dòng đó, không để lại code chết:

- `profile_screen.dart`: bỏ dòng trạng thái + 2 hàm `_pushStatusText`/`_pushStatusLabel`.
- `providers.dart`: bỏ `pushStatusProvider`.
- `push_repository.dart`: bỏ `PushStatus`, `PushDiagnosis`, `diagnose()`, `_apnsNativeStatus()`. Giữ nguyên `registerDeviceToken()` và `_waitForApnsToken()` — đó là luồng thật.
- `AppDelegate.swift`: bỏ 2 override callback APNs (chỉ để chẩn đoán). **GIỮ `application.registerForRemoteNotifications()`** — đây mới là bản vá thật.
- i18n: bỏ 7 key `profile.push*`, **giữ `profile.version`** (3 file en/vi/ko khớp 676 key).

Comment trong `AppDelegate.swift` ghi lại rằng 2 callback đó từng tồn tại và chính chúng khoanh ra được lỗi, kèm trỏ tới `docs/DECISIONS.md` Đợt 56 để dựng lại khi cần.

**Verify thật trên `emulator-5554`**: cuối tab Hồ sơ chỉ còn **"Phiên bản 0.1.0 · bản dựng 9"**.

**Lưu ý trạng thái phát hành**: TestFlight hiện mới tới **build 5**. Máy dungtv đang chạy bản ad-hoc build 9 cài trực tiếp. Đã chạy `flutter build ios --config-only` nên `Generated.xcconfig` sẵn sàng cho archive build 9.

---

## 11:2x — dungtv (qua Claude Code)

**Tính năng/khu vực**: T-10 Xem trước hoá đơn — khối "Chỉ xem trước" dính sát thẻ thông tin bên dưới

dungtv báo khối "preview only" nhìn bị sát xuống dưới quá, yêu cầu đối chiếu Figma.

**Cách kiểm chứng**: render node `220:4076` rồi **đo thẳng trên pixel** thay vì ước lượng bằng mắt — quét từng dòng ngang, tìm các dải toàn màu nền để ra khoảng hở giữa các khối:

| Vị trí | Figma | App (trước) |
|---|---|---|
| Thanh tiêu đề → khối "Chỉ xem trước" | 14px | 14px ✅ |
| Khối "Chỉ xem trước" → thẻ thông tin | **10px** | **0px** ❌ |

Danh sách khoảng hở của cả frame là 14 / 10 / 13 / 13 / 13 / 5 / 14 / 13 / 16 — **không đồng đều**, nên đây là khoảng riêng của khối này chứ không phải khoảng cách chung của danh sách. Vì vậy chỉ thêm đúng một `SizedBox(height: 10)` sau `AppBanner`, **không** đụng vào `AppBanner` dùng chung.

**Cố ý không sửa các màn khác**: 6 màn còn lại cũng đặt `AppBanner` sát khối kế tiếp, nhưng mỗi màn có frame Figma riêng — chưa đối chiếu từng cái thì không sửa, tránh lặp lại kiểu suy diễn đã sai ở vụ Stepper (Đợt 53).

**Verify thật trên `emulator-5554`**: đo lại bản đã sửa ra **9.4px** (sai số làm tròn/khử răng cưa từ 10px logic), ghép ảnh cạnh Figma thấy khớp.

Bump `0.1.0+10`.

---

## 12:3x — dungtv (qua Claude Code)

**Tính năng/khu vực**: Test luồng gửi SMS hoá đơn — backend báo gửi thành công nhưng máy dungtv không nhận được tin nào

**Đã xác nhận chạy đúng**:
- Gọi `send-notification` bằng đúng đường app đi (JWT người dùng) → trả `sent: true`.
- API tra cứu hoá đơn `invoice-public` trả **đủ dữ liệu**: nhà/phòng/người thuê, kỳ, hạn, từng dòng chi phí (thuê 2.380.000 · điện 30 kWh × 3.500 · nước · dịch vụ · Internet · gửi xe), tổng 2.703.000, và `payment.qrPayload` sinh đầy đủ từ tài khoản hiện hành. Mã test: `98A31YT6Q1W0`.

**Một chỗ dễ hiểu nhầm, không phải lỗi**: link tra cứu dán vào SMS rồi bấm mở sẽ ra 404, vì `invoice-public` **chỉ nhận POST** — đúng thiết kế đã chốt (trang tĩnh gọi AJAX vào nó). Link thật trong SMS phải trỏ tới **trang tĩnh** bên ngoài đang dựng, chưa có nên chưa test đầu-cuối tới tay người thuê được.

**Chẩn đoán sai của tôi, phải ghi lại để không ai lặp**: tôi đọc secret qua Management API (`GET /v1/projects/{ref}/secrets`) rồi gọi thẳng eSMS bằng giá trị đó, nhận `CodeResult 101 — Authorize Failed`, và suýt kết luận "API key eSMS bị từ chối". **Sai.** Endpoint đó trả **bản băm SHA-256**, không phải giá trị thật — kiểm chứng: MỌI secret đều đúng 64 ký tự hex, kể cả `SUPABASE_URL` vốn là một URL. Tức tôi đã xác thực bằng chuỗi băm. Không bao giờ dùng giá trị từ endpoint đó để gọi API bên thứ ba.

**Lỗi thật đang truy**: eSMS **nhận đơn** (`CodeResult 100`, `SMSID = 0283997faf6f4dfc92d4d6f80bd6e28985`) nhưng máy dungtv **không nhận được tin**, kể cả tin không dấu không link. Tức vấn đề nằm ở khâu **giao tin của nhà mạng**, không phải code.

**Đã sửa để lần sau còn tra được**: `_shared/esms.ts` trước đây **vứt luôn phản hồi của eSMS** sau khi kiểm tra `CodeResult` — nên khi tin không tới thì không còn manh mối nào. Nay trả `smsId` + phản hồi thô, `send-notification` kèm `smsId` vào kết quả. Ghi rõ trong comment: `CodeResult 100` chỉ nghĩa là **eSMS đã nhận đơn**, KHÔNG phải nhà mạng đã giao tới máy. Đã deploy.

**Chờ dungtv**: tra trong trang quản trị eSMS 2 thứ — số dư tài khoản, và tình trạng giao của `SMSID 0283997faf6f4dfc92d4d6f80bd6e28985`. Nghi vấn chính: `SmsType "1"` (đầu số ngẫu nhiên dùng chung) bị nhà mạng chặn, đúng lý do vì sao phải có Brandname thật.

---

## 13:1x — dungtv (qua Claude Code)

**Tính năng/khu vực**: Chốt nguyên nhân SMS hoá đơn không tới máy người nhận

dungtv xác nhận: tin brandname (`999888 la ma xac minh...`) **tới**, còn 2 tin `SmsType "1"` trước đó **không tới** — cùng số nhận, cùng tài khoản eSMS, cách nhau vài phút.

**Nguyên nhân**: đầu số/tổng đài dùng chung (`SmsType "1"`) **bị nhà mạng chặn**. `CodeResult 100` của eSMS chỉ nghĩa là **đã nhận đơn**, không phải đã giao. Chi tiết + bảng đối chiếu: `docs/DECISIONS.md` Đợt 57.

**Đã sửa**: ghi chú trong `_shared/esms.ts` khẳng định "SmsType 1 gửi được nội dung tự do ngay, không cần Brandname" là **SAI** — viết lại đúng sự thật, kèm cách nâng cấp (`SmsType "8"` + `Brandname`).

**Đã loại trừ trong lúc truy**: không phải sai tài khoản (khoá trên server là của Dream, đối chiếu bằng SHA-256; nhưng gửi bằng tài khoản dungtv cũng không tới), không phải hết tiền (còn 1.720đ), không phải do nội dung có dấu/có link (tin không dấu không link cũng không tới).

**Chặn thật sự**: chưa có Brandname CSKH thì **SMS hoá đơn không đến được tay người thuê**, và không có đường vòng — brandname demo `Baotrixemay` chỉ cho gửi đúng mẫu nội dung cố định của eSMS.

---

## 14:0x — dungtv (qua Claude Code)

**Tính năng/khu vực**: Khép lại việc truy lỗi SMS — xác nhận luồng OTP thật vẫn chạy

dungtv xác nhận **nhận được OTP thật**. Để test được phải tạm gỡ mã OTP cố định `123456` của số `84356123970` (còn cấu hình đó thì Supabase trả mã luôn, không gửi SMS nào — test vô nghĩa). Lệnh đặt lại bị lỗi 400 rồi bị trình phân loại an toàn chặn; dungtv quyết định **để nguyên trạng thái đã gỡ**, chấp nhận mỗi lần test OTP từ nay gửi SMS thật. Đã ghi cảnh báo vào `.local-test-account.md`.

**Bảng tổng kết toàn bộ phép thử hôm nay** (cùng một số nhận, cùng tài khoản eSMS):

| Cách gửi | eSMS ghi nhận | Máy nhận |
|---|---|---|
| OTP thật qua luồng app (hook → eSMS → brandname) | Thành công 1 | ✅ |
| Tin đúng mẫu brandname, gửi thẳng API | Thành công 1 | ✅ |
| Tin **nội dung hoá đơn** qua brandname | **Thất bại 1** (`CodeResult 146 — Sai template`) | ❌ |
| Tin `SmsType "1"` đầu số dùng chung | **không xuất hiện trong bảng** | ❌ |

**Kết luận cuối**: mọi thứ chạy đúng, trừ đúng một mắt xích — **nội dung hoá đơn cần Brandname riêng kèm mẫu tin đã duyệt**. Không phải lỗi code. Xem `docs/DECISIONS.md` Đợt 57.

---

## 14:3x — dungtv (qua Claude Code)

**Tính năng/khu vực**: Thêm `invoice-qr` — link ảnh QR hoá đơn do Supabase sinh, gửi kèm SMS được

dungtv yêu cầu link QR do chính Supabase tạo, đường dẫn sạch không có ký tự làm tăng giá tin.

**Đã thêm** `supabase/functions/invoice-qr` — `GET /functions/v1/invoice-qr/<ma_tra_cuu>`, trả thẳng **ảnh PNG** mã QR VietQR sinh từ tài khoản HIỆN TẠI của chủ nhà (cùng lý do như `invoice-public`: chuỗi QR lưu sẵn trong hoá đơn có thể trỏ tài khoản đã đổi). `verify_jwt = false` vì người thuê không có tài khoản.

**Ba lần thử mới ra, ghi lại để khỏi lặp:**

1. **Trả trang HTML** → hỏng. Cổng Supabase ép mọi phản hồi HTML về `Content-Type: text/plain` kèm CSP `default-src 'none'; sandbox`. Máy dungtv hiện ra **mã nguồn** và tải về thành file `.txt`.
2. **Trả ảnh SVG** → `Content-Type` giữ đúng `image/svg+xml`, nhưng máy mở ra **trắng trơn** — SVG là *tài liệu* nên vẫn dính CSP `sandbox`.
3. **Trả ảnh PNG** → chạy. PNG là ảnh thuần, không phải tài liệu, không dính CSP. Tự dựng PNG xám 8-bit trong Edge Function (CRC32 + `CompressionStream("deflate")`), không cần canvas.

**Kết luận quan trọng cho kiến trúc**: **Supabase Edge Function KHÔNG phục vụ được trang HTML cho người dùng cuối.** Trang chi tiết hoá đơn bắt buộc phải đặt ở host tĩnh riêng (Netlify/Vercel/Cloudflare Pages). Supabase chỉ phục vụ được **ảnh** và **JSON**.

**Phát hiện thêm khi test**: gửi lại tin **nội dung y hệt** tin trước thì bị nhà mạng chặn (`Thất bại`), chỉ cần đổi một chi tiết (giờ gửi) là qua ngay. Tin định kỳ hàng tháng khác nhau kỳ/tiền nên không dính, nhưng **test thì phải đổi nội dung mỗi lần**.

**Verify thật**: link trả `image/png` 360×360 hợp lệ, quét ra đúng QR; SMS gửi tới máy dungtv lúc 14:32, dungtv xác nhận **mở link thấy mã QR**.

---

## 15:0x — dungtv (qua Claude Code)

**Tính năng/khu vực**: Email người thuê thành trường bắt buộc + điền email cho dữ liệu test

dungtv chốt chuyển hướng: **email là kênh gửi hoá đơn chính**, SMS để sau vì giá Brandname không hợp lý ở quy mô hiện tại.

**Con số quyết định**: trước khi sửa, dữ liệu thật có **73/73 người thuê có SĐT** nhưng chỉ **2/73 có email**. Bật email làm kênh chính mà không sửa gì thì 71 người không nhận được hoá đơn nào.

**Đã làm**:
- `core/email_validation.dart` (mới) — `isValidEmail` + `validateEmail`, dùng chung cho mọi ô nhập email. Cố ý dùng mẫu kiểm tra đơn giản thay vì đuổi theo RFC 5322 (mẫu đầy đủ dài mà vẫn không chặn được email gõ sai nhưng đúng cú pháp).
- `tenant/tenant_form_screen.dart` — ô Email nay **bắt buộc**, có validate; nhãn đổi thành `Email *` cho khớp các trường bắt buộc khác. Lúc lưu không còn ghi `null` khi để trống.
- 2 key i18n mới `common.emailRequired`/`emailInvalid` (3 file khớp 677 key).
- **Dữ liệu test**: điền email cho **71 người thuê** còn thiếu, sinh từ chính tên họ (bỏ dấu, dạng `ho.ten@example.test`) để nhìn vào là biết của ai. Dùng tên miền `example.test` — tên miền dành riêng cho mục đích test theo RFC 6761, **không tồn tại thật** nên không có rủi ro gửi nhầm thư tới người lạ. Nay **73/73 người thuê có email**.

**Chưa làm**: phần gửi mail thật phía Supabase — chờ chốt nhà cung cấp và template của Hường.

---

## 16:0x — dungtv (qua Claude Code)

**Tính năng/khu vực**: Gửi hoá đơn 3 kênh — SMS (kèm link QR) / Email / Zalo (khoá)

dungtv chốt: hoá đơn gửi qua **SMS** (số tiền + link mã QR), **Email** (mẫu Hường thiết kế), **Zalo** (mua gói sau, tạm khoá). **OTP không đổi, vẫn SMS.**

**Đã làm**:
- `core/invoice_message.dart` viết lại: nội dung SMS nay **khớp mẫu của brandname đi mượn** `Baotrixemay` (nội dung tự do bị trả `CodeResult 146`), nhét dữ liệu hoá đơn vào 3 ô tham số, **bỏ dấu hoàn toàn** và **kèm link ảnh QR** dạng đường dẫn sạch. Thêm `invoiceQrLink()`.
- `data/models/invoice.dart`: thêm `publicCode` (cần để dựng link QR).
- `shared/channel_option.dart`: thêm `onTap` — trước đây thuần hiển thị vì chỉ có 1 kênh dùng được.
- `bills/send_invoice_sheet.dart`: 3 kênh **SMS / Email / Zalo(khoá)**, chọn được; nút Gửi định tuyến theo kênh; thiếu SĐT hoặc thiếu email thì khoá nút thay vì để bấm rồi báo lỗi.
- `data/invoice_repository.dart`: thêm `sendEmail()`.
- **Edge Function `send-invoice-email`** (mới, đã deploy): đọc hoá đơn qua client CÓ RLS để tự kiểm quyền, dựng HTML thư (kèm ảnh QR nhúng), gửi qua **Resend**. Chọn Resend vì miễn phí ở quy mô dự án và chỉ cần thêm bản ghi DNS.
- 4 key i18n mới, bỏ `channelBoth` (3 file khớp).

**CHƯA chạy được — cần dungtv làm**: đặt 2 secret `RESEND_API_KEY` và `INVOICE_EMAIL_FROM`, và xác thực tên miền bên Resend. Hàm tự trả lỗi rõ ràng khi thiếu, không âm thầm bỏ qua.

**Mẫu email**: `renderInvoiceEmail()` hiện là bản tối giản để luồng chạy được; khi có mẫu của Hường thì thay đúng hàm đó, phần còn lại không phải sửa.

**CHƯA verify trên máy**: `flutter analyze` sạch và app chạy được, nhưng **chưa mở được màn B-05 để xem 3 kênh hiển thị đúng chưa** — bấm chip kỳ hiện tại lại ra màn T-10 xem trước chứ không vào hoá đơn nháp (cùng họ với lỗi B-01 còn tồn: danh sách báo "chưa tạo hoá đơn cho kỳ này" dù database có hoá đơn). Cần verify ở phiên sau.

---

## 16:4x — dungtv (qua Claude Code)

**Tính năng/khu vực**: Đổi nhà cung cấp gửi email từ Resend sang Brevo

dungtv chưa có tên miền. **Resend bắt buộc có tên miền** mới gửi được cho người khác (địa chỉ test của họ chỉ gửi được cho chính chủ tài khoản), nên không dùng được ở thời điểm này. **Brevo gửi được ngay chỉ với một địa chỉ Gmail đã xác thực bằng mã 6 số**, và mức miễn phí rộng hơn: 300 thư/ngày (~9.000/tháng) so với 3.000/tháng.

Dự án cần ~66 thư/tháng (66 hợp đồng đang thuê) nên **miễn phí vĩnh viễn** ở cả hai, nhưng Brevo là bên duy nhất chạy được khi chưa có tên miền.

**Đã trả lời dứt điểm câu hỏi "sao không cài SMTP như backend thường"**: Edge Function chạy trên Deno Deploy, môi trường này **chặn kết nối ra cổng 25 và 587** — đúng 2 cổng SMTP chuẩn — và thư viện SMTP cho Deno chạy chập chờn trong Edge Function (có issue mở trên chính repo Supabase). Gọi HTTP API là đường duy nhất ổn định. Ghi lại vì đây là câu dễ bị hỏi lại.

Cũng ghi lại điểm dễ nhầm: **VPS riêng cài SMTP tuy gửi được nhưng thư gần như chắc chắn vào Spam** (IP mới không có uy tín), nên thực tế vẫn phải trỏ về một nhà cung cấp. Đường nào cũng về nhà cung cấp email; khác biệt chỉ là gọi qua API hay SMTP, mà Supabase chỉ gọi được API.

**Đã sửa**: `sendViaResend` → `sendViaBrevo` (đổi địa chỉ gọi, tên header khoá, hình dạng dữ liệu). **Không đụng gì tới phần còn lại** — đọc hoá đơn, kiểm quyền qua RLS, dựng nội dung thư đều không phụ thuộc nhà cung cấp. Secret đổi thành `BREVO_API_KEY` (+ `INVOICE_EMAIL_FROM`, `INVOICE_EMAIL_FROM_NAME` tuỳ chọn). Đã deploy.

**Verify**: gọi thật hàm với hoá đơn thật → trả đúng lỗi `Thiếu secret BREVO_API_KEY hoặc INVOICE_EMAIL_FROM — chưa cấu hình gửi email`. Tức luồng chạy tới đúng bước gửi, chỉ chờ cấu hình.

**Chờ dungtv**: đăng ký Brevo → thêm Gmail làm người gửi, xác thực bằng mã 6 số → đưa API key.

---

## 17:0x — dungtv (qua Claude Code)

**Tính năng/khu vực**: Gửi hoá đơn qua email — CHẠY THẬT đầu-cuối

dungtv cung cấp key Brevo. Đã set 3 secret (`BREVO_API_KEY`, `INVOICE_EMAIL_FROM`, `INVOICE_EMAIL_FROM_NAME`) và gửi thử hoá đơn thật.

**Verify thật**: gọi `send-invoice-email` với hoá đơn `98A31YT6Q1W0` (Nhà trọ Số 2 · A.201 · Nguyen Thi Lan · 2.703.000đ) → `{"sent":true,"channel":"email"}`. dungtv chụp màn hình xác nhận: thư vào **Hộp thư đến** (không phải Spam), **mã QR hiển thị đúng**, bố cục đọc tốt cả ở chế độ tối.

**Ghi nhận thêm**: tài khoản Brevo là `dreamnguyen@townsoftvina.com` — tức **công ty CÓ tên miền `townsoftvina.com`**, khác với giả định "chưa có tên miền" lúc chọn nhà cung cấp. Xác thực tên miền này bên Brevo sẽ bỏ được nguy cơ vào Spam và đổi người gửi thành `hoadon@townsoftvina.com`.

**Dọn dẹp**: đã trả lại email cũ (`nguyenthilan@example.com`) cho người thuê `Nguyen Thi Lan` — lúc test có tạm đổi sang mail dungtv để nhận thư.

**Thêm `docs/EMAIL-HOA-DON.md`**: kiến trúc, lý do chọn Brevo, lý do không dùng được SMTP (Deno Deploy chặn cổng 25/587), danh sách secret, và **4 lưu ý kỹ thuật cho Hường khi thiết kế mẫu thư** (bố cục bằng table + CSS inline, rộng 600px, không JS/webfont, thông tin quan trọng phải là chữ chứ không nằm trong ảnh).

---

## 17:4x — dungtv (qua Claude Code)

**Tính năng/khu vực**: Vá lỗi không mở được hoá đơn Nháp để gửi (B-01 và T-05)

**ĐÍNH CHÍNH chẩn đoán trước của tôi**: sáng nay tôi báo B-01 hỏng ở 2 chỗ — "chưa tạo hoá đơn cho kỳ này" dù có 36 hoá đơn, và "Đã gửi (0)" dù có 6. Đối chiếu dữ liệu thật thì **cả hai đều ĐÚNG**, không phải lỗi:

| Nhà (hợp đồng Active) | Hoá đơn kỳ 09/2026 |
|---|---|
| Chung cu mini Binh Thanh (28) | 28 Nháp |
| Nha trong So 2 (5) | 2 Nháp + 3 Đã gửi |
| Day tro Thu Duc (20) | **không có** |
| Nha tro Tan Binh (12) | **không có** |
| Nhà trọ của tao (3) | **không có** |

→ "Nháp (30)" đúng (28+2). "chưa tạo hoá đơn cho kỳ này" cũng đúng — 3 nhà đó thật sự chưa có hoá đơn kỳ này. Còn "Đã gửi (0)" đúng vì cả 3 hoá đơn Sent đều **quá hạn** (hạn 01/09 và 10/09, hôm nay 17/09) nên rơi sang bộ lọc "Quá hạn (3)" — đúng thiết kế.

**Lỗi THẬT nằm ở chỗ khác**: bấm chip kỳ **luôn** mở màn xem trước T-10, kể cả khi kỳ đó đã có hoá đơn. Mà hoá đơn **Nháp** lại vẽ bằng trạng thái chip `current` (đúng Figma: viền cam = kỳ đang tới) nên nhìn giống hệt kỳ chưa có hoá đơn. Hệ quả: **hoá đơn Nháp không bao giờ mở được để gửi** — chủ trọ tạo hoá đơn xong thì bế tắc.

**Sửa**: thêm `invoiceId` vào `InvoiceScheduleChipData` (tách khỏi `state` vì `state` không phân biệt được "chưa có hoá đơn" với "có hoá đơn nháp"), đổi `onTapPeriod` nhận nguyên chip thay vì mỗi `periodStart`, và cho cả **B-01** lẫn **T-05** mở `/bills/invoices/<id>` khi kỳ đã có hoá đơn, chỉ mở màn xem trước khi chưa có. **Không đụng vào màu/hình dạng chip** — đó là phần đã khớp Figma.

**Verify thật trên `emulator-5554`**: bấm chip T9 của hợp đồng B.4 (Nha trong So 2) → mở đúng **Hoá đơn 09/2026 · Nháp · 4.328.600đ**, không còn ra màn xem trước. Bấm tiếp "Gửi" → sheet hiện đủ **3 kênh**: SMS (đang chọn, xem trước có link mã QR), Email (`bui.duc.kien@example.test`), Zalo (Sắp có, khoá). `flutter analyze` sạch.
