# 2026-09-09

## 08:09 — Dream (qua Claude, Cowork)

**Tính năng/khu vực**: Docs — xử lý 7 phát hiện từ gap analysis Version 3 (Đợt 2)

**Thay đổi**:
- Xác minh 7 nhận xét trong `PHASE1GAPANALYSISV2.md` bằng cách đối chiếu trực tiếp với `docs/*.md` — cả 7 đều đúng (xem `Claude outputs/PHASE1-GAP-ANALYSIS-VERIFICATION.md`). Sau đó chốt hướng sửa cho từng mục với Dream:
- **`tb_tenant` thêm `houseId` bắt buộc** — sửa `DATABASE.md`, `BUSINESS-RULES.md` (BR-DATA-05), `REQUIREMENTS.md` (FR-TEN-01), `SCREEN-SPEC.md` (T-01, T-02).
- **Thêm rule `BR-METER-14`** — sửa chỉ số MOVE_OUT sau khi hợp đồng đã thanh lý, điều chỉnh qua `damageDeduction`/`settlementNote` — sửa `BUSINESS-RULES.md`, `DATABASE.md` (mô tả field `damageDeduction`).
- **Phí dịch vụ/phí định kỳ không chia theo ngày khi đổi khách giữa tháng** — tính trọn cho hợp đồng đang Active lúc tạo hoá đơn, phòng trống thì chủ nhà tự chịu — sửa `BUSINESS-RULES.md` (BR-BILL-08), `SCREEN-SPEC.md` (B-02), `USER-FLOWS.md` (Flow #3).
- **Làm rõ phòng `FLAT` vẫn ghi chỉ số đầy đủ** (chỉ không dùng tính tiền, có chủ đích để theo dõi tiêu thụ) — sửa `BUSINESS-RULES.md` (BR-READ-05), `REQUIREMENTS.md` (FR-READ-07).
- **Bỏ hẳn tính năng đổi số điện thoại** — SĐT chỉ xem, không sửa; thêm `BR-ROLE-09` — sửa `BUSINESS-RULES.md`, `DATABASE.md`, `REQUIREMENTS.md` (FR-MGR-01), `SCREEN-SPEC.md` (P-02), `USER-FLOWS.md` (Flow #4).
- **Giữ nguyên nhóm nhà theo `ownerFullName`** (H-01) — không sửa, chấp nhận rủi ro gõ sai.
- **Đổi hướng ngôn ngữ UI: song ngữ Anh–Việt ngay từ Phase 1** (trước đó dự kiến dời Phase 2) — chọn ở Profile, thêm màn mới **P-08 — Language Setting** (tổng màn hình 32 → 33) — sửa `DESIGN-SYSTEMS.md` (mục 0), `REQUIREMENTS.md` (NFR-02, thêm FR-MGR-05), `PRODUCT-OVERVIEW.md` (mục 5.2, mục 8 KPI), `SCREEN-SPEC.md` (thêm P-08, cập nhật P-01, tổng màn hình), `USER-FLOWS.md` (Flow #4).
- Ghi lại toàn bộ quyết định vào `DECISIONS.md` (entry 2026-09-09) và cập nhật `CURRENT_STATUS.md` (thêm việc "Đang làm": build bổ sung Figma P-08, migration DB kèm `tb_tenant.houseId`).
- Nhân tiện sửa 1 chỗ lệch dữ liệu cũ trong `SCREEN-SPEC.md` mục 3 (Liên kết Figma): ghi chú "Figma cần build lại 32 màn, chưa thực hiện" đã lỗi thời so với `DESIGN.md`/`CURRENT_STATUS.md` (Figma đã build xong 32 màn từ 08/09) — cập nhật lại cho khớp.

## 09:20 — Dream (qua Claude, Cowork)

**Tính năng/khu vực**: FigJam board (Version 3 — CURRENT) — đồng bộ 7 quyết định ở entry 08:09

**Thay đổi**:
- **Section 2. Tenant Management**: thêm field `houseId` (bắt buộc) vào bảng `tb_tenant`.
- **Section 3. Contract Management**: ghi chú `damageDeduction` (bảng `tb_contract`) tham chiếu `BR-METER-14`; thêm field mới `settlementNote`; ghi chú `electricityBillingMethod`/`waterBillingMethod` và `recurringFees`/`serviceFeeAmount` (bảng `tb_contract_version`) làm rõ FLAT vẫn ghi chỉ số (BR-READ-05) và không chia theo ngày (BR-BILL-08).
- **Section 4. Bill Management**: ghi chú `serviceFeeAmount`/`recurringFees` (bảng `tb_invoice`) không chia theo ngày, phòng trống chủ nhà tự chịu (BR-BILL-08).
- **Section 1. House/Room Management + Meter Reading**: bổ sung vào sticky ghi chỉ số — phòng FLAT không được miễn ghi chỉ số (BR-READ-05).
- **Section 5. User Setting**: ghi chú `phone` (bảng `tb_user`) không cho đổi sau khi tạo tài khoản (BR-ROLE-09); thêm shape **Menu: Language Setting (English / Tiếng Việt) — NEW · P-08** nối từ Menu User Profile.
- Mục H-01 (nhóm nhà theo `ownerFullName`) — không sửa, đúng như quyết định giữ nguyên.
- Đã xem lại toàn bộ 5 khu vực bằng `get_figjam`/screenshot để xác nhận nội dung đúng sau khi sửa.

## 09:45 — Dream (qua Claude, Cowork)

**Tính năng/khu vực**: Docs — điều chỉnh lại mục 7 (ngôn ngữ) của entry 08:09

**Thay đổi**:
- **Bỏ màn riêng P-08 — Language Setting**, thay bằng 1 dòng "Ngôn ngữ / Language" inline ngay trong **P-01**, chọn xong áp dụng tại chỗ, không điều hướng đi đâu — tổng màn hình trở lại **32** (không tăng lên 33).
- **Mở rộng từ song ngữ Anh–Việt lên 3 ngôn ngữ: English / Tiếng Việt / 한국어**, ngôn ngữ khác vẫn để Phase 2.
- Sửa `REQUIREMENTS.md` (`FR-MGR-05`, `NFR-02`), `SCREEN-SPEC.md` (xoá P-08, cập nhật P-01 + Screen Inventory 33→32 + ghi chú Figma), `DESIGN-SYSTEMS.md` (mục 0), `PRODUCT-OVERVIEW.md` (mục 5.2, mục 8 KPI), `USER-FLOWS.md` (Flow #4, node ngôn ngữ đổi thành nhánh inline).
- Ghi quyết định vào `DECISIONS.md` (entry mới "2026-09-09 (Đợt 3)", không sửa entry cũ) và cập nhật `CURRENT_STATUS.md` (mục "Đang làm" + dòng tổng kết 09/09).
- **Chưa cập nhật FigJam**: shape "Menu: Language Setting ... P-08" thêm ở entry 09:20 (Section 5) vẫn còn theo thiết kế cũ (2 ngôn ngữ, màn riêng) — cần sửa lại nếu Dream muốn đồng bộ tiếp.

## 10:15 — Dream (qua Claude, Cowork)

**Tính năng/khu vực**: Docs — xử lý 3 phát hiện A/B/C từ `PHASE1GAPANALYSISV3.md` (Đợt 4)

**Thay đổi**:
- Xác minh `PHASE1GAPANALYSISV3.md`: 7 điểm đợt 2/3 đều đã xử lý đúng; 2 điểm còn lại (D — `CHANGELOG.md` chưa cập nhật từ 04/09; E — quyết định ngôn ngữ đổi 2 lần trong ngày) **không thuộc phạm vi sửa lần này** theo yêu cầu của Dream — D vẫn còn tồn đọng, E coi là bình thường (đã tự sửa FigJam).
- **(A) Mất SIM điện thoại**: chốt coi là sơ suất người dùng, **Phase 1 không xử lý** (không có quy trình hỗ trợ thủ công); **Phase 2** sẽ thêm kênh khôi phục qua **OTP gửi email đã đăng ký** (khi đó `email` trở thành bắt buộc) — sửa `BUSINESS-RULES.md` (BR-ROLE-09), `REQUIREMENTS.md` (FR-AUTH-05). Nhân tiện phát hiện và bổ sung field `email` (đang thiếu hẳn) vào bảng `tb_user` trong `DATABASE.md` — field này đã được P-02/FR-MGR-01 nhắc tới từ trước nhưng schema chưa từng có.
- **(B) Ràng buộc Tenant cùng nhà với hợp đồng (T-04)**: hỗ trợ 2 chiều chọn — chọn Nhà/phòng trước (mặc định) thì Tenant Pool tự lọc theo nhà đó, pool rỗng thì tạo nhanh Tenant mới; hoặc chọn Tenant trước thì danh sách Nhà/phòng lọc ngược lại theo nhà của Tenant đó — thêm rule mới `BR-CTR-13`, requirement mới `FR-CTR-11` — sửa `BUSINESS-RULES.md`, `REQUIREMENTS.md`, `SCREEN-SPEC.md` (T-04), `USER-FLOWS.md` (Flow #2, thêm nhánh rẽ theo chiều chọn).
- **(C) Ngôn ngữ thông báo Zalo/SMS gửi Tenant**: cố định **tiếng Anh** ở Phase 1, độc lập hoàn toàn với ngôn ngữ UI tài khoản (EN/VI/KO chọn ở P-01) — thêm rule mới `BR-NOTI-07` — sửa `BUSINESS-RULES.md`, `REQUIREMENTS.md` (FR-NOTI-02), `DESIGN-SYSTEMS.md` (mục 0).
- Ghi lại toàn bộ quyết định vào `DECISIONS.md` (entry mới "2026-09-09 (Đợt 4)", không sửa các entry cũ) và cập nhật `CURRENT_STATUS.md` (mục "Giai đoạn", giữ nguyên entry đợt 2-3 phía dưới).

## 11:05 — Dream (qua Claude, Cowork)

**Tính năng/khu vực**: Docs — sửa lại 1 phần quyết định đợt 4 + dọn 2 thiếu sót (Đợt 5)

**Thay đổi**:
- Dream tự sửa trực tiếp vào repo `BR-NOTI-07`/`FR-NOTI-02`/`DESIGN-SYSTEMS.md` mục 0 sang phương án song ngữ Anh+Việt cho tin nhắn Zalo/SMS (kèm vài lỗi gõ trùng chữ), rồi yêu cầu Claude chốt lại theo hướng khác.
- **Đổi ngôn ngữ nội dung SMS/Zalo gửi Tenant: tiếng Anh (đợt 4) → tiếng Việt** — lý do: gửi song song cả 2 ngôn ngữ trong 1 tin nhắn làm tăng độ dài, đội chi phí gửi tin (đặc biệt SMS tính phí theo đoạn ký tự). Sửa `BUSINESS-RULES.md` (`BR-NOTI-07`), `REQUIREMENTS.md` (`FR-NOTI-02`), `DESIGN-SYSTEMS.md` (mục 0).
- Khôi phục lại nguyên văn quyết định gốc của entry "Đợt 4" trong `DECISIONS.md` (đã bị ghi đè thành song ngữ khi Dream tự sửa) để giữ đúng nguyên tắc append-only của file — quyết định mới (tiếng Việt) ghi vào entry mới "Đợt 5", không sửa đè lịch sử.
- **Bổ sung tham chiếu `BR-CTR-13` còn thiếu trong `DATABASE.md`**: đợt 4 đã thêm rule này vào `BUSINESS-RULES.md`/`FR-CTR-11` nhưng quên chú thích lại ở mô tả field `tenantId` của `tb_contract` — nay bổ sung.
- **Cập nhật `docs/CHANGELOG.md`** (file gốc, khác `changelog/*.md` hàng ngày) — bổ sung đầy đủ mục "Changed" cho các đợt 2/3/4/5 trong ngày 09/09/2026, đóng phát hiện D còn tồn đọng từ `PHASE1GAPANALYSISV3.md` (file này chưa được cập nhật từ 2026-09-04).
- Cập nhật `CURRENT_STATUS.md` (thêm bullet đợt 5, sửa lại bullet đợt 4 cho đúng lịch sử: tiếng Anh, không phải song ngữ).
- **Không đụng vào FigJam** cho đợt này — chỉ sửa repo theo đúng yêu cầu của Dream.

## 11:30 — Dream (qua Claude, Cowork)

**Tính năng/khu vực**: Figma design file (32 màn, official wireframes) — bổ sung dòng "Ngôn ngữ / Language" vào P-01, theo yêu cầu "sửa thiết kế UI trên Figma cho khớp docs mới nhất"

**Thay đổi**:
- Xác định qua `CURRENT_STATUS.md`/`SCREEN-SPEC.md`/`DESIGN-SYSTEMS.md` (mục 0): việc còn tồn đọng duy nhất phía Figma là thêm 1 dòng "Ngôn ngữ / Language" vào **P-01 — Profile & Settings** (`node-id 220:4706`, file [DESIGN.md](../docs/DESIGN.md)); FigJam board Dream báo đã tự sửa riêng nên không đụng tới trong đợt này.
- Dùng `use_figma` (Figma MCP) thêm 1 row mới vào section "Other" của P-01, nằm giữa "Notification center" và "Log out": icon Material Symbols `language` + nhãn "Ngôn ngữ / Language" + 3 chip lựa chọn **EN / VI / KO** (EN mặc định) — tái sử dụng đúng component **Chip** (State=Default/Active) và các biến màu/bo góc/border của Design System V3 (không tạo token mới). Đúng theo quyết định 09/09 (đợt 2-3): inline picker tại chỗ, không mở màn riêng, không thêm màn mới.
- Tăng chiều cao khung màn P-01 từ 829px → 883px (nội dung tự giãn theo auto-layout, không cần chỉnh tay từng phần tử) để "Log out" và Banner phía dưới không bị cắt sau khi thêm dòng mới. Đã chụp lại màn hình xác nhận layout đúng, không chồng/lệch.
- Tổng số màn Figma **giữ nguyên 32** — chỉ thêm nội dung vào màn có sẵn.
- Cập nhật `CURRENT_STATUS.md`: chuyển việc này từ "Đang làm" sang "Đã xong".

## 12:15 — Dream (qua Claude, Cowork)

**Tính năng/khu vực**: Figma prototype (toàn bộ 32 màn) — rà soát và bổ sung liên kết click/chuyển màn còn thiếu, theo yêu cầu "bổ sung prototype giữa các menu, di chuyển, click...."

**Thay đổi**:
- Rà soát có hệ thống toàn bộ 32 màn bằng `use_figma` (Figma MCP): liệt kê mọi Menu row, List card, Button, FAB, Chip, Segmented control, tab bottom-nav, Top bar và kiểm tra `reactions` (prototype) của từng cái.
- **Xác nhận đã ổn từ đợt build 08/09, không cần sửa**: nút back (`arrow_back`) trên mọi Top bar, 6 dòng menu gốc P-01, FAB ở mọi màn danh sách, phần lớn Button điều hướng chính (Save/Cancel giữa các form, Personal profile/Payout/Change password/Manager accounts/Notification center/Log out ở P-01, toàn bộ luồng T-04→T-09, B-01→B-05...) — đều đã nối `ON_CLICK → NAVIGATE` đúng màn đích.
- **Phát hiện thật sự thiếu và đã bổ sung**:
  - **Chip** (30 instance: bộ lọc ở H-03/T-01/T-02/B-01/H-05 + 3 chip Ngôn ngữ mới thêm ở P-01) và **Segmented control** (4 instance: S-03/H-03/T-01/T-02) — hoàn toàn chưa có tương tác click nào. Đã nối 1 lần duy nhất ở **component gốc** (master variant): Chip "State=Default" ↔ "State=Active", Segmented control "Selected=First" ↔ "Selected=Second", kiểu **Change to** (đổi trạng thái tại chỗ, không điều hướng màn) — Figma tự động áp dụng cho MỌI instance có sẵn trên toàn file, kể cả 3 chip EN/VI/KO ở P-01 (đóng luôn câu hỏi để ngỏ từ mục 11:30).
  - **T-06 — Contract Create**: nút "+ Quick add tenant" → nối sang **T-04 — Tenant Create/Edit**, đúng flow 2 chiều chọn Nhà/Tenant (`BR-CTR-13`/`FR-CTR-11`, đợt 4).
  - **B-04 — Invoice Detail** (tên trên Figma; nội dung trùng mục "B-05 — Invoice Detail" trong `SCREEN-SPEC.md`, xem phát hiện lệch số bên dưới): nút "Delete draft" → nối về **B-01 — Bills List**.
- **Cố tình không nối** (hành động tại chỗ hoặc ngoài app, không có màn đích trong 32 màn): "Call"/"Zalo" (T-03), "+ Add fee"/"+ Add other fee" (H-05/T-06/B-02), "Select all ready"/"Clear" (B-03), "Apply to all houses" (P-03), "Mark as collected"/"Undo collected" (B-04 — đổi trạng thái tại chỗ theo đúng `SCREEN-SPEC.md`, không rời màn).
- **Phát hiện phụ (CHƯA sửa, cần Dream xác nhận hướng xử lý)**: nhãn số màn tab Tenant & Contract trong `SCREEN-SPEC.md` (T-03→T-10) đang lệch so với tên màn thực tế trên Figma — ví dụ doc ghi "T-03 — Contract List", "T-04 — Create Contract", "T-06 — Renew Contract" nhưng Figma thực tế là "T-03 — Tenant Detail (View)", "T-04 — Tenant Create/Edit", "T-06 — Contract Create". Việc đối chiếu nội dung (không theo số) vẫn cho ra kết quả đúng ở trên, nhưng nên rà lại riêng để khớp số màn giữa 2 nơi, tránh nhầm lẫn về sau.
- Cập nhật `CURRENT_STATUS.md` (mục "Giai đoạn").

## 13:10 — Dream (qua Claude, Cowork)

**Tính năng/khu vực**: Figma design file — bổ sung nội dung H-03 (tách 2 tab House detail/Rooms) + sửa H-04 (nút Edit phòng), theo phản hồi "thiếu nội dung house detail... thiếu btn edit room, không biết click vào đâu để chuyển từ H-04 sang H-05"

**Thay đổi**:
- **H-04 — Room Detail (View)**: nối nút overflow (⋮ trên Top bar, đã có sẵn từ đợt build 08/09 nhưng chưa nối) → NAVIGATE → **H-05 (Room Edit)** — đây chính là thao tác "Edit" mà Dream không thấy chỗ bấm. Thêm nút mới **"View reading history"** (Style=Ghost, chèn ngay dưới khối thông tin phòng) → NAVIGATE → **H-06 (Record Monthly Reading · Detail/Edit)**. Nhân tiện sửa 1 lỗi hiển thị: nút "Create contract" (chỉ dành cho phòng Vacant) đang hiện luôn dù phòng đang Occupied — nay ẩn đúng theo trạng thái phòng.
- **H-03 — Room List (theo 1 House)**: tách thành 2 tab bằng Segmented control, đúng yêu cầu "click vào 1 house ở H-01 → hiển thị 2 tab":
  - **Tab 1 "House detail"** (frame Figma mới, cùng mã màn logic H-03, đặt tên "· House Detail" — theo đúng tiền lệ H-06 đã tách "· Entry"/"· Detail/Edit" nên KHÔNG tăng tổng số màn): ảnh nhà, tên + địa chỉ, khối "House info" (loại nhà, mô tả, số phòng), khối "Owner" (Mini profile, số CMND/CCCD, mã số thuế, tài khoản nhận tiền, email), khối "Pricing & fees defaults" (giá điện/nước, phí định kỳ mặc định). Nút Edit (⋮ trên Top bar) → NAVIGATE → **H-02 (House Create/Edit)**.
  - **Tab 2 "Rooms"** (frame gốc, giữ nguyên danh sách phòng + bộ lọc + FAB, chỉ thêm Segmented control ở đầu nội dung): nối Edit (⋮) → cùng H-02.
  - 2 tab chuyển qua lại bằng chính Segmented control đó (Change to → Navigate, dissolve 0.2s), tách biệt với reaction "đổi trạng thái tại chỗ" đã set ở component gốc từ đợt 12:15 (2 instance mới này override riêng).
  - **Cả 3 house card ở H-01 nay bấm vào sẽ vào thẳng tab "House detail" trước** (trước đây vào thẳng danh sách phòng luôn) — theo đúng thứ tự Dream nêu ("tab 1 là house detail, tab 2 là danh sách room"). **Cần Dream xác nhận lại**: nếu muốn giữ hành vi cũ (bấm vào house → vào thẳng "Rooms"), báo lại để đổi hướng điều hướng từ H-01.
- Đã chụp màn hình xác nhận layout cả 2 frame H-03 không bị cắt/lệch (House Detail cao 1084px sau khi giãn theo nội dung; Rooms giữ nguyên 812px, không cần giãn).
- Tổng số màn Figma **giữ nguyên 32**.
- Cập nhật `CURRENT_STATUS.md` (mục "Giai đoạn" + "Đã xong").

## 13:35 — Dream (qua Claude, Cowork)

**Tính năng/khu vực**: Figma design file — đổi bộ lọc nhà ở T-02 (tab Contracts) từ 4 chip riêng lẻ sang 1 dropdown, theo yêu cầu "tại menu T-02 phần 4 tag All house, Binh An, Phu Nhuan, Tan Binh, tôi muốn đổi thành dropdown"

**Thay đổi**:
- Xác định đúng màn qua Figma (không theo số `SCREEN-SPEC.md`, xem phát hiện lệch số ở mục 12:15): frame Figma tên **"T-02 — Tenant & Contract (Contracts)"** (tab "Contracts" của Segmented control Tenant & Contract) — đúng nội dung "Contract List" trong `SCREEN-SPEC.md` (mục ghi rõ "Filter theo tên nhà (dropdown)", trước giờ Figma lại làm bằng 4 Chip riêng — nay sửa lại cho khớp docs).
- Gỡ 3 Chip "Binh An"/"Phu Nhuan"/"Tan Binh", giữ lại đúng 1 phần tử duy nhất thay cho cả hàng: **"All houses" + icon `expand_more`** (tách khỏi component Chip gốc — vì component Chip dùng cho toggle chọn/bỏ chọn tại chỗ, còn ô này giờ đóng vai trò mở dropdown chọn 1 trong nhiều nhà, ngữ nghĩa khác), giữ nguyên màu nền/viền/bo góc/màu chữ như style Chip "Active" sẵn có để không lệch Design System.
- Hàng filter dưới (Active/Ending soon/Ended) giữ nguyên, không đổi.
- Đã chụp màn hình xác nhận: dropdown "All houses ⌄" hiển thị đúng, không chồng/lệch với hàng chip trạng thái phía dưới hay danh sách hợp đồng.
- **Phát hiện phụ (CHƯA sửa, chỉ ghi nhận)**: màn **B-01 — Bills List** cũng có đúng pattern 4-chip lọc theo nhà y hệt ("All houses"/Binh An/Phu Nhuan/Tan Binh) — nếu Dream muốn đồng bộ luôn thành dropdown cho nhất quán, cần yêu cầu riêng.
- Cập nhật `CURRENT_STATUS.md` (mục "Giai đoạn").

## 13:45 — Dream (qua Claude, Cowork)

**Tính năng/khu vực**: Figma design file — ghi nhận đồng bộ trạng thái mới nhất lên repo (Dream tự sửa trực tiếp trên Figma + phần Claude vừa sửa cùng ngày)

**Thay đổi**:
- Dream tự chỉnh sửa trực tiếp trên file Figma chính thức (thêm/xoá phần tử, căn chỉnh lại vị trí...), song song với các phần Claude đã sửa trong ngày (dòng Ngôn ngữ P-01, prototype 32 màn, H-03/H-04, dropdown lọc nhà T-02).
- Xác nhận: **file Figma hiện đã phản ánh đúng thiết kế mới nhất** (kết hợp cả 2 nguồn chỉnh sửa) — ghi nhận lại vào repo để không ai làm việc trên bản cũ.
- Cập nhật `docs/DESIGN.md` (dòng "Last updated" + mục "Nguồn thiết kế") và `CURRENT_STATUS.md` (mục "Giai đoạn") phản ánh đúng trạng thái đồng bộ này.

## 13:10 — dungtv (qua Claude Code)

**Tính năng/khu vực**: Đối chiếu SCREEN-SPEC.md với Figma thật — đóng phát hiện phụ số màn T-0x lệch

**Thay đổi**:
- Đọc trực tiếp Figma qua Figma MCP (`get_metadata`, `get_screenshot` trên các frame T-01→T-10 khu vực Tenant & Contract) để xác minh phát hiện phụ đã ghi ở mục 12:15 hôm nay ("nhãn số màn T-03→T-10 lệch so với Figma thật").
- Xác nhận lệch gần hết (8/10 màn), chỉ T-05/T-08 khớp sẵn. Phát hiện thêm 1 màn Figma có mà `SCREEN-SPEC.md` bản cũ chưa mô tả: **Tenant Detail (View)** (nay T-03) — đọc screenshot, viết spec mới đầy đủ.
- Xác nhận **Renew Contract và Amend Contract gộp chung 1 frame Figma** ("Contract Renew / Amend") — khác mô tả cũ (2 màn tách riêng). Nội dung frame chỉ có field kiểu Renew, không có UI đổi danh sách phòng như phần "Amend" mô tả cũ từng ghi — để lại ghi chú cần Dream xác nhận trước khi code UI màn này.
- Viết lại toàn bộ `docs/SCREEN-SPEC.md` mục 1.4 (Screen Inventory) và 2.3 (đặc tả chi tiết T-01→T-10) khớp đúng Figma, theo yêu cầu dungtv "làm theo Figma mới" (không chờ Dream xác nhận renumbering, chỉ xác nhận riêng khoảng trống tính năng Amend).
- Sửa tham chiếu chéo bị ảnh hưởng: `docs/REQUIREMENTS.md` (FR-CTR-11), `docs/BUSINESS-RULES.md` (BR-CTR-13) đổi "T-04"→"T-06"; `docs/DESIGN-SYSTEMS.md` đổi "T-09"→"T-10" (Invoice Schedule Preview).
- Ghi đầy đủ bảng mapping số cũ→mới vào `docs/DECISIONS.md` (2026-09-09, Đợt 6). Cập nhật `docs/CURRENT_STATUS.md` đóng phát hiện phụ.

## 13:29 — dungtv (qua Claude Code)

**Tính năng/khu vực**: Chốt bỏ tính năng đổi danh sách phòng (Amendment) khỏi Phase 1

**Thay đổi**:
- Trao đổi trực tiếp với dungtv về khoảng trống để ngỏ ở entry 13:10 (màn T-07 không có UI đổi phòng). Phân tích ảnh hưởng kỹ thuật: giữ tính năng này cần thêm cơ chế versioning cho `tb_contract_room` (chưa có trong schema hiện tại) để giữ đúng lịch sử phòng theo từng `contract_version`; bỏ đi thì giữ nguyên schema đơn giản đã thiết kế.
- dungtv xác nhận theo hướng nghiệp vụ: đổi phòng thuê = kết thúc hợp đồng cũ (T-09) + tạo hợp đồng mới (T-06), không sửa ngầm hợp đồng đang có — quyết định luôn, không cần hỏi lại Dream.
- Cập nhật: `docs/BUSINESS-RULES.md` (BR-CTR-07 đánh dấu bỏ khỏi Phase 1; BR-VER-03/BR-VER-06 sửa lại), `docs/REQUIREMENTS.md` (FR-CTR-05/07, mục 6 Ngoài phạm vi), `docs/PRODUCT-OVERVIEW.md` (mục 5.2), `docs/USER-FLOWS.md` (Flow #4, bỏ nhánh MOVE_OUT khi loại phòng), `docs/SCREEN-SPEC.md` (T-07, đổi ghi chú "chờ Dream xác nhận" thành quyết định dứt khoát).
- Ghi đầy đủ vào `docs/DECISIONS.md` (2026-09-09, Đợt 7), đóng nốt trong `docs/CURRENT_STATUS.md`.

## 13:36 — dungtv (qua Claude Code)

**Tính năng/khu vực**: Viết lại migration database từ đầu theo Version 3

**Thay đổi**:
- Theo yêu cầu dungtv ("clear hết database tuần trước làm lại từ đầu cho sạch gọn"), tạo `supabase/migrations/20260909133320_v3_schema_rebuild.sql`: DROP toàn bộ 11 bảng Version 1, tạo mới 12 bảng Version 3 đúng `docs/DATABASE.md` (tb_user, tb_house, tb_user_house_access, tb_room, tb_tenant, tb_contract, tb_contract_room, tb_contract_version, tb_invoice, tb_electricity_reading, tb_water_reading, tb_device_token).
- RLS đầy đủ theo mô hình vai trò-theo-từng-nhà (`tb_user_house_access`, khoá theo SĐT từ JWT), 2 trigger (auto-cấp quyền owner khi tạo nhà; đồng bộ `is_active` trên `tb_contract_room` để enforce 1 phòng/1 hợp đồng Active qua partial unique index).
- File migration Version 1 cũ giữ nguyên làm lịch sử (không xoá), chỉ bị nội dung migration mới ghi đè khi áp lên DB thật.
- Áp lên Supabase dev qua `supabase db push` — thành công. Verify qua Supabase Management API (curl trực tiếp vào REST endpoint `database/query`, vì máy không có psql): đủ 12 bảng, RLS bật cả 12, 20 policy, 3 trigger, 6 function; xác nhận sạch 11 bảng V1 cũ.
- 2 điểm tự quyết cần Dream xác nhận sau: bỏ `passwordHash` khỏi `tb_user` (khớp quyết định dùng Supabase Auth, có thể do DATABASE.md sót chưa dọn); thêm bảng `tb_device_token` cho push (khớp số đếm "12 bảng" trong DATABASE.md mà mục thực thể chỉ liệt kê 11).
- Ghi đầy đủ vào `docs/DECISIONS.md` (2026-09-09, Đợt 8), cập nhật `docs/CURRENT_STATUS.md`.

## 13:44 — dungtv (qua Claude Code)

**Tính năng/khu vực**: Viết lại 3 Edge Function theo schema Version 3

**Thay đổi**:
- `supabase/functions/_shared/vietqr.ts` (mới): module dùng chung sinh mã VietQR/NAPAS-247 chuẩn EMVCo (TLV + CRC16-CCITT-FALSE). Verify CRC16 đúng test vector chuẩn quốc tế bằng Python độc lập. Không cần tài khoản bên thứ 3 (chuẩn mở, khác SMS/Zalo).
- `generate-invoice/index.ts`: viết lại hoàn toàn — hỗ trợ `single`/`batch`, đọc chỉ số qua `previousReadingId`, tính điện/nước/tiền nhà/phí dịch vụ/phí định kỳ đúng BR-BILL-01..13, tự sinh QR, khoá chỉ số đã dùng (`invoice_id`). TODO: prorate tiền nhà theo ngày khi MOVE_IN/MOVE_OUT giữa kỳ.
- `generate-payment-qr/index.ts` (mới): sinh lại QR cho 1 hoá đơn đã có, dùng chung module vietqr.
- `send-notification/index.ts`: đổi nguồn người nhận Push sang `tb_user_house_access`/`tb_device_token` (schema Version 3).
- Deploy thử cả 3 lên Supabase dev — thành công, không lỗi compile.
- Cập nhật `docs/API.md`, `docs/CURRENT_STATUS.md`, `docs/DECISIONS.md` (2026-09-09, Đợt 9).

## 14:04 — dungtv (qua Claude Code)

**Tính năng/khu vực**: Cài Android Studio + code khung app/Auth, chạy thật trên emulator

**Thay đổi**:
- Cài Android Studio (.dmg trực tiếp, không cần tài khoản) + Android SDK qua `sdkmanager` CLI (platform 35/36, build-tools, emulator, system-image), dùng JDK bundled sẵn trong Android Studio. Tạo AVD `rentmanager_pixel` (Pixel 6, API 35). `flutter doctor` Android toolchain xanh. Xcode để dungtv tự cài (cần Apple ID + App Store).
- Thêm `go_router`, `google_fonts`, `pin_code_fields` vào `pubspec.yaml`.
- Viết `lib/core/theme.dart` (design tokens từ DESIGN-SYSTEMS.md), `lib/core/router.dart` (go_router + auto-redirect theo session), `lib/data/auth_repository.dart` (Supabase Auth Phone+OTP+Password), `lib/core/providers.dart` (Riverpod authStateProvider).
- Viết 3 màn: `shared/splash_screen.dart` (S-00), `shared/login_screen.dart` (S-01), `shared/signup_screen.dart` (S-02, 3 bước SĐT→OTP→mật khẩu), `landlord/home_placeholder_screen.dart` (tạm, chờ code H-01 thật).
- Build + chạy thật lên emulator (`flutter run`). Phát hiện lỗi theme (chữ trắng vô hình trên nền sáng) qua chụp màn hình `adb screencap` — sửa `theme.dart` set màu tường minh cho titleMedium/bodyMedium/labelLarge. Build lại, verify lại bằng ảnh chụp — đúng. Test thêm điều hướng sang Signup bằng `adb shell input tap` — đúng.
- `flutter analyze` sạch. Ghi bài học quy trình vào `docs/DECISIONS.md` (2026-09-09, Đợt 10): luôn build+chụp màn hình thật trước khi báo xong 1 màn UI, không chỉ dựa vào analyze.

## 14:09 — dungtv (qua Claude Code)

**Tính năng/khu vực**: Dùng logo thật thay icon placeholder

**Thay đổi**:
- dungtv phản hồi: UI chưa giống Figma mới nhất, yêu cầu dùng đúng file logo trong `design/Logo/`.
- Thêm `flutter_svg`, copy 3 file logo (`biztown-rent-manager-lockup.svg`, `-lockup-on-navy.svg`, `biztown-wordmark.svg`) vào `src/assets/logo/`, khai báo trong `pubspec.yaml`.
- `splash_screen.dart`: dùng `lockup-on-navy.svg` (đã tự vẽ sẵn nền navy bên trong SVG). `login_screen.dart`: dùng `lockup.svg` (nền trong suốt, icon navy) thay `Icons.home_work_outlined`.
- Build lại + chụp màn hình verify — logo thật hiển thị đúng.
- ⚠️ Chưa verify được khớp pixel với Figma thật (S-00/S-01/S-02) vì bị giới hạn quota Figma MCP (View seat) — cần dungtv gửi ảnh chụp Figma trực tiếp hoặc chờ quota hồi.

## 14:24 — dungtv (qua Claude Code)

**Tính năng/khu vực**: Sửa S-00/S-01/S-02 khớp đúng ảnh Figma thật

**Thay đổi**:
- dungtv gửi trực tiếp 2 ảnh (Figma S-00/S-01/S-02/S-03 và app chạy thật), yêu cầu giải thích rõ vì sao chưa giống Figma.
- Trả lời: bản trước code từ mô tả text (SCREEN-SPEC.md), không có ảnh Figma thật do bị rate-limit MCP đúng lúc — text spec chỉ nói có gì trên màn, không nói cách trình bày, nên tự suy diễn layout/copy sai khá nhiều.
- Sửa `splash_screen.dart`: dùng đúng logo `lockup-on-navy.svg` + tagline đúng "Houses and Rooms Renting Management Tool" (trước để nhầm "Simple rental management").
- Viết lại `login_screen.dart`: canh trái toàn bộ, label tĩnh phía trên field (`_FieldLabel`), đúng copy "Log in"/"Welcome! Please sign in to continue.", "Forgot password?" canh phải, nút full-width.
- Viết lại hẳn `signup_screen.dart`: 1 màn liên tục (không phải 3 bước tách rời) — AppBar "Create account"/"Main Manager", `_ProgressDots` (4 chấm), phần OTP + "STEP 3 — SET PASSWORD" cộng dồn hiển thị.
- Build lại + chụp màn hình Login (khớp rất sát Figma). Tạm ép state để chụp thử phần "STEP 3" (chưa gọi được OTP thật do chưa có SMS provider) — chụp xong revert lại ngay code debug, không để sót trong commit.
- `flutter analyze` sạch sau khi revert.

## 14:45 — dungtv (qua Claude Code)

**Tính năng/khu vực**: Đổi tên app + icon app (trong lúc chờ Dream redesign màn Verify)

**Thay đổi**:
- dungtv yêu cầu: trong lúc chờ Dream thiết kế lại màn Verify trên Figma, đổi tên app đúng tên thật + đổi icon app (đang để icon Dart mặc định).
- Đổi `android:label` trong `AndroidManifest.xml` và `CFBundleDisplayName` trong `Info.plist`: `"rent_manager"`/`"Rent Manager"` → `"BizTown Rent Manager"`.
- Tạo icon app từ `design/Logo/biztown-rent-icon.svg` (nền navy bo góc + biểu tượng bar-chart trắng/cam). `cairosvg` không convert được do thiếu thư viện native Cairo (không có Homebrew trên máy để cài) — thay vào đó dựng lại chính xác hình học của SVG (các rounded-rect toạ độ cố định) bằng Python/Pillow, render supersample 8x rồi downsample về PNG 1024×1024 để có viền mịn.
- Thêm `flutter_launcher_icons: ^0.13.1` vào `dev_dependencies`, copy PNG vào `src/assets/icon/app_icon.png`, cấu hình trong `pubspec.yaml`, chạy `dart run flutter_launcher_icons` — sinh icon cho toàn bộ mipmap Android + AppIcon.appiconset iOS.
- `flutter analyze` sạch. Build APK debug, cài + mở app thật trên emulator, chụp màn hình Home/App drawer — xác nhận icon mới (navy + bar-chart) và tên "BizTown Rent Manager" hiển thị đúng, không còn icon/tên mặc định của Flutter.
