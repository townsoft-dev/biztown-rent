# 2026-09-04

## 13:39 — Dream (qua Claude, Cowork)

**Tính năng/khu vực**: Rà soát chéo docs/ + đối chiếu Figma Version 2 + bổ sung field còn thiếu trên Figma

**Thay đổi**:
- Rà soát toàn bộ `docs/*.md` để tìm mâu thuẫn/thiếu sót giữa các file, phát hiện:
  - 3 quy ước đặt tên khác nhau cho cùng 1 tập entity: `tb_xxx` (SCREEN-SPEC.md, USER-FLOWS.md) vs không tiền tố (DATABASE.md) vs PascalCase (REQUIREMENTS.md mục 5) — chưa thống nhất.
  - `DATABASE.md` entity `invoice` thiếu field thời điểm gửi/thu tiền (`sentAt`/`collectedAt` hay tương đương) dù SCREEN-SPEC.md (B-03) và bản thiết kế Figma đều cần hiển thị lịch sử này.
  - `REQUIREMENTS.md` FR-NOTI-01 nhắc tới push "đến hạn ghi số" nhưng `BUSINESS-RULES.md` mục 5 (Notification Rules) không có rule tương ứng, `SCREEN-SPEC.md` (S-03) cũng chưa định nghĩa điều hướng khi tap loại thông báo này.
  - `ARCHITECTURE.md` mô tả Manager đăng nhập qua Supabase Auth như Landlord, nhưng schema `manager_account` trong `DATABASE.md` tự lưu `phone`/`passwordHash` riêng — chưa rõ ràng Manager có dùng chung Supabase Auth (`auth.users`) hay auth tự viết riêng.
  - `docs/CHANGELOG.md` (bản tóm tắt release) chưa được cập nhật theo quy trình ở CLAUDE.md — thiếu toàn bộ các mốc từ khi build xong Figma Version 2 đến các đợt sửa docs hôm nay.
- Đối chiếu 22 màn Figma Version 2 (page `MVP Wireframes (EN) - Version2`, file `AElzfTBuL8YyA8OJ85f7aX`) với mô tả trong `SCREEN-SPEC.md` — hầu hết khớp tốt (đúng màu, đúng bottom nav, đúng badge trạng thái), phát hiện 4 màn thiếu field so với spec: N-02 (thiếu Ngày sinh/Email/Ghi chú), C-02 (thiếu Thông tin môi giới), C-03 (thiếu Phí định kỳ/Phạt trễ hạn/Thông tin môi giới hiển thị), U-03 (thiếu Ghi chú).
- **Bổ sung trực tiếp vào Figma Version 2** (qua Figma MCP) đúng 4 màn trên, giữ nguyên style hiện có (màu, font Inter, bo góc, spacing 8px grid):
  - N-02: thêm Date of Birth + Email (2 cột), Note (textarea) — frame cao thêm từ 812 lên 860px để đủ chỗ, nút Cancel/Save dời xuống theo.
  - C-02: thêm section "Broker info (optional)" — tên/agency, liên hệ, phí (3 ô).
  - C-03: thêm 2 hàng dữ liệu Recurring fees / Late fee, và 1 hàng Broker (optional) — dời khối nút hành động (Renew/Amend/History/End) xuống 76px.
  - U-03: thêm Note (optional) giữa toggle Active và khối nút.
- Đã verify lại bằng screenshot từng màn sau khi sửa — không bị vỡ layout, không đè chữ.

## 15:20 — Dream (qua Claude, Cowork)

**Tính năng/khu vực**: Xử lý 4/5 mâu thuẫn phát hiện lúc rà soát docs/ (bỏ lại điểm thứ 5 — `docs/CHANGELOG.md` chưa cập nhật — theo yêu cầu, chưa xử lý)

**Thay đổi**:
- **Quy ước đặt tên bảng:** thống nhất `snake_case`, không tiền tố. Sửa `SCREEN-SPEC.md`, `USER-FLOWS.md` (bỏ toàn bộ tiền tố `tb_`), `BUSINESS-RULES.md` (2 chỗ còn sót `tb_manager_house_access`), `REQUIREMENTS.md` mục 5 (đổi từ PascalCase sang snake_case khớp DATABASE.md). Thêm ghi chú quy ước vào đầu mục "Các thực thể" trong `DATABASE.md`.
- **`invoice` thiếu mốc thời gian:** thêm `sentAt`, `collectedAt` vào entity `invoice` trong `DATABASE.md`.
- **"Đến hạn ghi số" thiếu rule/điều hướng:** thêm `BR-NOTI-05` vào `BUSINESS-RULES.md` mục 5 (làm rõ đây là nhắc tạo hoá đơn kỳ mới, không phải bước ghi số riêng — đã bỏ theo BR-CTR-07), thêm điều hướng tương ứng vào `SCREEN-SPEC.md` S-03 (tap → màn Tạo hoá đơn).
- **Auth Manager chưa rõ ràng:** chốt Manager 1-1 với `auth.users`, tạo qua Edge Function + Supabase Admin API (`auth.admin.createUser`), không dùng `passwordHash` riêng. Sửa `DATABASE.md` (entity `manager_account`) và `ARCHITECTURE.md` (mục Auth) cho khớp nhau.
- Ghi lại toàn bộ quyết định trên vào `DECISIONS.md` (entry 2026-09-04 "Rà soát chéo docs/, chốt 4 điểm mâu thuẫn").

