## 07:52 — dungtv (qua Claude Code)

**Tính năng/khu vực**: Sửa P-03/P-06 chưa giống Figma (field "Select"/"Textarea" bị lẫn với "Readonly"), tách các thành phần form dùng lại nhiều nơi thành shared widget

**Bối cảnh**: dungtv báo P-03/P-06 chưa giống design, cụ thể dropdown "Bank name" ở P-03 vẫn sai — yêu cầu kiểm tra kỹ, sửa lại đúng Figma, và bất kỳ thành phần form nào dùng lại ở nhiều màn phải tách thành common component thay vì mỗi màn tự làm 1 kiểu.

**Nguyên nhân gốc — `AppTextField` trộn lẫn 2 biến thể Figma khác nhau thành 1**: Figma "Input field" thực ra có 4 biến thể riêng biệt: **Text** (trắng, viền, chữ đậm), **Readonly** (xám mờ, không viền, chữ nhạt vừa — dùng cho field tĩnh thật như Phone/Created at), **Select** (LUÔN trắng + viền GIỐNG Text, nhưng chữ nhạt tertiary + icon `expand_more` — dùng cho field bấm mở picker hoặc chỉ hiện giá trị đã chọn), và **Textarea** (trắng + viền, chữ tertiary). Code trước đó chỉ có khái niệm `readOnly` (2 trạng thái: trắng/xám) và gán `readOnly: true` cho MỌI field có icon `select` — đúng với Readonly nhưng sai với Select thật sự, khiến 2 field dùng `trailing: select` trong toàn app (Status ở H-05, Bank name ở P-03) đều bị hiện SAI thành ô xám mờ không viền thay vì ô trắng có viền + chữ nhạt đúng Figma. Đây là lỗi lặp lại lần 2 trong session (lần 1 đã sửa cho field "Reading period" ở H-06 hồi đợt trước) — nay sửa tận gốc trong chính `AppTextField` để không tái diễn lần 3 ở màn khác.

**Đã sửa**:
- `AppTextField`: thêm logic phân biệt rõ — `trailing: select` LUÔN render trắng + viền + chữ tertiary (bỏ qua tham số `readOnly` truyền vào, không để caller tự chọn nhầm nữa); thêm tham số `textarea: true` cho biến thể Textarea (chữ tertiary, dùng cho Note). Đã gỡ `readOnly: true` thừa khỏi 2 field Status (H-05)/Bank name (P-03) — không còn ý nghĩa gì sau khi sửa logic.
- `AppBanner`: thêm `AppBannerTone` (`info`/`warning`) — trước đó chỉ có đúng 1 tone Info (xanh) hardcode, trong khi banner cảnh báo "1 house has no payout account yet" ở P-03 cần tone Warning (cam nhạt, đúng Figma `--badge-warning-bg`/`--color-accent-orange`) nhưng bị dùng nhầm tone Info có sẵn vì không có lựa chọn khác.
- Tách 2 widget dùng riêng trong `manager_form_screen.dart` (P-06) thành shared, đúng yêu cầu tránh mỗi màn 1 kiểu: `shared/check_row.dart` (`CheckRow` — "check row" Figma, dòng checkbox dạng thẻ bo góc cho House access; sửa luôn bán kính góc bị sai 14px→12px, đúng token Figma "radius-md") và `shared/toggle_row.dart` (`ToggleRow` — khung viền chứa nhãn + `Switch`, dùng cho "Account active").
- Thêm `AppRadii.checkRow = 12.0` (token "radius-md" trên Figma) vào `theme.dart`.

**Verify trên thiết bị thật** (dữ liệu thật của dungtv — không sửa/lưu gì thêm, chỉ xem và Cancel):
- P-03: field "Bank name" giờ hiện đúng nền trắng + viền, chọn "Techcombank" hiện đúng chữ màu nhạt tertiary (trước đó nền xám mờ không viền).
- P-06: field "Note" gõ thử "Test" hiện đúng chữ tertiary (trước đó chữ đậm như field thường); "House access" check row giữ đúng hành vi disable/tên Manager đã verify trước đó, nay đúng luôn bán kính góc.
- H-05 (Room Edit) — phát hiện field "Status" dính CÙNG lỗi Select/Readonly (chưa ai báo trước đó) — verify sau khi sửa: hiện đúng nền trắng + viền + chữ nhạt, và field "Note" cũng hiện đúng chữ tertiary.
- Nhân tiện xác nhận dungtv đã tự vào P-03 sửa lại dữ liệu "Payout bank account" bị lệch cột từ trước (đã tách đúng Account name "Tran Van Dung"/Account number "012375649321") — tính năng P-03 hoạt động đúng mục đích.

`flutter analyze` sạch, `dart format` đã chạy.

## 08:06 — dungtv (qua Claude Code)

**Tính năng/khu vực**: Sửa toggle "Account active" ở P-06 bị cắt chữ (chỉ hiện "Account")

**Bối cảnh**: dungtv báo màn "Manager account" (P-06) vẫn chưa đúng Figma — switch thiếu chữ, "Account active" chỉ hiện "Account", kích cỡ switch sai. Lấy lại design context thật cho node P-06 (220:5099) xác nhận: switch Figma đúng 44×24px cố định, label "Account active" nằm trong phần không gian còn lại (`justify-content: space-between`, không có gap riêng).

**Nguyên nhân**: `ToggleRow` (tách thành shared ở đợt trước) dùng `Transform.scale(scale: 0.8)` để thu nhỏ `Switch` — nhưng `Transform` chỉ co phần VẼ, không co phần KHÔNG GIAN mà `Row` dành cho nó (layout tính theo kích thước GỐC trước khi transform), khiến label bị bóp hẹp hơn thực tế cần, tự xuống dòng và bị cắt bởi chiều cao cố định 42px của khung — chỉ còn thấy đúng 1 dòng đầu "Account".

**Đã sửa** (2 vòng, vì lần sửa đầu vẫn còn thiếu):
1. Đổi `Transform.scale` → `SizedBox(44×24) + FittedBox` để Switch có ĐÚNG kích thước cố định 44×24 cả về layout lẫn hình vẽ (đúng Figma), đồng thời bỏ khoảng cách 8px tự thêm giữa label và switch (Figma dùng `justify-content: space-between`, không có gap riêng).
2. Vẫn còn cắt nhẹ ("Account acti…") vì tỉ lệ Figma cho field này vốn đã rất khít (đo lại: khung chỉ có ~97px cho chữ, trong khi "Account active" ở đúng cỡ 13.5px cần nhỉnh hơn) — bọc label trong `FittedBox(fit: BoxFit.scaleDown)` để chữ TỰ CO vừa đủ không gian còn lại khi thiếu chỗ, thay vì cắt bớt bằng `TextOverflow.ellipsis` (mất thông tin thật). Co gần như không nhận ra bằng mắt thường, nhưng đảm bảo KHÔNG BAO GIỜ mất chữ.

**Verify trên thiết bị thật**: "Account active" hiện đầy đủ, switch đúng tỉ lệ, bấm toggle vẫn hoạt động bình thường (chuyển ON/OFF đúng).

`flutter analyze` sạch, `dart format` đã chạy.

## 08:11 — dungtv (qua Claude Code)

**Tính năng/khu vực**: Sửa switch "Account active" bị méo hình (đợt sửa trước)

**Bối cảnh**: dungtv báo switch bị méo sau lần sửa trước. Nguyên nhân: dùng `FittedBox(fit: BoxFit.fill)` để ép `Switch` gốc của Material (kích thước tự nhiên ~59×39, tỉ lệ dài/ngắn khác hẳn) vào đúng khung 44×24 theo Figma — `BoxFit.fill` kéo giãn KHÔNG giữ tỉ lệ, khiến cả track lẫn nút tròn bên trong bị bóp méo thay vì chỉ thu nhỏ đều.

**Đã sửa**: bỏ hẳn `Switch` của Material, tự vẽ `_MiniSwitch` (trong `shared/toggle_row.dart`) bằng `AnimatedContainer` (track bo tròn 44×24, màu xanh khi ON/xám khi OFF) + `AnimatedAlign` (nút tròn trắng 20×20 trượt trái/phải) — đúng đích 44×24 ngay từ đầu, không cần co kéo gì nên không có gì để méo.

**Verify trên thiết bị thật**: switch hiện đúng hình pill bo tròn, không méo, cả 2 trạng thái ON (xanh)/OFF (xám) đều đúng, bấm chuyển trạng thái mượt.

`flutter analyze` sạch, `dart format` đã chạy.

## 08:19 — dungtv (qua Claude Code)

**Tính năng/khu vực**: Sửa ô "Manager"/"Account active" ở P-06 lệch chiều cao

**Bối cảnh**: dungtv báo ô toggle vẫn thấp hơn hẳn ô "Manager" cạnh nó.

**Nguyên nhân thật sự** (khác với 2 lần sửa trước, vốn chỉ sửa switch bên trong): Figma chỉ có **1 label "Role" dùng chung** cho cả 2 ô — nhưng code dùng `AppTextField(label: 'Role', ...)` cho ô "Manager", khiến ô này TỰ có thêm label riêng (label + gap 4px + box), trong khi `ToggleRow` không có label. Ghép 2 khối kích thước khác nhau vào 1 `Row` rồi bám đáy (`crossAxisAlignment.end`) khiến 2 Ô (không phải 2 cột) không bao giờ cùng chiều cao — không sửa được chỉ bằng cách chỉnh số bên trong `ToggleRow`.

**Đã sửa**: dựng lại đúng cấu trúc Figma — 1 `FieldLabel('Role')` dùng chung phía trên, bên dưới là 1 `Row` bọc `IntrinsicHeight` + `crossAxisAlignment: CrossAxisAlignment.stretch` chứa 2 ô ngang hàng thật sự (ô "Manager" viết tay bằng `Container` thay vì `AppTextField` vì không cần label riêng nữa; `ToggleRow` bỏ `height: 42` cố định, để tự co giãn theo `IntrinsicHeight`). Cách này ép 2 ô LUÔN cùng 1 chiều cao thật (do layout engine tính, không phải số tay dễ sai), không phụ thuộc vào việc đoán đúng chiều cao `TextFormField` render ra bao nhiêu pixel.

**Verify trên thiết bị thật**: 2 ô "Manager"/"Account active" nay cao bằng nhau, top/bottom thẳng hàng, khớp Figma; switch vẫn đúng hình dạng, bấm chuyển ON/OFF bình thường.

`flutter analyze` sạch, `dart format` đã chạy.

## 08:25 — dungtv (qua Claude Code)

**Tính năng/khu vực**: Sửa switch "Account active" hơi dính chữ; xác nhận hành vi "Join from"/"Remove manager account" chỉ hiện ở Edit là đúng chủ ý

**Bối cảnh**: dungtv báo switch vẫn hơi dính vào chữ "active", và hỏi có phải "Join from"/"Remove manager account" chỉ hiện ở form Edit (không hiện ở form Tạo mới) không.

**Đã sửa (switch dính chữ)**: `ToggleRow` — giảm padding dọc từ 12→9px (khớp đúng chiều cao 42px Figma khai báo cho khung này: switch cao 24 + padding 9×2 = 42; số "py-12" Figma xuất ra mâu thuẫn với chính "h-42" nó khai báo, 12×2+24=48, nên ưu tiên khớp đúng chiều cao đã khai báo rõ ràng), thêm khoảng thở 6px giữa chữ và switch.

**Xác nhận hành vi Create vs Edit (không phải bug)**: "Join from" và "Remove manager account" chỉ hiện khi `_isEdit == true` — đây là chủ ý đã cài từ đầu, đúng logic nghiệp vụ (Manager mới tạo chưa tồn tại thì chưa có ngày gia nhập thật, và chưa có gì để xoá). Đã tự tay test full luồng trên thiết bị thật để xác nhận: tạo Manager "Demo" (SĐT `0966555666`, gán 1 Nhà) → Save → mở lại (Edit) → đúng như dự kiến: "Join from" hiện ngày tạo thật (11/09/2026), "Remove manager account" xuất hiện đầy đủ. Xoá sạch dữ liệu test ngay sau khi xác nhận.

**Verify trên thiết bị thật**: 2 ô "Manager"/"Account active" cùng cao, switch không còn dính, chữ "Account active" có khoảng thở dễ nhìn hơn.

`flutter analyze` sạch, `dart format` đã chạy.

## 09:06 — dungtv (qua Claude Code)

**Tính năng/khu vực**: Đa ngôn ngữ EN/VI/KO — triển khai toàn bộ app (không chỉ P-0x)

**Bối cảnh**: dungtv hỏi seri P-0x đã gắn BE thật chưa; sau khi xác nhận P-0x gắn BE đầy đủ (trừ "Change photo" và chuyển ngôn ngữ), dungtv yêu cầu triển khai nốt cả 2. Việc dịch vốn được `docs/CLAUDE.md` ghi "để Phase 2", nên trước khi làm đã hỏi lại dungtv có muốn làm ngay không — dungtv chọn **"Làm ngay bây giờ — toàn bộ app"** (không chỉ riêng P-0x).

**Phát hiện quan trọng**: app đã có sẵn cơ chế đa ngôn ngữ tự viết từ một phiên làm việc trước (`lib/core/app_strings.dart` + `assets/lang/{en,vi,ko}.json`, API `AppStrings.t('section.key', {...})`) — cố tình KHÔNG dùng `flutter_localizations`/ARB hay `easy_localization` để dungtv tự sửa file JSON được mà không cần biết Flutter tooling. Trước đợt này chỉ 2 màn Login/Signup dùng cơ chế này (159 keys); mọi màn khác (toàn bộ H-0x còn lại, toàn bộ P-0x, các shared widget) vẫn hardcode tiếng Anh. Đợt này MỞ RỘNG cơ chế có sẵn ra toàn app, không tự chế cơ chế mới.

**Đã làm**:
- Mở rộng `assets/lang/{en,vi,ko}.json` từ 159 → 305 keys mỗi file (thêm section `profile`, `personalProfile`, `payoutBankAccount`, `changePassword`, `managerList`, `managerForm`; bổ sung key còn thiếu cho `home`/`houseForm`/`roomList`/`roomDetail`/`roomForm`/`readingEntry`/`readingDetail`/`common`/`status`; xoá 3 key rác không còn dùng). 3 file luôn giữ đúng cùng 1 bộ key (đối chiếu bằng script Python sau mỗi lần sửa).
- `lib/core/locale_provider.dart` (mới): `resolveInitialLanguage()` (đọc `SharedPreferences`, fallback theo ngôn ngữ máy, cuối cùng fallback `en`), `LanguageNotifier`/`languageProvider` (Riverpod) — đổi ngôn ngữ ở đâu cũng cập nhật `AppStrings.current` + lưu lại `SharedPreferences` để lần mở app sau nhớ đúng ngôn ngữ đã chọn.
- `lib/core/enum_labels.dart` (mới): `roomStatusLabel()`/`utilityTypeLabel()`/`readingTypeLabel()` — tách nhãn hiển thị (dịch được) ra khỏi enum `.label` gốc trong `data/models/*.dart` (giữ nguyên tiếng Anh, dùng nội bộ/khớp DB) — đúng quy ước sẵn có của project là không trộn tầng data với tầng UI.
- Toàn bộ shared widget dùng lại nhiều màn (`ConfirmDialog`, `AppBottomNav`, `ProgressCard`, `PhotoPickerRow`, `RecurringFeesEditor`, `MeterCard`, `TopBar` action menu, `SplashScreen`, `ComingSoonScreen`) + toàn bộ 13 màn còn hardcode (H-01 Home, H-02 House form, H-04 Room list/detail, H-05 Room form, H-06 Entry/Detail, S-03 Notification center — chỉ phần khung, không dịch nội dung mock, và toàn bộ P-01→P-06) đã chuyển sang `AppStrings.t()`, mỗi widget/màn tự `ref.watch(languageProvider)` để tự vẽ lại khi đổi ngôn ngữ (bản thân `AppStrings.t()` không phải nguồn dữ liệu Riverpod nên bắt buộc phải watch tay).
- P-01: gắn thật 3 chip EN/VI/KO vào `languageProvider` (trước đó chỉ hiện đúng hình Figma, bấm VI/KO chỉ báo "sắp có") — bấm là đổi ngôn ngữ thật ngay lập tức, chip `selected` phản ánh đúng ngôn ngữ đang dùng.
- `room_form_screen.dart` (H-05) có ca đặc biệt: danh sách tiện ích cố định (`A/C`, `Water heater`, `Balcony`, `Window`) vừa là NHÃN hiển thị vừa là GIÁ TRỊ lưu/so khớp thật trong `tb_room.amenities` — xử lý bằng cách giữ nguyên danh sách tiếng Anh này làm dữ liệu lưu/so khớp, chỉ thêm hàm `_fixedAmenityLabel()` dịch riêng phần HIỂN THỊ trên chip, không đụng gì tới dữ liệu đã lưu.
- Vài widget dùng tham số mặc định kiểu `this.label = 'Some text'` (bắt buộc là hằng số biên dịch, không gọi `AppStrings.t()` được) — đổi sang tham số nullable, resolve thật trong `build()` bằng `label ?? AppStrings.t(...)`: `ConfirmDialog.cancelLabel`, `AppBottomNav._tabs` (từ `static const` sang biến local mỗi lần build), `ProgressCard.label`.

**Verify trên thiết bị thật**: `flutter analyze` sạch trên toàn bộ `src`, `dart format lib` đã chạy, build APK release thành công, cài lên thiết bị thật (`R7AY3074GAD`). Tự tay đổi ngôn ngữ qua P-01 (EN→VI→KO→EN) và chụp màn hình xác nhận dịch đúng, không vỡ layout, không văn bản tràn/cắt chữ, trên toàn bộ các màn: H-01 Home (kể cả bottom nav 4 tab tự vẽ lại chữ theo ngôn ngữ), H-04 House detail (cả 2 tab "House detail"/"Rooms", filter chip theo trạng thái phòng), H-04 Room detail, H-06 Entry (kỳ ghi, tiến độ, 2 MeterCard điện/nước, banner), H-06 Detail (đủ DetailRow + lịch sử), TopBar action menu (Edit/Record readings/Delete), P-01 (chip ngôn ngữ đổi `selected` đúng theo lựa chọn thật, không còn báo "sắp có" khi bấm VI/KO), P-02, P-03 (kể cả dropdown "Bank name" đã sửa hình đợt trước vẫn đúng), P-04 (kể cả field "Confirm new password" mới bổ sung), P-05 (rỗng lẫn có dữ liệu), P-06 (form Create, 2 ô "Manager"/"Account active" vẫn cùng chiều cao đúng như đã sửa), S-03 Notification center (khung dịch, nội dung mock giữ nguyên tiếng Anh đúng như phạm vi đã chốt).

**Còn nợ lại lúc viết entry trên (đã làm xong ngay sau, xem entry kế tiếp)**: "Change photo" (P-02) — cần thêm cột ảnh đại diện vào `tb_user` + Storage bucket + UI upload thật, hiện vẫn chỉ báo "sắp có" khi bấm.

## 09:15 — dungtv (qua Claude Code)

**Tính năng/khu vực**: "Change photo" (P-02) — upload ảnh đại diện thật, việc còn lại từ yêu cầu "triển khai nốt đi" cùng đợt với đa ngôn ngữ

**Đã làm**:
- Migration `20260911100000_tb_user_avatar.sql`: thêm cột `tb_user.avatar_path`; bucket Storage riêng `avatars` (private, khác `property-photos` dùng chung House/Room) — path quy ước `<userId>/<file>`, RLS chỉ cho phép chính chủ tài khoản (`auth.uid()`) đọc/ghi/xoá đúng thư mục của mình, không liên quan gì tới `has_house_access` (avatar là dữ liệu cá nhân, không phải dữ liệu theo Nhà). Đã `supabase db push --linked` thành công lên project thật.
- `UserProfile` thêm field `avatarPath`; `UserRepository` thêm `uploadAvatar()` (upload xong tự cập nhật luôn `tb_user.avatar_path`, trả về path mới để màn gọi tự xoá ảnh cũ SAU KHI DB đã trỏ sang ảnh mới — tránh cửa sổ hở nếu lỗi giữa chừng), `deleteAvatar()`, `signedAvatarUrl()` (URL tạm 1h, cùng quy ước với `HouseRepository.signedPhotoUrl`).
- `Avatar` (`shared/avatar.dart`) thêm tham số `imageUrl` — có ảnh thật thì hiện ảnh (bo tròn theo đúng khung tròn có sẵn), lỗi tải/chưa có thì tự rơi về chữ cái đầu như cũ (`errorBuilder`), không đổi hành vi mặc định của những nơi đang dùng `Avatar` mà chưa truyền `imageUrl`.
- P-02 (`personal_profile_screen.dart`): "Change photo" giờ bấm là đổi thật — hiện bottom sheet "Take photo"/"Choose from gallery" (giống hệt cơ chế `PhotoPickerRow` đã có ở H-02/H-05), upload ngay lúc chọn xong (KHÔNG gộp vào nút "Save" chung của form — Full name/ID number vẫn cần Save riêng, đổi ảnh là hành động tức thời quen thuộc của hầu hết app). P-01 cũng hiện đúng ảnh vừa đổi (đọc chung `avatarUrlProvider` mới, family theo path nên tự có key cache mới mỗi lần đổi ảnh, không cần tự tay invalidate).

**Verify trên thiết bị thật**: `flutter analyze` sạch, `dart format` đã chạy, build APK release cài lên `R7AY3074GAD`. Tự tay đổi ảnh đại diện qua P-02 (chọn từ thư viện) 2 lần liên tiếp, xác nhận ảnh hiện đúng tại P-02 và đồng bộ sang P-01 (đọc chung `avatarUrlProvider`, không cần refresh tay). Đối chiếu trực tiếp qua Supabase REST API: `tb_user.avatar_path` luôn trỏ đúng file mới nhất, và list bucket `avatars` theo prefix `<userId>/` chỉ còn đúng 1 file sau lần đổi thứ 2 — xác nhận `deleteAvatar()` xoá đúng ảnh cũ, không tích rác trong Storage.

## 11:29-11:42 — dungtv (qua Claude Code)

**Tính năng/khu vực**: dungtv báo tạo Manager, gán vào Nhà, nhưng H-03 (House info) vẫn hiện tên mình (Owner) làm Manager — kiểm tra lại rule "set manager"

**Điều tra**: đối chiếu trực tiếp `tb_user_house_access` qua Supabase REST API (service role) — phát hiện dòng Manager "Nguyen van B" dungtv vừa tạo có `is_active = false`. Field "Manager" ở H-03 chỉ hiện Manager đang **active**, fallback về Owner khi chưa có Manager active nào (đúng rule đã chốt Đợt 20, 2026-09-10) — nên đây không phải bug ở phần fallback, mà do "Account active" đang tắt lúc tạo. dungtv báo "vừa active" nhưng đối chiếu DB vẫn thấy `is_active = false` — nghi có bug thật, nên tự tay lên thiết bị dungtv (qua adb) tái hiện lại: mở P-06 Edit Manager "Nguyen van B" → bật "Account active" → Save → xác nhận qua REST API `is_active` đã đổi thành `true` → quay lại H-03 xác nhận field "Manager" đã đổi đúng sang "Nguyen van B". **Kết luận: không phải bug, chỉ là lần dungtv báo "vừa active" trước đó thao tác chưa tới (bấm Save) hoặc chưa bấm đúng toggle** — dungtv tự xác nhận lại trên máy mình sau khi tôi demo cũng thấy đúng.

## 11:33-11:42 — dungtv (qua Claude Code)

**Tính năng/khu vực**: Test lại toàn bộ luồng điền đầy đủ form seri P-0x (P-01→P-06) lần cuối theo yêu cầu dungtv — phát hiện + sửa 1 bug thật

**Đã test đầy đủ, điền hết field, bấm Save thật** (không phải chỉ xem qua):
- **P-01**: hub điều hướng, avatar hiện đúng ảnh thật dungtv đã tự upload trước đó (không phải ảnh test của tôi — không đụng vào), badge "Main Manager", đếm "1" Manager account, 3 chip ngôn ngữ.
- **P-02**: điền lại đúng dữ liệu hiện có (không ghi đè thông tin thật bằng dữ liệu giả), bấm Save thành công.
- **P-03**: chọn ngân hàng "Vietcombank" qua dropdown (trước đó field trống dù đã có sẵn Account name/number), bấm Save thành công.
- **P-04**: gõ mật khẩu mới đủ 3 điều kiện (checklist tự tick sống theo từng ký tự), test lỗi "Passwords do not match" (2 mật khẩu xác nhận khác nhau) và lỗi "Current password is incorrect" (cố ý gõ sai mật khẩu hiện tại) — **cố tình KHÔNG test luồng đổi thành công thật** để tránh khoá nhầm mật khẩu đăng nhập thật của dungtv (giữ nguyên thận trọng đã có từ Đợt 22).
- **P-05**: xem danh sách Manager.
- **P-06 Create**: điền đầy đủ Full name/Phone/ID number/Note cho 1 Manager mới — xác nhận đúng thiết kế: checkbox "House access" của nhà duy nhất bị **disable + hiện "Managed by Nguyen van B"** (đúng rule 1-Manager-active/nhà, Đợt 20 luật #5) vì nhà đó đã có Manager active. Bấm Save với 0 nhà được chọn → **quay lại danh sách như thành công (không báo lỗi) nhưng KHÔNG có gì được lưu thật** (đối chiếu DB xác nhận không có dòng nào) — đây là **UX gap cần dungtv quyết định**: có nên chặn Save + báo lỗi rõ ràng khi Manager mới tạo chưa được gán vào Nhà nào không, thay vì im lặng không lưu gì như hiện tại? Chưa sửa, chờ dungtv xác nhận hướng.
- **P-06 Edit**: mở lại Manager có sẵn, dữ liệu hiện đúng, bấm "Remove manager account" xác nhận dialog hiện đúng (Cancel để không xoá thật, giữ nguyên dữ liệu dungtv đang dùng).

**Bug thật tìm thấy + đã sửa**: field "Payout bank account" ở H-03 (`room_list_screen.dart`) chưa bao giờ ghép tên ngân hàng vào, chỉ hiện `Account name · Account number` — dù P-03 đã lưu/đọc đúng `bankBin` từ Đợt 22. Lỗi có từ commit gốc lúc build P-0x (không phải regression do đợt sửa đa ngôn ngữ), phát hiện được nhờ vòng test lần này chọn thật 1 ngân hàng rồi đối chiếu H-03. Sửa bằng cách ghép thêm `VnBank.byBin(house.bankBin)?.name` vào đầu chuỗi, đúng y cách `payout_bank_account_screen.dart` đã làm cho ListCard riêng của nó.

**Verify trên thiết bị thật**: build lại APK release + cài lên `R7AY3074GAD` (phát hiện quên cài bản build trước đó — bài học: luôn `adb install -r` ngay sau khi build xong, đừng test tiếp trên bản cũ). Sau khi cài đúng bản mới: "Payout bank account" hiện đúng "Vietcombank · Tran Van Dung · 012375649321". `flutter analyze`/`dart format` sạch.

## 09:52-12:09 — dungtv (qua Claude Code)

**Tính năng/khu vực**: Cho phép tạo hồ sơ Manager (P-06) mà chưa gán Nhà nào — đổi kiến trúc theo yêu cầu dungtv, xem [DECISIONS.md](DECISIONS.md) Đợt 25

**Bối cảnh**: dungtv chưa hiểu rõ báo cáo trước đó ("chưa tick nhà nào → Save không lưu gì"), hỏi lại rõ nghĩa. Sau khi giải thích bằng ví dụ cụ thể, dungtv xác nhận muốn hướng khác với đề xuất ban đầu của tôi (chỉ chặn lỗi): **muốn tạo trước 1 Manager, gán Nhà sau**. Đối chiếu lại BUSINESS-RULES.md/DATABASE.md/SCREEN-SPEC.md xác nhận đây là giới hạn kiến trúc thật (Đợt 20: Manager không có hồ sơ độc lập, chỉ tồn tại qua dòng cấp quyền theo Nhà) — hỏi lại dungtv rõ ràng giữa 2 hướng trước khi code, dungtv chọn đổi kiến trúc.

**Đã làm**: `tb_user_house_access.house_id` bỏ NOT NULL, cho phép 1 dòng "nháp" (`house_id=null`) mang hồ sơ Manager (tên/SĐT/CCCD/note/active) khi chưa gán Nhà nào — migration `20260911110000_manager_profile_without_house.sql` (thêm unique partial index `one_draft_profile_per_owner`, RLS policy riêng cho dòng nháp vì policy cũ dựa vào `has_house_access(house_id,...)` luôn `false` khi `house_id` null). `UserRepository.saveManager()`/`listManagerAccounts()`/`removeManager()` xử lý thêm dòng nháp: tự tạo/cập nhật khi 0 Nhà, tự xoá khi chuyển sang có Nhà thật. P-06 thêm hint "chưa gán Nhà, gán sau bằng Sửa" khi 0 Nhà được tick.

**Verify trên thiết bị thật** (tự tay dựng lại đúng ca dungtv mô tả, không phải chỉ đọc code): tạo Manager "Le Thi Test" không tick Nhà (nhà duy nhất đang bị Manager khác chiếm) → Save thành công thật (trước đó: mất trắng), P-05 hiện "No house granted". Tắt Manager cũ để giải phóng Nhà → sửa lại "Le Thi Test" tick Nhà đó → Save → đối chiếu DB xác nhận dòng nháp bị xoá, thay bằng dòng thật; H-03 "Manager" hiện đúng tên mới. dungtv chủ động hỏi thêm 1 ca: bật lại Manager cũ ("Nguyen van B") trong khi "Le Thi Test" đang active CÙNG Nhà đó → test trực tiếp xác nhận bị chặn đúng bởi `one_active_manager_per_house` (đã có từ Đợt 20), báo lỗi rõ ràng, không tạo ra 2 Manager active cùng lúc 1 Nhà — 2 cơ chế (nháp + 1-active-manager-per-house) hoạt động đúng, không xung đột. dungtv chọn giữ nguyên dữ liệu test hiện tại, tự dọn sau.

`flutter analyze`/`dart format` sạch. Đã cập nhật `docs/DATABASE.md`, `docs/BUSINESS-RULES.md` (BR-ROLE-04), `docs/SCREEN-SPEC.md` (P-06) theo kiến trúc mới.

## 12:11-12:26 — dungtv (qua Claude Code)

**Tính năng/khu vực**: dungtv hỏi xác nhận seri P-0x đã xong chưa (UI/Luồng/Validate) — rà soát lại có hệ thống, phát hiện + sửa 3 lỗi validate thật

**Bối cảnh**: Trước khi trả lời "đã xong", chủ động test trực tiếp thêm các trường hợp validate chưa đụng tới ở các đợt test trước (thay vì chỉ đọc lại code). Phát hiện 3 vấn đề thật ở P-03/P-06:

1. **Bug nghiêm trọng nhất — P-03 "Payout bank account" không xoá trắng được field đã có giá trị**: `House.copyWith()` dùng kiểu `param ?? this.param` — khi form gửi `null` (ý xoá trắng "Account name"/"Account number"/"Owner tax code"), `??` hiểu nhầm thành "không truyền gì, giữ nguyên", nên field KHÔNG BAO GIỜ xoá được, dù Save báo thành công. Test trực tiếp: xoá trắng "Account name"/"Account number" đã có sẵn dữ liệu thật của dungtv → Save "thành công" → đối chiếu DB thấy giá trị CŨ vẫn còn nguyên. Sửa bằng sentinel `_unset` (kiểu `Object`) thay cho mặc định `null` trên 4 tham số optional của `copyWith()`, phân biệt đúng "bỏ qua tham số" (giữ nguyên) với "truyền `null` thật" (xoá trắng).
2. **P-03 "Bank name"/"Account name"/"Account number" có dấu `*` (bắt buộc) nhưng chưa hề có validator nào** — màn này thiếu hẳn `Form`/`GlobalKey<FormState>`, `_save()` không kiểm tra gì trước khi lưu. Test trực tiếp: để trống cả 3 field vẫn Save "thành công" (trước khi sửa lỗi #1, còn khiến check này khó nhận ra vì giá trị cũ tự "phục hồi"). Đã thêm `Form` + `validator` bắt buộc cho cả 3 field, chặn Save/`Apply to all houses` đúng khi thiếu.
3. **P-06 "Phone" chỉ check không được để trống, không check định dạng** — gõ `"12"` vẫn qua được validator cũ. Thêm check độ dài số (9-11 chữ số). Đồng thời phát hiện **1 edge case đã ghi rõ trong `SCREEN-SPEC.md` từ trước ("Số điện thoại là chính mình → chặn, báo lỗi") nhưng chưa từng được code** — test trực tiếp: nhập đúng SĐT của chính dungtv làm Manager mới vẫn hợp lệ. Đã thêm check so khớp với `currentUserProfileProvider`, chặn đúng + báo lỗi rõ ràng "You can't invite yourself as a manager."

**Verify trên thiết bị thật** cho cả 3 sửa: (1) xoá trắng Account name/number ở P-03 → Save → đối chiếu DB xác nhận dữ liệu KHÔNG đổi (validator #2 chặn trước khi kịp gọi #1, nhưng đã verify riêng từng lỗi theo đúng thứ tự phát hiện); (2) để trống 3 field bắt buộc ở P-03 → hiện đúng 3 dòng "Required" màu đỏ, Save bị chặn; (3) P-06 nhập SĐT của chính dungtv → hiện đúng "You can't invite yourself as a manager."; nhập `"12"` → hiện đúng "Invalid phone number." `flutter analyze`/`dart format` sạch.

**Kết luận cho câu hỏi của dungtv**: seri P-0x (UI/Luồng/Validate) nay đã đầy đủ — đã rà soát và vá xong toàn bộ lỗ hổng validate phát hiện được qua vòng test kỹ lần này.
