## 00:15 — dungtv (qua Claude Code)

**Tính năng/khu vực**: Chọn xong nhà cung cấp SMS — eSMS.vn (thay SpeedSMS)

**Thay đổi**:
- SpeedSMS: tài khoản demo (2000đ) không gửi được SMS nào — API luôn trả lỗi "sender not found" cho mọi `type` thử (2, 4/Verify). Đăng ký Brandname cần giấy tờ thật (công văn, ĐKKD, CMND), duyệt thủ công — không dùng ngay được, và link tải công văn mẫu trên trang SpeedSMS bị lỗi 404.
- dungtv đăng ký thêm eSMS.vn, cung cấp ApiKey/SecretKey. Tra cứu tài liệu chính thức tại `developers.esms.vn` (không dùng trang marketing chung chung như lần trước với SpeedSMS) — tìm đúng Brandname demo **"Baotrixemay"** dùng test miễn phí bằng tài khoản khuyến mãi có sẵn (5.000đ), với đúng 1 template nội dung bắt buộc.
- Test trực tiếp bằng `curl` trước khi sửa code: lần đầu sai template (`CodeResult: 146 — "Sai template Brandname CSKH"`), sau khi dùng đúng template (`"{code} la ma xac minh dang ky Baotrixemay cua ban"`) thì `CodeResult: "100"` (thành công) — dungtv xác nhận nhận được SMS thật trên điện thoại.
- Viết lại `supabase/functions/send-otp-sms/index.ts`: gọi `POST rest.esms.vn/MainService.svc/json/SendMultipleMessage_V4_post_json/` với `ApiKey`/`SecretKey`/`Phone`/`Content`/`Brandname`/`SmsType`. Thêm `toLocalVnPhone()` — Supabase gửi payload SĐT dạng E.164 (`+84...`) nhưng eSMS cần dạng nội địa (`0...`).
- Lưu `ESMS_API_KEY`/`ESMS_SECRET_KEY` làm Supabase Secret (`supabase secrets set`), xoá secret `SPEEDSMS_ACCESS_TOKEN` không dùng nữa (`supabase secrets unset`). Deploy lại function — test trực tiếp qua HTTP (status 200), xác nhận dungtv nhận được SMS.
- Dọn toàn bộ tài liệu còn nhắc "SpeedSMS" như 1 lựa chọn đang cân nhắc: `docs/REQUIREMENTS.md` (INT-02: đánh dấu đã chọn eSMS), `docs/ARCHITECTURE.md`, `docs/API.md`, comment trong `send-notification/index.ts`. Thêm quyết định vào `docs/DECISIONS.md` (Đợt 14).
- **⚠️ Cảnh báo quan trọng, đã ghi rõ đầu file code**: Brandname "Baotrixemay" là demo dùng chung của eSMS, nội dung tin nhắn bắt buộc đúng template của họ, không nhắc gì tới BizTown — chỉ hợp lệ để test kỹ thuật. Trước production phải đăng ký Brandname CSKH thật qua eSMS, đợi duyệt, rồi đổi `ESMS_BRANDNAME`/`Content` trong code.

## 07:00 — dungtv (qua Claude Code)

**Tính năng/khu vực**: Vá lỗ hổng bảo mật Send SMS Hook + hoàn thiện các edge case còn thiếu của luồng Auth (rate-limit, phân biệt lỗi OTP, timeout Splash, nối Forgot password)

**Thay đổi**:
- **Bảo mật `send-otp-sms`**: implement verify chữ ký webhook bằng thư viện chính thức `standardwebhooks` (theo đúng hướng dẫn Supabase Auth Hooks) — trước đó `auth: "none"` khiến bất kỳ ai biết URL function cũng gọi được để gửi SMS tốn tiền eSMS. Gặp 1 vướng mắc: giá trị `hook_send_sms_secrets` đọc lại qua Management API GET khác với giá trị đã PATCH lúc đầu (khả năng API trả bản băm/che giấu, không phải secret gốc) — xử lý bằng cách tạo secret mới và set khớp đúng cùng 1 giá trị ở cả 2 phía (Auth Hook config + Supabase Function secret `SEND_SMS_HOOK_SECRET`), tránh phụ thuộc vào GET trả về gì.
  - Verify 2 chiều bằng test thật: gọi trực tiếp không chữ ký → 401 (chặn đúng); gọi qua `signInWithOtp` thật (số mới, không phải Test OTP) → 200 (vẫn hoạt động đúng cho request hợp lệ).
  - Gặp thêm lỗi phụ `hook_payload_invalid_content_type` (GoTrue báo thiếu Content-Type) khi test với số thật — do response thành công trả `Response(null)` không có header Content-Type; sửa thành `Response.json({})`. Đây là quirk đã biết của GoTrue (tìm thấy qua tra cứu GitHub issues), không phải lỗi code logic.
  - ⚠️ Lưu ý tự nhận: lúc test đã lỡ dùng số điện thoại tự đổi 1 chữ số cuối từ số thật của dungtv (`+84356123971`) — có thể là số thật của người khác, đã gửi 1 SMS thật tới đó. Rút kinh nghiệm không tự chế số điện thoại kề số thật để test nữa.
- **Rate-limit đăng nhập** (SCREEN-SPEC.md S-01 edge case: khoá 10 phút sau 5 lần sai): thêm `lib/data/login_rate_limiter.dart` (đếm số lần sai + thời điểm hết khoá lưu qua `shared_preferences`, theo từng SĐT đã chuẩn hoá E.164). Verify bằng test thật trên emulator: sai đúng 5 lần → hiện "Bạn đã nhập sai quá 5 lần. Vui lòng thử lại sau 10 phút.", lần thứ 6 bị chặn ngay lập tức (không gọi mạng nữa, verify qua log không thấy request tới Supabase).
- **Nối "Forgot password?"**: điều hướng sang `/signup` (tái dùng luồng OTP + Set Password có sẵn, verifyOTP tạo session hợp lệ cho SĐT đã tồn tại) — không có màn Forgot Password riêng trong Figma nên tái dùng đúng như TODO đã ghi trước đó, không tự vẽ thêm UI mới.
- **Phân biệt lỗi OTP sai/hết hạn**: bắt riêng `AuthApiException` với `e.code == 'otp_expired'` → thông báo "Mã đã hết hạn...", còn lại → "Mã OTP không đúng, thử lại". ⚠️ Test thật phát hiện: với Test OTP number, nhập sai mã (chưa hết hạn đếm ngược) vẫn khiến GoTrue trả về `otp_expired` — có thể là quirk riêng của cơ chế Test OTP, chưa xác nhận được hành vi này có khác với OTP thật (eSMS) hay không vì tốn thêm SMS thật + phải chờ hết hạn thật (60s) mới test được đúng kịch bản. Code đã bắt đúng theo tài liệu GoTrue, nhưng độ chính xác của thông báo hiển thị phụ thuộc vào GoTrue phân loại đúng hay không.
- **Timeout Splash 10s**: bọc `initSupabase()` trong `main.dart` bằng `.timeout(Duration(seconds: 10))` — nếu quá hạn (mạng chậm lúc làm mới session) vẫn vào app bình thường thay vì treo màn hình trắng. Chưa test được trực tiếp kịch bản mạng chậm thật trên emulator (khó giả lập đáng tin cậy), chỉ verify không có regression ở luồng khởi động bình thường.
- Thêm `shared_preferences: ^2.3.0` vào `pubspec.yaml` (trước đó chỉ có sẵn gián tiếp qua dependency khác).

## 10:00 — dungtv (qua Claude Code)

**Tính năng/khu vực**: Sửa splash hệ thống (native) — đổi từ nền trắng mặc định sang navy khớp thương hiệu; bắt đầu dùng thiết bị Android thật để test thay máy ảo

**Bối cảnh**: dungtv cắm điện thoại Android thật (Samsung Galaxy A06, Android 16) qua USB, yêu cầu build/deploy trực tiếp lên máy thật thay vì máy ảo từ nay. dungtv phản hồi chỉ thấy "splash mặc định của Flutter" (nền trắng + icon) — đây là **splash hệ thống (native)**, khác với `SplashScreen` (Dart) đã làm ở S-00 — native splash hiện trước khi Flutter kịp vẽ frame đầu tiên, cấu hình ở `android/app/src/main/res/drawable/launch_background.xml` vẫn để mặc định của template Flutter (`@android:color/white`), chưa từng chỉnh.

**Thay đổi**:
- Thêm `flutter_native_splash: ^2.4.1`, cấu hình `color: "#23305E"` (đúng navy của S-00) + `image: assets/icon/app_icon.png` (icon đã có sẵn), riêng cho cả bản thường và Android 12+ (`android_12`, dùng Splash Screen API mới của Android). Chạy `dart run flutter_native_splash:create` — tool tự sinh lại `launch_background.xml`, `styles.xml` (thêm cả `values-v31`/`values-night-v31` cho Android 12+ chưa có sẵn), và cấu hình iOS.
- Build + cài lên máy thật, chụp màn hình xác nhận: mở app giờ chuyển thẳng nền navy (không còn trắng), rồi qua `SplashScreen` (Dart, cùng nền navy nên liền mạch không bị "nhảy màu"), rồi Login — toàn bộ quá trình chưa tới 1 giây trên máy thật (nhanh hơn máy ảo nhiều).
- Từ nay build/test trực tiếp trên thiết bị thật (`adb -s R7AY3074GAD`) thay vì `emulator-5554`.

## 10:35 — dungtv (qua Claude Code)

**Tính năng/khu vực**: Tăng thời gian hiện Splash lên 2 giây; verify luồng Forgot Password bằng bằng chứng đổi mật khẩu thật

**Thay đổi**:
- dungtv phản hồi Splash (400ms) chớp quá nhanh trên máy thật, không kịp thấy logo. Đổi `splash_screen.dart` từ `Duration(milliseconds: 400)` → `Duration(seconds: 2)`. Đo lại bằng chuỗi ảnh chụp theo mốc thời gian (0.4s/lần) trên máy thật: Splash (logo+tagline) hiện đúng ~2.4s trước khi qua Login — khớp yêu cầu. Lưu ý: tổng thời gian từ lúc mở app tới lúc thấy Splash (qua splash hệ thống) dao động 1-4s tuỳ lần, do overhead của **build debug** (chưa tối ưu), không phải do code.
- Cập nhật lại comment TODO cũ trong `splash_screen.dart` (timeout 10s đã làm ở `main.dart` từ hôm trước, không còn là TODO).
- **Verify kỹ luồng Forgot Password bằng bằng chứng thật** (không chỉ test điều hướng): (1) xác nhận mật khẩu gốc "matkhau123" đăng nhập được (baseline), (2) qua "Forgot password?" → Verify OTP (Test OTP number) → đặt mật khẩu mới "matkhaumoi456" → Create account thành công, (3) đăng xuất, thử lại mật khẩu CŨ → **thất bại đúng như kỳ vọng** (chứng minh mật khẩu đã đổi thật trong Supabase Auth, không phải chỉ điều hướng đúng màn), (4) thử mật khẩu MỚI → **đăng nhập thành công**. Toàn bộ test trên thiết bị Android thật.

## 11:10 — dungtv (qua Claude Code)

**Tính năng/khu vực**: Bottom Navigation 4-tab thật (thay Home placeholder) + code H-01 (Home/House List) theo Figma thật

**Thay đổi**:
- Lấy design context thật của H-01 qua Figma MCP (node `220:2347`, đã đăng nhập bằng tài khoản CEO có quyền Editor) — không suy đoán từ ảnh/text spec.
- Thêm 5 shared component mới trong `lib/shared/`: `StatusPill` (badge tròn theo tone), `ListCard` (dòng list dùng chung cho Nhà/Phòng/Hợp đồng/Tenant/Notification — `thumbShape: square|round`), `StatCard` (cặp số liệu tổng quan), `SectionLabel` (nhãn overline có sẵn margin), `AppFab` (nút tròn nổi cam đúng shadow Figma). Refactor luôn `NotificationCenterScreen` (S-03) để dùng lại `ListCard`/`StatusPill` thay vì widget riêng, tránh trùng code với H-01.
- Mở rộng `TopBar` thêm constructor `TopBar.home(...)` (dòng "Hello," + tên + dòng tổng quan, thay cho back button) và `TopBarBellButton` — dùng chung 1 component `TopBar` cho cả 3 kiểu (Title/Title+Action/Home) thay vì tạo widget mới.
- Thêm `AppBottomNav` (4 tab Home/Tenant & Contract/Bills/Profile, đúng icon+label Figma node `166:103`) và `AppShell` (bọc `StatefulShellRoute.indexedStack` của go_router — mỗi tab giữ nguyên stack điều hướng riêng). Sửa `router.dart`: bỏ route `/home` phẳng trỏ `HomePlaceholderScreen` (xoá file placeholder luôn), thay bằng 1 `StatefulShellRoute.indexedStack` với 4 branch (`/home`, `/tenant`, `/bills`, `/profile`).
- Code `lib/landlord/home_screen.dart` (H-01 thật): dùng đúng `TopBar.home` + 2 `StatCard` + `SectionLabel` + 3 `ListCard` (dữ liệu mẫu y hệt nội dung trên Figma — 3 nhà, badge theo đúng tone Success/Neutral) + `AppFab`. Tenant/Bills/Profile tạm dùng chung 1 `ComingSoonScreen` (thay vì 3 file gần giống nhau) — Profile có thêm nút "Đăng xuất" tạm để còn test được, sẽ bỏ khi có P-01 thật.
- **Bug tự phát hiện lúc build/test thật trên máy thật**: build `flutter build apk --release` không truyền `--dart-define=SUPABASE_URL`/`SUPABASE_PUBLISHABLE_KEY` (đọc trong `supabase/.env`, không hardcode) khiến app luôn báo "Sai số điện thoại hoặc mật khẩu"/"Không gửi được OTP" dù tài khoản/OTP đúng — không phải lỗi code Auth, chỉ do thiếu tham số build. Build lại đúng cách thì đăng nhập/khôi phục session hoạt động bình thường.
- **Bug thật phát hiện qua ảnh chụp máy thật**: `AppBottomNav` tự vẽ bằng `Container` (không phải `BottomNavigationBar` có sẵn) nên không tự trừ hao vùng thanh điều hướng hệ thống — trên máy Android dùng 3 nút điều hướng (không phải gesture), thanh tab bị đè lên thanh 3 nút hệ thống. Sửa bằng cách bọc nội dung trong `SafeArea(top: false)`. Đã build lại + chụp ảnh xác nhận hết đè.
- Verify toàn bộ trên thiết bị Android thật (`R7AY3074GAD`, tài khoản test `0356123970`): H-01 khớp pixel với ảnh Figma (màu thumb, badge, số liệu, layout), chuyển tab Tenant/Bills/Profile đúng trạng thái chọn, nút chuông ở H-01 → mở đúng Notifications (S-03), nút "Đăng xuất" ở Profile hoạt động. `flutter analyze` sạch, `dart format` đã chạy.

## 11:20 — dungtv (qua Claude Code)

**Tính năng/khu vực**: Sửa lỗi nút back ở Notifications (S-03) không bấm được — thiếu SafeArea trên cho `TopBar`

**Thay đổi**:
- dungtv phản hồi trên máy thật: nút back ở màn Notifications không bấm được, "bị vướng gì đó". Nguyên nhân: `TopBar` (component dùng chung cho S-02/S-03/H-01 và mọi màn H-0x/T-0x/B-0x/P-0x sau này) vẽ nội dung ngay từ y=0 của `Column`, không bọc `SafeArea` — status bar hệ thống đè lên đúng vùng chứa nút back/action bên phải, khiến tap ở vùng đó bị hệ thống chặn thay vì tới được `InkWell` của app (cùng loại lỗi với `AppBottomNav` bị thanh điều hướng dưới đè lên đã sửa trước đó trong ngày, nhưng ở cạnh trên).
- Sửa 1 chỗ duy nhất trong `top_bar.dart`: bọc `Container` bằng `SafeArea(bottom: false)` — tự động áp dụng cho mọi màn dùng `TopBar`, không phải sửa từng màn riêng lẻ.
- Rà soát thêm: không có `Positioned`/absolute-width nào khác trong `lib/shared`/`lib/landlord` giả định đúng 375dp như Figma — mọi layout đều dùng `Expanded`/`Row`/`double.infinity` nên tỉ lệ đã tự thích ứng đúng theo chiều rộng thật của thiết bị, không cần sửa thêm.
- Verify trên thiết bị thật: "Hello," ở H-01 không còn bị status bar đè lên nữa, nút back ở Notifications bấm được và quay đúng về H-01. `flutter analyze` sạch, `dart format` đã chạy.

## 11:40 — dungtv (qua Claude Code)

**Tính năng/khu vực**: Code UI seri màn H-02 → H-06 (House/Room/Reading) theo Figma thật

**Thay đổi**:
- Lấy design context thật qua Figma MCP cho cả 6 node: H-02 House Registration (`220:2614`), H-03 Rooms (`220:2424`) + House detail (`312:2069`), H-04 Room Detail (`220:2714`), H-05 Room Create/Edit (`220:2804`), H-06 Entry (`220:3077`) + Detail/Edit (`220:2905`).
- Thêm 9 shared component mới trong `lib/shared/` (đều map 1-1 với component đã định nghĩa sẵn trong Figma Design System, đọc từ usage description của từng component): `AppButton` (Primary/Ghost/Accent/Danger, Md/Sm), `DetailRow` + `DetailBlock` (dòng key-value + khung thẻ chứa), `AppTextField` (Text/Textarea/Select/Date, có state readonly), `AppBanner` (info alert), `PhotoPickerRow` (ảnh có sẵn + ô "+" thêm ảnh, chưa nối Supabase Storage), `AppChip` (dùng chung cho filter H-03 và multi-select amenities H-05), `MiniProfileCard` (tóm tắt Tenant, dùng lại được ở T-05 sau này), `ProgressCard` và `MeterCard` (riêng cho H-06).
- Code 5 màn: `house_form_screen.dart` (H-02, 1 file dùng chung Create/Edit qua `houseId == null`), `room_list_screen.dart` (H-03, 1 file 2 state qua Segmented control thay vì 2 file), `room_detail_screen.dart` (H-04), `room_form_screen.dart` (H-05, dùng chung Create/Edit), `reading_entry_screen.dart` + `reading_detail_screen.dart` (H-06, đúng 2 frame Figma).
- Nối route lồng trong branch `/home` của `StatefulShellRoute` (giữ được Bottom Navigation xuyên suốt): `houses/new`, `houses/:houseId` (H-03), `houses/:houseId/edit`, `houses/:houseId/rooms/new`, `houses/:houseId/rooms/:roomId` (H-04), `.../edit`, `houses/:houseId/readings` (H-06 entry), `.../readings/:readingId` (H-06 detail). Nối FAB + list card ở H-01 sang đúng luồng.
- **Chỉ nối điều hướng cho nhà "Nha tro Binh An"** (2 nhà còn lại ở H-01 chưa nối `onTap`) — đây là nhà DUY NHẤT Figma có đủ data mẫu chi tiết cho cả 5 màn; nối luôn 2 nhà kia sẽ hiện nhầm data của Binh An, đúng nguyên tắc không tự bịa nội dung. Tương tự, "Current contract" ở H-04 chưa nối (chờ seri T-0x), meter card ở H-06 Entry không có tap-to-detail (đã có ô nhập số ngay trong card, tránh chồng chéo vùng bấm).
- **Rà soát trước khi làm** (đúng yêu cầu dungtv, chú ý tỉ lệ trước khi làm màn mới): áp dụng ngay bài học SafeArea từ lỗi vừa sửa — mọi `TopBar` ở 5 màn mới đều tự động có `SafeArea` (đã sửa ở component gốc), không lặp lại lỗi cũ.
- Thêm màu `AppColors.bgMuted` (#F3F4F8, ảnh placeholder + field readonly) vào `theme.dart` — token còn thiếu, phát hiện khi code H-02.
- Verify toàn bộ 5 màn bằng build thật + chụp ảnh trên thiết bị Android thật (`R7AY3074GAD`): H-03 (2 tab), H-04, H-05, H-02 khớp pixel với Figma. H-06 Entry verify qua 1 nút tạm nối vào TopBarMoreButton của H-03 (chỉ để chụp ảnh, đã revert ngay sau khi xác nhận đúng — H-06 chưa có điểm vào thật trong Figma, sẽ nối khi rõ luồng, VD qua Notification Center). `flutter analyze` sạch, `dart format` đã chạy.
- **Việc còn tồn đọng**: chưa nối "Ghi chỉ số kỳ này" (H-06) vào đâu cả vì Figma thật không có nút này ở H-03 (khác mô tả cũ trong SCREEN-SPEC.md) — cần hỏi lại Dream điểm vào thật của H-06 là gì; "+ Add" (amenity mới)/"+ Add fee" ở H-05 chưa mở form thêm; Photo picker chưa nối chọn ảnh thật (chờ Supabase Storage).

## 11:55 — dungtv (qua Claude Code)

**Tính năng/khu vực**: Chuẩn bị đa ngôn ngữ (VI/EN/KO) — tạo 3 file JSON bản nháp, CHƯA nối vào app

**Bối cảnh**: dungtv nhận thấy 1 số màn còn thiếu chỗ chọn Edit/Delete (⋮) nhưng đợi design xong đã tính sau. Yêu cầu chuẩn bị luồng đa ngôn ngữ VI/EN/KO bằng file JSON để tự sửa được nếu Claude dịch sai ngữ pháp/text. Đã hỏi lại 2 việc trước khi làm: (1) cách làm — chọn **tự viết JSON loader nhẹ, không thêm package** (thay vì `easy_localization` hay `flutter gen-l10n`/ARB); (2) phạm vi — chọn **chỉ tạo 3 file JSON bản nháp trước**, chưa nối app đọc từ đây, các màn hiện tại vẫn hiển thị y như cũ.

**Đã làm**:
- Rà toàn bộ text UI thật trong code (S-00→S-02, S-03, H-01→H-06, Bottom Navigation, ComingSoon) bằng cách đọc/grep từng file — không tự bịa thêm key nào ngoài text đang thực sự tồn tại trong code.
- Tạo 3 file: `src/assets/lang/en.json` (nguồn gốc, đúng theo text hiện có trên Figma/code), `vi.json`, `ko.json` — 159 key giống hệt nhau ở cả 3 file (đã verify bằng script Python so khớp key, không thiếu/thừa key nào), gộp theo từng nhóm màn (`login`, `signup`, `home`, `houseForm`, `roomList`, `roomDetail`, `roomForm`, `readingEntry`, `readingDetail`, `comingSoon`...) + `common`/`bottomNav`/`status` cho phần dùng chung nhiều nơi. Chỗ nào cần chèn số/tên linh hoạt sau này thì để `{placeholder}` (VD `{minutes}`, `{phone}`, `{count}`).
- **Phát hiện phụ khi rà text**: 1 số thông báo lỗi trong code hiện tại (`login_screen.dart`, `signup_screen.dart`) đang bị lẫn tiếng Việt vào giữa 1 app tiếng Anh là chính — VD "Sai số điện thoại hoặc mật khẩu", "Nhập số điện thoại", "Mã OTP không đúng, thử lại"... Bản `en.json` đã viết lại đúng tiếng Anh cho các câu này; bản `vi.json` giữ nội dung tiếng Việt tương đương (được viết lại cho nhất quán, không chỉ copy y nguyên bản lẫn lộn cũ); `ko.json` dịch mới sang tiếng Hàn. **Chưa sửa code** (đúng phạm vi đã chốt) — khi nối vào app ở đợt sau, các câu lỗi này trong `login_screen.dart`/`signup_screen.dart` sẽ tự động thành đúng ngôn ngữ luôn, không cần sửa 2 lần.
- Chỉ đưa vào JSON phần **text giao diện (chrome)** — tiêu đề, nhãn field, nút, thông báo lỗi, banner. Dữ liệu mẫu (tên nhà, tên người thuê, địa chỉ...) KHÔNG đưa vào vì đó là dữ liệu nghiệp vụ sẽ tới từ Supabase sau này, không phải text cần dịch.
- **Chưa làm** (đúng phạm vi đã chốt): chưa sửa `pubspec.yaml` để khai báo `assets/lang/`, chưa viết loader/provider đọc JSON, chưa sửa bất kỳ file `.dart` nào để dùng key thay vì text cứng, chưa thêm dòng chọn ngôn ngữ ở P-01. Việc này để đợt sau khi dungtv duyệt xong nội dung 3 file JSON.
- ⚠️ **Lưu ý cho dungtv**: bản dịch tiếng Hàn (`ko.json`) do Claude tự dịch, độ chính xác ngữ pháp/văn phong chưa được người bản xứ kiểm tra — nên nhờ người biết tiếng Hàn rà lại trước khi dùng thật, đúng như mục đích tạo file JSON để tự sửa được.

## 12:35 — dungtv (qua Claude Code)

**Tính năng/khu vực**: Nối `AppStrings` (JSON loader) vào S-01/S-02 — sửa bug lẫn tiếng Việt vào app tiếng Anh, mặc định tiếng Anh tạm thời

**Bối cảnh**: dungtv yêu cầu sửa ngay bug lẫn ngôn ngữ đã phát hiện ở đợt trước (11:55), mặc định ngôn ngữ tạm thời là tiếng Anh, sau này mới lấy theo cài đặt ngôn ngữ máy.

**Đã làm**:
- Viết `lib/core/app_strings.dart` — class `AppStrings` tự đọc `assets/lang/{en,vi,ko}.json` lúc khởi động (`AppStrings.init()`, gọi trong `main.dart` trước `runApp`), `AppStrings.t('section.key', {params})` để lấy text + thay `{placeholder}`. `AppStrings.current` mặc định `AppLanguage.en` — để hẳn 1 field tĩnh đơn giản (không bọc Riverpod) vì **chưa có UI nào đổi ngôn ngữ** (P-01 chưa code) nên chưa cần state phản ứng (reactive) vội, tránh làm dư thừa trước khi cần thật. Có để sẵn TODO chỉ rõ chỗ sẽ đọc `WidgetsBinding.instance.platformDispatcher.locale` khi làm màn chọn ngôn ngữ.
- Khai báo `assets/lang/` trong `pubspec.yaml`.
- Sửa toàn bộ `login_screen.dart` và `signup_screen.dart` — thay MỌI text hiển thị (không chỉ 3-4 câu lỗi bị lẫn tiếng Việt) sang gọi qua `AppStrings.t(...)` đúng key đã chuẩn bị sẵn trong 3 file JSON đợt trước, gồm cả text tĩnh (tiêu đề, field label, nút) lẫn text có tham số (`{minutes}`, `{phone}`, `{countdown}`). Bug lẫn ngôn ngữ tự hết vì bản `en.json` đã viết đúng tiếng Anh cho các câu này từ đợt trước.
- Verify trên thiết bị Android thật (`R7AY3074GAD`): validator rỗng hiện đúng "Please enter your phone number."/"Please enter your password." (trước đó là tiếng Việt), đăng nhập sai hiện "Incorrect phone number or password." (trước đó "Sai số điện thoại hoặc mật khẩu"), khoá sau 5 lần sai hiện đúng "You've entered the wrong password too many times. Please try again in 10 minute(s)." (verify cả nội suy `{minutes}` hoạt động đúng). `flutter analyze` sạch, `dart format` đã chạy.
- **Chưa làm** (ngoài phạm vi lần này): các màn H-0x/S-00/S-03/ComingSoon vẫn giữ nguyên text cứng tiếng Anh (không có bug lẫn ngôn ngữ nên chưa cấp thiết phải nối) — sẽ nối khi làm màn chọn ngôn ngữ P-01 thật; chưa đọc cài đặt ngôn ngữ máy (đang cố định "en" như dungtv yêu cầu tạm thời).

## 13:00 — dungtv (qua Claude Code)

**Tính năng/khu vực**: Nối nút "⋮" (Edit/Delete) ở H-03/H-04 theo Figma vừa cập nhật, kèm dialog xác nhận trước khi xoá

**Bối cảnh**: dungtv báo Dream vừa thêm phần Edit/Delete trên Figma, yêu cầu làm tiếp và chú ý việc xác nhận (confirm) trước khi xoá. Rà lại Figma qua Figma MCP phát hiện: (1) component **Menu row** (171:55, dùng cho P-01, chưa liên quan H-03/H-04) — không phải phần đang hỏi; (2) component **Dialog** (173:109) mới — "Confirm dialog... swap nút phải sang Danger cho xác nhận phá huỷ"; (3) **2 frame trạng thái mới** Dream thêm: "H-02 — House delete" (400:2720) và "H-03 — Room delete" (400:2731), mỗi frame là dialog xác nhận đè lên scrim navy 45% với nội dung xoá Nhà/Phòng cụ thể.

**Đã làm**:
- Thêm `lib/shared/confirm_dialog.dart` — `ConfirmDialog.show(...)` dùng chung cho mọi xác nhận phá huỷ sau này (đúng component Dialog 173:109: tiêu đề 16 bold, mô tả 13 thường, 2 nút Cancel (Ghost) + nút phải đổi Danger được, scrim navy 45%).
- Mở rộng `top_bar.dart`: thêm `TopBarActionMenuButton` (thay `TopBarMoreButton` ở màn có Edit/Delete) — bọc `PopupMenuButton` quanh đúng visual nút tròn "⋮" có sẵn thay vì tự vẽ menu riêng, 2 mục "Edit" (đen) / "Delete" (đỏ) đúng như mô tả component "Top bar — Title+Action". Tách `_TopBarCircleIcon` (phần visual) ra khỏi `_TopBarCircleButton` (bọc `InkWell`) để dùng lại được cho cả 2 nơi.
- Nối `room_list_screen.dart` (H-03): "⋮" → Edit đi `/home/houses/:id/edit` (H-02), Delete → `ConfirmDialog.show(title: 'Delete house?', ...)` đúng nguyên văn Figma, xác nhận xong mới `context.go('/home')` (chưa nối xoá `tb_house` thật, có TODO rõ ràng buộc nghiệp vụ cần thêm sau — chặn nếu còn phòng/hợp đồng).
- Nối `room_detail_screen.dart` (H-04): "⋮" → Edit đi `/home/houses/:id/rooms/:id/edit` (H-05), Delete → `ConfirmDialog.show(title: 'Delete room?', ...)` đúng nguyên văn Figma, xác nhận xong mới `context.pop()` về H-03 (tương tự, TODO nối xoá `tb_room` thật + chặn khi phòng đang Occupied).
- **Không** nối menu Edit/Delete cho H-06 (Reading) — Figma chưa có frame delete/nội dung mới cho reading (chỉ có "Edit reading" nút riêng đã có sẵn từ trước), tránh tự bịa hành vi chưa được thiết kế.
- **Sự cố lúc test (không phải bug thật)**: gặp "Incorrect phone number or password" liên tục dù đã đổi mật khẩu qua Supabase Admin API và xác nhận đúng bằng `curl` trực tiếp tới `/auth/v1/token` (login thành công 200 ở tầng REST) — hoá ra do thao tác test tự động (tap) bị bàn phím Samsung mở "Bộ nhớ tạm" đè lên nút Log in / gõ thiếu ký tự, không phải lỗi code. Xác nhận lại bằng debug build có tạm thêm `debugPrint` (đã gỡ, verify bằng `grep DEBUG TEMP` rỗng trước khi build lại bản release cuối) — login thành công ngay khi thao tác test cẩn thận hơn.
- Verify toàn bộ trên thiết bị Android thật: menu "⋮" ở H-03 hiện đúng Edit/Delete; Delete → đúng dialog "Delete house?" pixel-khớp Figma (kể cả scrim); Cancel đóng dialog không làm gì; Edit → đúng H-02 nạp sẵn dữ liệu. Lặp lại tương tự ở H-04 — dialog "Delete room?" khớp Figma, Delete → quay về H-03 đúng. `flutter analyze` sạch, `dart format` đã chạy.

## 14:30 — dungtv (qua Claude Code)

**Tính năng/khu vực**: Nối CRUD thật House/Room (H-01→H-05) + Supabase Storage cho ảnh Nhà/Phòng, vá 2 lỗi RLS nghiêm trọng + 1 lỗi thiếu quyền INTERNET ở bản release Android

**Bối cảnh**: dungtv yêu cầu "tiến hành làm crud và store ảnh đi" — thay toàn bộ dữ liệu mẫu tĩnh ở H-01→H-05 bằng CRUD thật qua Supabase, và dựng Storage cho ảnh (đã được cho phép tự làm từ trước, "hướng dẫn tôi cách setup hoặc bạn tự làm nếu có thể").

**Đã làm**:
- Migration `20260910130000_house_room_photos_storage.sql`: bucket riêng tư `property-photos` (8MB, `image/jpeg|png|webp|heic`) + 3 policy RLS trên `storage.objects` dùng `(storage.foldername(name))[1]::uuid` (segment đầu = houseId) tái dùng `has_house_access()` — 1 bộ policy cho cả ảnh Nhà (`<houseId>/houses/<file>`) và ảnh Phòng (`<houseId>/rooms/<roomId>/<file>`).
- 4 migration vá lỗ hổng Figma-vs-schema: `tb_house.owner_phone`, `tb_house.owner_email`, `tb_house.default_electricity_price`/`default_water_price`, `tb_room.note` — các field này có trên Figma H-02/H-04/H-05 nhưng schema chưa từng có cột tương ứng.
- Thêm model `models/house.dart`, `models/room.dart`, `models/recurring_fee.dart`; `HouseRepository`/`RoomRepository` (`lib/data/`) — CRUD đầy đủ + `uploadPhoto`/`deletePhoto`/`signedPhotoUrl` (bucket riêng tư nên phải dùng signed URL, không dùng được `getPublicUrl`).
- Thêm `PhotoPickerController`/`RecurringFeesController` (state cho form ảnh + phí định kỳ, dùng chung giữa House form và Room form đúng yêu cầu tránh lặp component); viết lại `lib/shared/photo_picker_row.dart` từ placeholder tĩnh thành thực sự chọn ảnh qua `image_picker` (bottom sheet Take photo/Choose from gallery); thêm `lib/shared/recurring_fees_editor.dart` (dùng chung H-02/H-05).
- Thêm provider Riverpod trong `core/providers.dart`: `housesProvider`, `houseProvider(id)`, `roomsProvider(houseId)`, `roomProvider(id)`, `roomStatusesByHouseProvider` — chuyển `home_screen.dart`, `room_list_screen.dart`, `room_detail_screen.dart`, `house_form_screen.dart`, `room_form_screen.dart` từ `StatelessWidget`/dữ liệu mẫu sang `Consumer(Stateful)Widget` đọc/ghi dữ liệu thật, tự `ref.invalidate(...)` sau mọi create/update/delete để các màn phụ thuộc tự cập nhật.
- Nối xoá Nhà/Phòng thật ở `room_list_screen.dart`/`room_detail_screen.dart` (thay TODO tạm ở đợt 13:00) — bắt riêng `PostgrestException` code `23503` (foreign_key_violation) để hiện thông báo "Can't delete: this house/room still has rooms/contract history" thay vì lỗi chung chung.
- Thêm package `image_picker`, `uuid` vào `pubspec.yaml`; thêm `NSCameraUsageDescription`/`NSPhotoLibraryUsageDescription` vào `ios/Runner/Info.plist`; thêm quyền `CAMERA` vào Android manifest.

**3 lỗi nghiêm trọng phát hiện qua test thật trên thiết bị (`R7AY3074GAD`), không lộ ra khi test với dữ liệu mẫu**:
1. **RLS đệ quy vô hạn** (`54001 stack depth limit exceeded`): `has_house_access()` không phải `security definer` nên khi đọc `tb_user_house_access`, chính policy SELECT của bảng đó lại gọi lại `has_house_access()` — lặp vô hạn. Chỉ lộ ra khi bảng `tb_user_house_access` thật sự có dữ liệu (tạo Nhà đầu tiên), mọi test trước đó dùng dữ liệu mẫu tĩnh nên bảng luôn rỗng, policy chưa từng phải evaluate thật. Vá bằng migration `20260910140000_fix_has_house_access_recursion.sql` (đổi hàm sang `security definer set search_path = public`).
2. **`INSERT ... RETURNING` bị RLS chặn** (`42501`) dù INSERT tự nó hợp lệ: Postgres áp lại policy SELECT ngay lúc trả hàng vừa insert, nhưng quyền `owner` do trigger `AFTER INSERT` cấp chưa kịp "nhìn thấy" đúng lúc đó. Vá trong `HouseRepository.create()`: sinh `id` sẵn ở client bằng `uuid`, insert không yêu cầu trả hàng, gọi `getById` ở 1 request riêng sau đó.
3. **Thiếu quyền `INTERNET` ở bản release Android**: Flutter template mặc định chỉ đặt quyền này ở `android/app/src/debug` và `.../profile`, không có ở `.../main` (áp dụng cho release) — thiếu từ đầu project, chỉ lộ khi build release thật (không phải `flutter run` debug), gây `SocketException: Failed host lookup` ở mọi request. Phát hiện qua `adb shell dumpsys package ... | grep -A5 "requested permissions"`. Đã thêm thẳng vào `src/main/AndroidManifest.xml` kèm comment giải thích tại chỗ.

Chi tiết đầy đủ 3 lỗi trên (nguyên nhân gốc + cách verify) đã ghi tại `docs/DECISIONS.md` mục "2026-09-10 (Đợt 16)".

**Verify trên thiết bị thật, qua UI thật (không phải curl)**: build lại release APK sau khi vá đủ 3 lỗi, cài lại, chạy toàn bộ luồng: tạo Nhà "Nha Tro Test" kèm ảnh chọn từ thư viện máy → hiện đúng ở H-01 (số liệu tổng hợp thật)/H-03; tạo Phòng "P.101" kèm chọn tiện ích → hiện đúng ở H-03/H-04; xoá Phòng qua "⋮" → Delete → xác nhận dialog đúng Figma → quay về H-03, danh sách rỗng đúng, số liệu H-01 cập nhật theo; xoá Nhà tương tự → quay về H-01, về đúng trạng thái rỗng ban đầu. `flutter analyze` sạch (`No issues found!`), `dart format` không có gì cần sửa (49 file, 0 thay đổi).

**Chưa làm/còn nợ**: chưa test được luồng xoá bị DB chặn bởi FK (còn phòng có lịch sử hợp đồng) vì chưa có dữ liệu hợp đồng thật — code đã bắt lỗi `23503` nhưng chưa verify bằng tình huống thật; H-06 (ghi chỉ số điện nước) và các tab Tenant/Bills/Profile vẫn là `ComingSoonScreen`, chưa có CRUD; chưa cập nhật `docs/CURRENT_STATUS.md` cho đợt này (làm ngay sau đây); chưa commit.

## 15:30 — dungtv (qua Claude Code)

**Tính năng/khu vực**: Sửa menu "⋮" Edit/Delete cho đúng Figma (thiếu gạch phân cách, sai màu Delete)

**Bối cảnh**: dungtv phản hồi bản build chưa giống design ở đúng chỗ menu Edit/Delete vừa làm đợt trước (13:00) — thiếu gạch phân cách giữa 2 dòng.

**Đã làm**:
- Lấy design context thật qua Figma MCP cho đúng frame "action pop-up" (node `400:2669`/`400:2759`, xuất hiện kèm 2 frame trạng thái "H-02 — House delete"/"H-03 — Room delete") — phát hiện bản trước code SAI theo giả định, không phải theo Figma thật: Figma dùng **cùng 1 màu navy đậm cho cả "Edit" và "Delete"** (không tô đỏ cho Delete), có 1 gạch chia mảnh (`border-bottom` màu hairline) dưới dòng "Edit", khung bo góc 9px (token `radius-xs`), viền hairline, padding 14px ngang/5px dọc quanh cả khối.
- Viết lại `TopBarActionMenuButton` trong `lib/shared/top_bar.dart`: dùng `menuPadding` (tham số có sẵn từ Flutter 3.x) để khớp đúng padding khối, thêm widget `_ActionPopupRow` (dòng chữ 14 bold navy + gạch chia mảnh tuỳ chọn) thay cho `PopupMenuItem` mặc định (vốn có padding/màu cứng của Material, không khớp Figma). Bỏ màu đỏ `AppColors.error` khỏi "Delete".
- Thêm token `AppRadii.xs = 9.0` vào `core/theme.dart` (đúng tên biến "radius-xs" trên Figma).
- Verify trên thiết bị thật (nhà thật của dungtv, "Nha tro của tao"): menu "⋮" giờ hiện đúng gạch phân cách + cả 2 dòng cùng màu navy, khớp ảnh Figma. `flutter analyze` sạch.

## 15:40 — dungtv (qua Claude Code)

**Tính năng/khu vực**: Nối CRUD thật cho H-06 (Record Monthly Reading) — hoàn tất seri màn H-0x

**Bối cảnh**: dungtv yêu cầu "làm nốt seri màn H-0x đi tenant bill profile để sau" — hoàn tất phần CRUD còn thiếu của seri Home tab (H-06), để Tenant/Bills/Profile lại cho đợt sau.

**Đã làm**:
- Thêm `lib/data/models/reading.dart` (`enum UtilityType` — điện/nước, chọn đúng bảng `tb_electricity_reading`/`tb_water_reading`; `enum ReadingType` — Periodic/Move-in/Move-out; class `Reading`) và `lib/data/reading_repository.dart` (`ReadingRepository`, dùng chung 1 class cho cả 2 bảng cấu trúc giống hệt nhau qua tham số `UtilityType`) — CRUD đúng theo BR-READ-01..07/BR-METER-13/14 (docs/BUSINESS-RULES.md mục 6): `latestForRoom`, `periodicForPeriod`, `historyForRoom`, `createPeriodic` (tự tìm `previousReadingId` đúng theo chuỗi, validate current ≥ previous trước khi gọi DB — ném `ReadingOrderException` để UI chặn đúng 1 dòng, không chặn lưu cả các phòng khác, đúng edge case SCREEN-SPEC.md), `updateCurrentReading`, `isLocked` (derived theo BR-READ-04: đọc `invoice_id` + quét `utility_lines` của các hoá đơn non-Draft khác — luôn trả `false` trong thực tế vì Bills chưa có tính năng tạo hoá đơn, nhưng cài đúng logic để tự đúng khi Bills xây xong), `uploadPhoto`/`signedPhotoUrl` (path `<houseId>/readings/<roomId>/<electricity|water>/<file>`, dùng chung bucket `property-photos` sẵn có).
- Đổi route `readings/:readingId` → `readings/:roomId/:utility` (`core/router.dart`) — 1 phòng có 2 chuỗi chỉ số độc lập (điện/nước), không thể định danh bằng 1 `readingId` đơn lẻ như thiết kế route cũ.
- Thêm provider trong `core/providers.dart`: `houseMeterEntriesProvider((houseId, periodYm))` (dữ liệu cho H-06 Entry — mọi phòng × 2 tiện ích, mỗi entry biết `previous`/`thisPeriod`) và `readingHistoryProvider((roomId, utilityType))` (lịch sử đầy đủ cho H-06 Detail).
- Viết lại hoàn toàn `reading_entry_screen.dart` (H-06 Entry, `ConsumerStatefulWidget`): period stepper (mũi tên trái/phải, mặc định tháng hiện tại, dùng package `intl` mới thêm), `ProgressCard`/`MeterCard` đọc dữ liệu thật, lưu từng dòng độc lập (1 phòng lưu lỗi không chặn phòng khác), validate current ≥ previous ngay trước khi gọi repository.
- Viết lại hoàn toàn `reading_detail_screen.dart` (H-06 Detail, `ConsumerStatefulWidget`): lịch sử đầy đủ 1 phòng+1 tiện ích, tính `isLocked` thật, "Edit reading" sửa tại chỗ (không chuyển màn, đúng SCREEN-SPEC) khi chưa khoá, chặn nút khi đã khoá kèm banner giải thích (BR-METER-13).
- Mở rộng `lib/shared/meter_card.dart`: thêm `errorText` (hiện lỗi validate ngay dưới field, không chặn các dòng khác), `onViewHistory` (tap header mở lịch sử — dùng lại 1 component cho cả 2 nhu cầu thay vì tạo card riêng), `enabled: !recorded` cho field Current (phòng đã ghi kỳ này thì chỉ đọc, sửa phải qua Detail).
- Nối thật `room_detail_screen.dart` (H-04): "View reading history" (trước đó `onPressed: null`, để TODO chờ H-06) giờ mở bottom sheet chọn Electricity/Water rồi điều hướng đúng; thay text tĩnh "No reading recorded yet." bằng widget `_ReadingSummaryRow` đọc thật từ `readingHistoryProvider`, hiện chỉ số + ngày gần nhất, tap mở Detail.
- **2 lỗi tự phát hiện qua test tay trên thiết bị thật (nhà thật "Nha tro của tao"), sửa ngay trong đợt này**:
  1. Sau khi lưu 1 phòng đã "Recorded", "Previous" hiển thị sai — dùng `latestForRoom` (chỉ số MỚI NHẤT bất kỳ) làm "cũ" ngay cả khi phòng đã ghi xong kỳ này, nên tự tham chiếu chính bản ghi vừa tạo thay vì bản ghi trước đó. Sửa: khi `thisPeriod != null`, lần theo đúng `thisPeriod.previousReadingId` (thêm `ReadingRepository.byId`) thay vì gọi lại `latestForRoom`.
  2. Field "Current" của 1 phòng đã "Recorded" hiện trống (`Enter…`) thay vì hiện đúng số đã ghi, và bộ nhớ controller theo `room+utility` (không theo kỳ) khiến gõ dở ở kỳ này rồi chuyển kỳ khác vẫn còn thấy chữ cũ. Sửa: thêm `prefill` cho controller (điền sẵn `thisPeriod.currentReading`), đổi key controller thành `room+utility+kỳ` thay vì chỉ `room+utility`.
- Thêm package `intl: ^0.19.0` vào `pubspec.yaml` (định dạng tháng/ngày, dùng chung nhiều nơi trong H-06).
- **Verify toàn bộ trên thiết bị thật, dữ liệu thật** (nhà "Nha tro của tao", phòng "p.101" — nhà/phòng của dungtv tự tạo, chỉ số test do tôi tạo để verify rồi xoá sạch sau khi xong qua Management API, không để lại dữ liệu rác): ghi chỉ số điện+nước kỳ 09/2026 → hiện đúng ở H-04 + H-06 Detail (kể cả "Recorded by" đúng SĐT thật của tài khoản); sửa tại chỗ chỉ số điện (100→125) → cập nhật đúng ngay; validate chặn đúng 1 dòng khi nhập chỉ số nhỏ hơn chỉ số cũ, dòng còn lại vẫn lưu được bình thường; chuyển sang kỳ kế tiếp → đúng "Not recorded" + "Previous" lấy đúng từ kỳ trước (không tự tham chiếu). `flutter analyze` sạch, `dart format` không có gì cần sửa.
- Dùng 1 nút bấm tạm (`[debug] Record readings (house)`) để có đường vào H-06 Entry lúc test trên thiết bị thật — đã gỡ bỏ hoàn toàn ngay sau khi xác nhận xong (`grep debug` rỗng), theo đúng tiền lệ đã dùng ở các đợt trước.

**Chưa làm/còn nợ (giữ nguyên từ trước, chưa đổi)**: **H-06 vẫn chưa có điểm vào thật trong Figma** (không có nút "Ghi chỉ số kỳ này" ở H-03 như mô tả cũ của SCREEN-SPEC.md) — cố tình không tự thêm nút thật vào H-03/H-04 vì đó sẽ là bịa layout ngoài Figma, cần Dream xác nhận vị trí trước; hiện chỉ có đường vào từ H-04 "View reading history" (đã có sẵn trên Figma, nay nối thật) cho phần xem/sửa lịch sử — còn màn "ghi hàng loạt cho cả nhà" (Entry) tạm thời chỉ gọi được qua route trực tiếp, chưa có nút bấm thật nào trong app dẫn tới. Chưa lọc theo `electricityBillingMethod`/`waterBillingMethod = NOT_BILLED` (BR-READ-05) vì đó là field trên `tb_contract_version`, Tenant/Contract (T-0x) chưa xây nên chưa có hợp đồng nào để mà lọc — sẽ bổ sung khi làm T-0x. Photo công tơ (tuỳ chọn theo SCREEN-SPEC) chưa nối ở H-06 Entry.

## 15:55 — dungtv (qua Claude Code)

**Tính năng/khu vực**: Cập nhật H-03/H-04 theo Figma mới — thêm "Record monthly readings" vào menu "⋮" của Nhà (điểm vào H-06 thật), ảnh phòng dạng slider, lịch sử chỉ số hiện `ListCard` thật

**Bối cảnh**: dungtv gửi 2 ảnh chụp Figma mới nhất — Dream đã cập nhật: (1) menu "⋮" ở H-03 (House Detail) giờ có 3 mục "Edit / Record monthly readings / Delete" thay vì 2 — chính là điểm vào H-06 Entry còn thiếu, ghi nhận nợ từ đợt 15:40; (2) H-04 Room Detail đổi ảnh sang dạng slider (mũi tên trái/phải) vì 1 phòng có thể có nhiều ảnh; (3) phần lịch sử chỉ số ở H-04 hiện 2 thẻ đầy đủ (icon + ngày + prev→current + usage + loại) thay vì dòng tóm tắt text.

**Đã làm**:
- Lấy lại design context thật qua Figma MCP: node `400:2652` (frame "Action" — menu 3 mục của House, có "Record monthly readings" ở giữa, đúng background/gạch chia như bản 2 mục đã sửa ở đợt 15:30) và node `220:2714` (H-04 cập nhật — ảnh dạng slider node `410:2908`, lịch sử chỉ số dùng lại đúng component "List card" 168:48 đã có sẵn trong Design System).
- Mở rộng `TopBarActionMenuButton` (`top_bar.dart`): thêm tham số optional `onRecordReadings` — khi có, chèn thêm dòng "Record monthly readings" giữa Edit/Delete (có gạch chia dưới cả 2 dòng đầu, đúng dòng cuối "Delete" không có gạch). Không set tham số này ở H-04 (menu Phòng vẫn 2 mục Edit/Delete, đúng Figma — chỉ menu Nhà ở H-03 có 3 mục).
- Nối `room_list_screen.dart` (H-03): `onRecordReadings: () => context.push('/home/houses/$houseId/readings')` — đây chính là điểm vào H-06 Entry thật đầu tiên trong app, không cần nút debug tạm nữa.
- Viết lại `_RoomPhoto` trong `room_detail_screen.dart` thành `ConsumerStatefulWidget` có state `_index` — 2 nút mũi tên trái/phải luôn hiện (đúng Figma, kể cả lúc chưa có ảnh), chỉ thật sự đổi ảnh khi `room.photos.length >= 2` (dùng phép modulo để quay vòng).
- Đổi `_ReadingSummaryRow` (dòng text tóm tắt) thành `_ReadingSummaryCard` — dùng lại `ListCard` component có sẵn (`lib/shared/list_card.dart`, đã dùng ở H-01/H-03) thay vì tự vẽ Row riêng, đúng nguyên tắc dùng chung component. Thumb vuông 52px màu cam (điện)/xanh dương (nước), title "ngày · loại tiện ích", body "cũ → mới · lượng dùng đơn vị · loại (Periodic/Move-in/Move-out)" — khi chưa có chỉ số nào thì hiện "Tên tiện ích · no reading yet" / "Tap to record the first reading." thay vì bịa nội dung mẫu.
- Verify toàn bộ trên thiết bị thật (dữ liệu thật của dungtv): menu "⋮" ở H-03 hiện đúng 3 mục, bấm "Record monthly readings" mở đúng H-06 Entry (không cần nút debug); ghi thử 1 chỉ số điện (80 kWh) qua Entry → quay lại H-04, card "Reading history" tự đổi từ "no reading yet" sang "10/09/2026 · Electricity" + "— → 80 · — kWh · Periodic" đúng định dạng Figma; ảnh slider hiện đúng 2 mũi tên ở khung ảnh trống. Xoá sạch chỉ số test qua Management API ngay sau khi xác nhận. `flutter analyze` sạch, `dart format` đã chạy.

**Đã đóng nợ từ đợt trước**: **H-06 giờ đã có điểm vào thật trong app** (qua menu "⋮" của Nhà ở H-03) — không còn cần nút debug tạm nữa, xoá mục "chưa có điểm vào" khỏi danh sách còn thiếu. Vẫn còn nợ: chưa lọc `NOT_BILLED` (chờ T-0x), chưa nối photo công tơ ở H-06 Entry.

## 16:10 — dungtv (qua Claude Code)

**Tính năng/khu vực**: Sửa ảnh slider H-04 bị co lại, đổi ô chọn kỳ H-06 sang đúng dạng thiết kế (date picker thật), điều tra báo cáo "chỉ số bị mất/lẫn"

**Bối cảnh**: dungtv phản hồi 3 việc sau khi tự test trên máy: (1) ảnh slider ở H-04 bị co lại, không full cả card; (2) ô "Reading period" ở H-06 nên là date picker thật nhưng hiển thị đúng dạng thiết kế (field readonly + icon lịch, không phải mũi tên tự vẽ); (3) nhập điện 80 rồi save, quay lại thì mất, còn nước lại thấy số 12345 lạ.

**Đã làm**:
- `_RoomPhoto` (`room_detail_screen.dart`): đổi từ `Row` (ảnh bị 2 nút mũi tên ép hẹp lại + padding ngang 10px ăn bớt chiều rộng) sang `Stack` với `fit: StackFit.expand` — ảnh giờ phủ đúng 100% khung 160px, 2 mũi tên `Positioned` đè lên trên (không chiếm layout nữa). Style mũi tên tách theo 2 trạng thái: chưa có ảnh → icon xám nhạt không nền (đúng mock placeholder gốc trên Figma); đã có ảnh thật → thêm nền tròn mờ đen để luôn đọc được trên ảnh sáng màu (Figma không mock sẵn trạng thái này, suy luận hợp lý theo pattern overlay đã dùng ở `photo_picker_row.dart`).
- `reading_entry_screen.dart`: bỏ hẳn khối `Container` tự vẽ mũi tên trái/phải, thay bằng đúng widget `AppTextField` (đã có sẵn, đúng Figma gốc — label "Reading period *", readonly, icon lịch cuối field) + `onTap` mở `showDatePicker` thật. Chọn ngày xong lấy year/month (day=1 vì `period_ym` là mốc đầu tháng). Phải thêm `key: ValueKey(_period)` vì `TextFormField.initialValue` chỉ đọc 1 lần lúc tạo — không có key này thì đổi kỳ xong chữ hiển thị vẫn đứng yên (tự phát hiện lúc test).
- **Điều tra báo cáo "chỉ số bị mất/lẫn"**: kiểm tra trực tiếp DB qua Management API (tách riêng từng bảng, tránh gộp 2 câu query khiến chỉ đọc được kết quả câu sau — đây chính là lý do tôi từng đọc nhầm giá trị 12345 là ở bảng điện, gây hiểu lầm) — xác nhận: giá trị "12345" là ở bảng NƯỚC, do chính dungtv tự nhập lúc test trước đó (không phải dữ liệu tôi tạo), còn giá trị "80" (điện) đúng là bị thiếu — 1 lần dọn dữ liệu test trước đó của tôi (đợt 15:55) chỉ xoá bảng điện, bỏ sót bảng nước, khiến 2 giá trị hiện cạnh nhau trông như bị lẫn. Test lại trực tiếp trên bản build mới nhất (nhập điện=80, giữ nguyên nước=12345, bấm Save): verify qua Management API xác nhận cả 2 bảng đều lưu đúng, tách biệt, không lẫn dữ liệu — CRUD hoạt động đúng ở build hiện tại.
- Phát hiện thêm khi test slider: 1 lần chụp ảnh quá sớm (2s sau khi mở màn) thấy khung ảnh trống dù đã có ảnh thật trong `tb_room.photos` — hoá ra chỉ là chưa tải xong signed URL qua mạng (chờ thêm ~2s là hiện đúng), không phải lỗi — đã thêm `CircularProgressIndicator` nhỏ khi đang tải + hiện lỗi thật (thay vì im lặng để trống) nếu tải ảnh thất bại thật sự, dễ chẩn đoán hơn về sau.
- Verify trên thiết bị thật, dữ liệu thật của dungtv (nhà "Nha tro của tao", phòng "p.101", đã có sẵn 2 ảnh thật do dungtv tự upload): slider chuyển đúng 2 ảnh, ảnh full khung, mũi tên overlay đẹp; ô "Reading period" đúng dạng thiết kế, tap mở date picker thật, chọn xong hiển thị đúng định dạng "Tháng Năm · dd/MM/yyyy"; điện=80 lưu đúng, tách biệt với nước=12345 có sẵn. `flutter analyze` sạch, `dart format` đã chạy.

**Chưa làm/còn nợ**: chưa hỏi dungtv có muốn xoá 2 giá trị test (điện 80, nước 12345) ở phòng p.101 hay giữ lại — sẽ hỏi trực tiếp, không tự ý xoá dữ liệu phòng thật của dungtv. → Đã hỏi, dungtv chọn giữ nguyên (xem đợt sau).

## 16:20 — dungtv (qua Claude Code)

**Tính năng/khu vực**: Rà soát lại H-06 Entry so với Figma thật (dungtv báo "vẫn chưa giống"), sửa 2 chỗ lệch thật

**Bối cảnh**: dungtv yêu cầu bám sát design, tự kiểm tra kỹ xem màn còn thiếu chi tiết gì. Lấy lại design context thật qua Figma MCP cho đúng node H-06 Entry (`220:3077`) để so từng chi tiết thay vì đoán, phát hiện 2 điểm lệch thật:

1. **Subtitle Top bar thiếu "X/Y rooms recorded"** — Figma: "Nha tro Binh An  ·  6/8 rooms recorded  ·  Sep 2026" (3 phần); code trước đó chỉ hiện mỗi tên nhà, thiếu hẳn tiến độ ghi chỉ số và kỳ (dùng tháng viết tắt "Sep 2026", khác với field "Reading period" dùng tháng đầy đủ "September 2026" — 2 định dạng khác nhau có chủ đích theo đúng Figma).
2. **Số liệu (Previous/Current/Usage) thiếu dấu phẩy phân cách hàng nghìn** — Figma hiện "1,120 kWh"/"1,216"/"96 kWh" ở mọi nơi hiện chỉ số (H-06 Entry, H-06 Detail, thẻ tóm tắt H-04), code trước đó hiện số thô không dấu phẩy ("1120"/"1216").

**Đã làm**:
- Thêm `formatReadingValue()` (dùng `NumberFormat('#,##0.##')` từ package `intl`) vào `models/reading.dart` — hàm dùng chung cho mọi nơi hiện số chỉ số trong app, tránh mỗi màn tự viết logic format riêng.
- `reading_entry_screen.dart`: subtitle Top bar giờ ghép đủ 3 phần (tên nhà · X/Y rooms recorded · tháng viết tắt); áp `formatReadingValue` cho Previous, Usage, và cả giá trị điền sẵn (`prefill`) của field Current khi phòng đã "Recorded" — an toàn vì field đó bị disable, `_save()` không bao giờ đọc lại để parse số (đã kiểm tra kỹ trước khi làm, tránh vá nhầm chỗ ảnh hưởng lưu dữ liệu).
- Áp `formatReadingValue` đồng bộ ở `room_detail_screen.dart` (thẻ tóm tắt H-04) và `reading_detail_screen.dart` (H-06 Detail: dòng Previous→Current, lịch sử, cả thông báo lỗi validate) — cùng 1 loại dữ liệu, cùng 1 chuẩn hiển thị trong toàn app, không để mỗi màn 1 kiểu.
- Verify trên thiết bị thật: subtitle hiện đúng "Nhà trọ của tao · 2/2 rooms recorded · Sep 2026"; số nước 12345 (dữ liệu test của dungtv, giữ nguyên theo yêu cầu) hiện đúng "12,345" có dấu phẩy. `flutter analyze` sạch, `dart format` đã chạy.

## 16:35 — dungtv (qua Claude Code)

**Tính năng/khu vực**: Sửa lỗi "2/2 rooms" sai (đếm theo dòng thay vì theo phòng), sửa màu nền field "Reading period" chưa đúng Figma

**Bối cảnh**: dungtv phát hiện 2 vấn đề mới. (1) Nhà chỉ có 1 phòng (p.101) nhưng H-06 Entry hiện "2/2 rooms recorded" — sai bản chất, vì 1 phòng sinh 2 dòng (điện + nước) trong danh sách entries, code trước đó đếm thẳng số dòng thay vì số phòng thật. (2) Màu nền field "Reading period" chưa giống thiết kế (còn phần chọn ngày/tháng cụ thể phụ thuộc UI hệ điều hành Android/iOS nên không cần khớp Figma).

**Đã làm**:
- Thêm `_roomProgress()` trong `reading_entry_screen.dart` — gộp `entries` (vốn có 2 dòng/phòng: điện + nước) theo `room.id`, 1 phòng chỉ tính "đã ghi" khi CẢ điện lẫn nước đều `recorded`. Áp cho cả `ProgressCard` và dòng "X/Y rooms recorded" ở subtitle Top bar — trước đó cả 2 chỗ đều lấy thẳng `entries.length`/`entries.where(recorded).length`, vô tình đếm theo (phòng × tiện ích) nên nhà 1 phòng bị hiện thành "2/2".
- Kiểm tra lại đúng component "Input field" (variant Date) trên Figma cho field "Reading period": nền **trắng + viền** (giống field đang gõ được), KHÔNG phải nền xám mờ `bgMuted` không viền của field readonly thật sự (VD field "Status" ở H-05, vẫn đúng vì đó là field tĩnh hoàn toàn, không tap được). Bỏ tham số `readOnly: true` khỏi `AppTextField` ở `reading_entry_screen.dart` — chỉ để `onTap` một mình đảm nhiệm việc chặn gõ tay (bản thân `AppTextField` đã tự tính `readOnly || onTap != null` cho `TextFormField` bên trong), không còn đổi nhầm màu nền.
- Verify trên thiết bị thật: nhà 1 phòng hiện đúng "1/1 rooms recorded" (cả ở subtitle lẫn Progress card); field "Reading period" hiện đúng nền trắng + viền khớp Figma. `flutter analyze` sạch, `dart format` đã chạy.

