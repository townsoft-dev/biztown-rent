# 2026-08-26

## 07:37 — dungtv

**Tính năng/khu vực**: Project setup

**Thay đổi**:
- Khởi tạo git repo, tạo cấu trúc thư mục ban đầu: `src/`, `backend/`, `tests/`, `docs/`.
- Tạo bộ docs mẫu: PRODUCT.md, ARCHITECTURE.md, DESIGN.md, API.md, DATABASE.md, DECISIONS.md, CURRENT_STATUS.md, CHANGELOG.md, CLAUDE.md.
- Đặt tên tạm cho project: **RentEase**.
- Set git identity: `user.name = dungtv`, `user.email = dungtv@townsoftvina.com`.

## 07:42 — dungtv

**Tính năng/khu vực**: Design folder

**Thay đổi**:
- Chuyển thư mục `design/` (file thiết kế Figma/mockup) từ ngoài repo vào trong repo, để dễ đồng bộ với code.
- Cập nhật README.md, docs/DESIGN.md, docs/CLAUDE.md, docs/DECISIONS.md tham chiếu `design/` trong repo.

## 07:50 — dungtv

**Tính năng/khu vực**: Tech stack

**Thay đổi**:
- Chốt stack: **Flutter** (frontend, thư mục `src/`) + **Supabase** (DB/Auth/Storage) + **Vercel** (serverless functions, thư mục `backend/`).
- Xóa `go.mod`, `package.json` (root); thêm `src/pubspec.yaml` placeholder, `backend/package.json` cho Vercel functions.
- Cập nhật docs/PRODUCT.md, docs/ARCHITECTURE.md, docs/DATABASE.md theo đúng entities/flow nghiệp vụ (House, Room, Tenant, Contract, meter_reading, Bill).

## 07:57 — dungtv

**Tính năng/khu vực**: Thông báo Bill

**Thay đổi**:
- Đổi kênh gửi Bill từ Zalo/SMS sang **Email/SMS** (bỏ Zalo để tránh phải xin duyệt Zalo OA).
- Cập nhật docs/PRODUCT.md, docs/ARCHITECTURE.md, docs/DATABASE.md, src/README.md.

## 07:59 — dungtv

**Tính năng/khu vực**: Quy trình / Changelog

**Thay đổi**:
- Thêm folder `changelog/` riêng, dùng để ghi nhật ký thay đổi chi tiết theo ngày/giờ/người thực hiện (khác với `docs/CHANGELOG.md` là tóm tắt release).

## 08:09 — dungtv

**Tính năng/khu vực**: Backend infra

**Thay đổi**:
- Bỏ Vercel khỏi stack, chuyển toàn bộ backend logic sang Supabase-only (Edge Functions + Scheduled Triggers thay cho Vercel serverless functions + Cron).
- Xóa thư mục `backend/`, thêm `supabase/` (functions/, migrations/, README hướng dẫn setup Supabase CLI).
- Cập nhật README.md, docs/ARCHITECTURE.md, docs/CLAUDE.md, docs/API.md, docs/CURRENT_STATUS.md, docs/CHANGELOG.md, docs/DECISIONS.md, src/README.md, .gitignore.

## 08:30 — dungtv

**Tính năng/khu vực**: Claude Code config

**Thay đổi**:
- Tạo `CLAUDE.md` ở root (import `docs/CLAUDE.md` + quy tắc quy trình) — file thật được Claude Code tự load mỗi session.
- Tạo `.claude/rules/flutter.md` (path-scoped `src/**/*.dart`) làm khung tiêu chuẩn code Flutter senior-dev, hiện là placeholder chờ design UI + mô tả nghiệp vụ để điền chi tiết.

## 08:48 — dungtv

**Tính năng/khu vực**: Local dev environment

**Thay đổi**:
- Commit git đầu tiên (root commit) cho toàn bộ scaffold project.
- Cài Flutter SDK 3.47.1 (git clone stable channel vào `~/development/flutter`, thêm PATH vào `~/.zshrc`). `flutter doctor` chạy được; Android SDK và Xcode đầy đủ chưa cài (cần khi build thật).
- Cài Supabase CLI 2.115.0 làm npm devDependency ở root (`package.json` mới, chỉ dùng cho CLI tooling, không phải app code) — chạy qua `npx supabase`.
- Thêm `node_modules/` vào `.gitignore`.
- Cập nhật docs/CURRENT_STATUS.md, supabase/README.md, src/README.md theo trạng thái môi trường mới.

## 08:58 — dungtv

**Tính năng/khu vực**: GitHub

**Thay đổi**:
- Tạo SSH key (`~/.ssh/id_ed25519`) và thêm vào tài khoản GitHub (dungtv1291).
- Fix lỗi "Host key verification failed" (thêm github.com vào `~/.ssh/known_hosts`).
- Tạo repo GitHub trống (`dungtv1291/rentease`), add remote `origin`, push nhánh `main`.
- Repo: https://github.com/dungtv1291/rentease

## 09:07 — dungtv

**Tính năng/khu vực**: GitHub

**Thay đổi**:
- Transfer repo từ tài khoản cá nhân (dungtv1291) sang Organization **townsoft-dev** (chuẩn bị cho bàn giao sau này).
- Cập nhật git remote local sang `git@github.com:townsoft-dev/rentease.git`.
- Repo mới: https://github.com/townsoft-dev/rentease

## 09:10 — dungtv

**Tính năng/khu vực**: Supabase env

**Thay đổi**:
- Xác nhận push lên `townsoft-dev/rentease` đúng (local HEAD khớp origin/main).
- Thêm `supabase/.env.example` — mẫu biến môi trường (SUPABASE_URL, ANON_KEY, SERVICE_ROLE_KEY, ACCESS_TOKEN), verify `.env` đã được `.gitignore` bỏ qua.
- Quyết định tạm thời: tạo Supabase project bằng email cá nhân dungtv, chấp nhận transfer ownership sau khi có tài khoản khách.

## 09:24 — dungtv

**Tính năng/khu vực**: Supabase link

**Thay đổi**:
- Nhận Publishable key + Secret key từ project Supabase thật (`rentease`, ref `rrtppoibjprlvasnbvwr`, region South Asia/Mumbai). Lưu vào `supabase/.env` (local, gitignored) — **Secret key bị dán vào chat nên coi như lộ, đã khuyến nghị rotate**.
- Chạy `supabase init` — sinh `supabase/config.toml` + `supabase/.gitignore`.
- Thử `supabase link --project-ref rrtppoibjprlvasnbvwr` — thất bại, thiếu Personal Access Token. Đang chờ bạn tạo token tại dashboard/account/tokens.

## 09:29 — dungtv

**Tính năng/khu vực**: Supabase link

**Thay đổi**:
- Nhận Personal Access Token, lưu vào `supabase/.env` — **token cũng bị dán vào chat, coi như lộ, đã khuyến nghị revoke sau khi dùng**.
- `supabase link --project-ref rrtppoibjprlvasnbvwr` thành công. Verify qua `supabase projects list`: project `rentease`, status ACTIVE_HEALTHY, Postgres 17, region ap-south-1, `linked: true`.

## 09:33 — dungtv

**Tính năng/khu vực**: Bàn giao / Docs

**Thay đổi**:
- Thêm `docs/SETUP.md` — hướng dẫn setup môi trường đầy đủ cho người mới nhận project (Flutter, Supabase CLI, lấy API keys, link project, 2 kịch bản dùng lại project cũ hoặc tạo mới).
- Cập nhật README.md trỏ tới file này.
- Quyết định: không cần lo bảo mật token trong giai đoạn dev, khách sẽ tự lấy key riêng lúc bàn giao — nhưng cần doc rõ ràng để việc đó chỉ còn là đọc hướng dẫn.

## 15:13 — dungtv

**Tính năng/khu vực**: Đổi tên project

**Thay đổi**:
- Khách hàng chốt tên chính thức: Brand **BizTown**, Product **Rent-Manager**.
- Đổi tên repo GitHub từ `rentease` sang `biztown-rent`, cập nhật git remote local.
- Thay "RentEase" bằng "BizTown Rent-Manager" trong README, docs/*, CLAUDE.md, .claude/rules/flutter.md, design/README.md, supabase/README.md, src/README.md.
- `src/pubspec.yaml`: `name` đổi sang `rent_manager`. `package.json`: `name` đổi sang `biztown-rent-tooling`, chạy lại `npm install` để đồng bộ lockfile.
- Supabase project trên dashboard vẫn tên `rentease` (chưa đổi, không bắt buộc).

## 16:12 — dungtv

**Tính năng/khu vực**: Claude Code + Figma

**Thay đổi**:
- Cài chuẩn `@anthropic-ai/claude-code` CLI qua npm (extension VS Code không tự expose lệnh này ra terminal).
- Xác minh lại thông tin: không có marketplace "claude-plugins-official" cho plugin Figma như dự đoán ban đầu — tìm ra URL MCP server chính thức thật của Figma qua developers.figma.com: `https://mcp.figma.com/mcp` (remote, không cần Figma desktop app hay gói trả phí, Claude Code nằm trong danh sách client hỗ trợ).
- Đăng ký: `claude mcp add --transport http figma https://mcp.figma.com/mcp` — thành công, lưu vào `~/.claude.json`.
- `claude mcp login figma` thất bại vì cần terminal tương tác (mở trình duyệt) — phiên hiện tại không làm được, cần dungtv tự chạy lệnh này ở terminal thật để hoàn tất OAuth.
- Thêm mục 7 vào `docs/SETUP.md` hướng dẫn bước này cho người sau.
