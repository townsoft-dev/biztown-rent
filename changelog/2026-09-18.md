# 2026-09-18

## 12:30 — Claude · Vá gạch phân cách trong menu "⋮" (Nhà/Phòng)

**Bối cảnh**: dungtv gửi ảnh Figma frame "Action" (menu 3 mục Edit / Record monthly readings / Delete) kèm nhận xét *"mấy cái gạch nhìn rõ ràng thế này cơ mà"* — tức trong app đường kẻ ngăn cách không giống thiết kế.

Chụp lại menu thật trên `emulator-5554` rồi phóng to đối chiếu, thấy **hai lỗi** chứ không phải một:

**1. Gạch chỉ dài bằng đúng chữ của dòng đó.** "Sửa" có gạch cụt ngủn bằng 2 chữ cái, "Ghi chỉ số hàng tháng" có gạch dài hơn hẳn — nhìn như vỡ layout chứ không ra đường kẻ ngăn cách. Nguyên nhân: `_ActionPopupRow` bọc mỗi chữ trong một `Container` tự co, mà `PopupMenuButton` lại bọc toàn bộ nội dung trong `IntrinsicWidth`, nên mỗi dòng rộng đúng bằng chữ của nó. Sửa bằng `width: double.infinity` — dưới `IntrinsicWidth` thì cả 3 dòng cùng nhận bề rộng bằng dòng dài nhất, gạch chạy suốt.

**2. Màu gạch gần như vô hình.** Đang dùng `borderSubtle` `#EEF0F5`, chỉ lệch ~4% so với nền trắng của menu nên trên máy thật gần như không thấy. Đổi sang `neutral200` `#B9BDCC` — đây cũng là token viền đã định nghĩa trong `docs/DESIGN-SYSTEMS.md`, và là màu khớp với bản thiết kế dungtv gửi.

Đây là **điều chỉnh một quyết định cũ**: Đợt 17 ngày 10/09/2026 chọn `borderSubtle` sau khi đọc node Figma, và đã verify trên máy thật lúc đó. Lần này bản render Figma do dungtv gửi cho thấy đường kẻ đậm hơn thế, nên lấy bản render làm chuẩn — xem `docs/DECISIONS.md`.

**Verify**: chụp lại cả hai nơi dùng component này — menu Nhà ở H-03 (3 mục) và menu Phòng ở H-04 (2 mục) — gạch nay chạy hết bề ngang menu và nhìn rõ ở cả hai. `flutter analyze` sạch.

**Ghi chú**: Figma MCP đang mất đăng nhập nên không lấy được node trực tiếp; đối chiếu bằng ảnh dungtv gửi, đúng cách đã thống nhất cho trường hợp này.

---

## 13:10 — Claude · Đường ngăn cách trong thẻ chi tiết: đổi từ nét liền sang **nét chấm**

**Bối cảnh**: dungtv gửi tiếp 2 ảnh Figma (B-02 Hoá đơn chi tiết và khối "Điều khoản hiện hành" ở T-05) — *"cả mấy cái gạch chấm này nữa, xem figma mà làm lại cho đúng"*. Thiết kế vẽ đường ngăn cách giữa các dòng key–value là **nét chấm**, còn app đang vẽ **nét liền** màu `borderSubtle` `#EEF0F5` (vốn đã gần như vô hình, cùng nguyên nhân với lỗi menu "⋮" sửa lúc 12:30).

**Sửa ở đúng một chỗ**: `DetailRow` (`lib/shared/detail_row.dart`) — component dùng chung, nên **13 file/màn** dùng nó đều được vá cùng lúc: H-03 Thông tin nhà, H-04 Phòng, B-02 Hoá đơn, T-04 Người thuê, T-05 Hợp đồng (kể cả khối điều khoản dùng lại ở màn Gia hạn và Kết thúc), T-10 Xem trước hoá đơn, màn Chỉ số, màn Tạo hoá đơn lẻ, và checklist mật khẩu.

`Border` của Flutter không có kiểu nét chấm nên phải tự vẽ qua `Decoration`/`BoxPainter` — dùng lại đúng cách đã làm cho viền đứt nét của chip "kỳ tương lai" ở `invoice_schedule_strip.dart`, **không** lấy nét liền làm giải pháp thay thế cho xong.

Thông số: chấm rộng 1.5, cách nhau 3.0, dày 1.0, đầu bo tròn (`StrokeCap.round`), màu `neutral200` `#B9BDCC` — nét chấm vốn đã thưa nên dùng `#EEF0F5` thì coi như không thấy gì.

**Verify**: chụp màn thật trên `emulator-5554` ở H-03 và T-10, thu nhỏ về đúng mật độ ảnh Figma dungtv gửi rồi đối chiếu — khớp. Lưu ý khi tự kiểm tra sau này: nét chấm 1px **biến mất khi thu nhỏ ảnh chụp**, phải xem ở độ phân giải gốc mới thấy. `flutter analyze` sạch.

---

## 13:45 — Claude · Figma MCP nối lại: đo pixel và sửa cả 2 thông số đã đoán sai

dungtv nối lại connector Figma. Đo trực tiếp trên node thật thì **cả hai lần sửa hôm nay đều sai thông số** — sửa đúng hướng (nét liền → nét chấm, gạch cụt → chạy suốt) nhưng sai màu và sai bước chấm.

| | Sáng nay (đoán từ ảnh) | Đo thật từ Figma |
|---|---|---|
| Gạch menu "⋮" (`400:2652`) | `#B9BDCC` | **`#7F7F7F`** |
| Gạch nét chấm thẻ chi tiết (`168:58`) | chấm 1.5 / cách 3.0, `#B9BDCC` | **chấm 2.0 / cách 2.0**, **`#EEF0F5`** |

Cách đo: tải ảnh render của node về rồi quét từng hàng pixel, tìm hàng có nhiều điểm không-trắng nhất và đếm hoa tiết. Gạch menu ra `.###...` liên tục 96% bề rộng ⇒ nét liền. Gạch thẻ chi tiết ra đúng `.##..##..##..` ⇒ chấm 2px cách 2px, phủ 47% bề rộng.

**Một điểm dễ sai**: màu `#EEF0F5` của thẻ chi tiết **vốn đã đúng trong code cũ**, chỉ mỗi kiểu nét là sai. Sáng nay tôi suy "`#EEF0F5` gần như vô hình nên chắc chắn sai" — đúng với menu nhưng sai với thẻ chi tiết. Hai component dùng hai màu khác nhau, không suy từ cái này ra cái kia được.

`#7F7F7F` thêm thành `AppColors.menuDivider` riêng, không gộp vào token viền có sẵn vì nó không nằm trong bảng token của Design System.

**Verify**: chụp lại emulator rồi đo ngược — gạch menu ra đúng `#7F7F7F` liền, gạch thẻ chi tiết ra đúng nét chấm nhạt như bản render Figma. `flutter analyze` sạch. Đã sửa lại mục Đợt 61 trong `docs/DECISIONS.md` cho khỏi để lại bản ghi sai.

---

## 08:12 — Claude · Bổ sung bộ logo thương hiệu mới, đổi đồng loạt + tách riêng Splash

**Bối cảnh**: Dream gửi bộ file logo thương hiệu mới vào `design/Logo/` (các file `biztown-*-icon-wordmark*.png`, `rentmanager-wordmark*.png`, `rentmanager-2line-cover-*.png`, cùng lúc ~00:30-00:56 sáng nay) và yêu cầu: (1) chốt lại rõ **thương hiệu "BizTown"** / **sản phẩm "Rent Manager"** — tên thương hiệu luôn đi cùng icon, không dùng icon trần; (2) đổi đồng loạt logo trong thiết kế theo bộ mới; (3) riêng Splash: chỉ đẩy logo thương hiệu lên trên, tên sản phẩm để riêng ở dưới.

**Đã làm**:
- Xem qua toàn bộ 26 file mới bằng cách mở từng ảnh — phát hiện bộ icon-wordmark có 4 biến thể theo module (Rent/Invoice/Staff/Inventory, phân biệt bằng màu cột), và 2 bản chữ cho riêng "Rent": `biztown-rent-icon-wordmark.png` ("BIZTOWN" viết hoa toàn bộ) và `...-alt.png` ("BizTown" viết hoa/thường). **Chọn dùng bản `-alt`** vì đây là kiểu chữ khớp với 3 module còn lại (Invoice/Staff/Inventory) và khớp cách viết "BizTown" trong mọi doc/README hiện có — bản không-alt viết hoa toàn bộ là điểm lẻ duy nhất, có khả năng là bản nháp/thử kiểu chữ chưa chốt. Đây là suy luận từ tính nhất quán, **không phải Dream xác nhận trực tiếp** — xem `NEEDS INPUT` ở `docs/DESIGN-SYSTEMS.md` mục 1.1 nếu cần đổi lại.
- Copy 2 file cần dùng vào `src/assets/logo/` (không copy hết cả bộ vào app — các biến thể màu khác/module khác/`2line-cover`/`wordmark-bg-*` chỉ dùng ngoài app, để lại trong `design/Logo/` làm tham khảo, xem bảng chi tiết trong `docs/DESIGN-SYSTEMS.md`):
  - `biztown-rent-icon-wordmark-alt.png` — logo thương hiệu (icon + "BizTown"), dùng ở S-00 Splash và S-01 Login.
  - `rentmanager-wordmark-white.png` — wordmark "RENT MANAGER" riêng, chữ trắng nền trong suốt, dùng ở Splash (dưới logo thương hiệu).
  - `pubspec.yaml` không cần sửa — đã khai báo cả thư mục `assets/logo/` từ trước.
- **`lib/shared/splash_screen.dart`**: đổi từ 1 `SvgPicture.asset` lockup gộp (icon+BizTown+RENT MANAGER chung 1 khối, trên nền navy) thành 2 `Image.asset` xếp dọc: logo thương hiệu (116px) → khoảng cách 18px → wordmark "RENT MANAGER" (188px) → khoảng cách 14px → tagline (giữ nguyên). Bỏ import `flutter_svg` (không còn dùng trong file này).
- **`lib/shared/login_screen.dart`**: đổi từ lockup gộp nền sáng (200px, có cả "RENT MANAGER") thành riêng logo thương hiệu (72px) — **không** lặp lại "RENT MANAGER" ở đây vì màn đã có tiêu đề "Login" ngay dưới, tránh thừa. Bỏ import `flutter_svg` (không còn dùng trong file này).
- Rà lại toàn bộ `lib/` — chỉ 2 màn trên có tham chiếu tới logo (`grep` thủ công qua từng file khả nghi: `top_bar.dart`, `app_banner.dart`, `mini_profile_card.dart`, `app_shell.dart`, `signup_screen.dart`, `forgot_password_screen.dart`, `main.dart` — không file nào dùng asset logo). App icon OS (`assets/icon/app_icon.png`, dùng bởi `flutter_launcher_icons`) **giữ nguyên, không đổi** — icon trần không chữ là ngoại lệ hợp lý cho icon màn hình chính OS (chữ không đọc được ở size nhỏ), xem ghi chú trong `docs/DESIGN-SYSTEMS.md`.
- Cập nhật `docs/DESIGN-SYSTEMS.md` mục 1.1 (bảng asset logo, ghi rõ file nào còn dùng/đã thay thế) và `docs/SCREEN-SPEC.md` mục S-00 (đặc tả lại 2 tầng logo thương hiệu/tên sản phẩm).

**Chưa làm / giới hạn phiên làm việc này**: không chạy được `flutter analyze` hay `flutter build` — phiên Claude này chỉ có quyền đọc/ghi file qua cầu nối máy Dream (`device_stage_files`/`device_commit_files`), không có shell (`device_bash`) trên máy Dream nên không tự chạy lệnh Flutter/git được. **Dream cần tự chạy `flutter analyze` để xác nhận sạch, và tự `git add`/`git commit` các file đã đổi** (đã liệt kê ở trên) — session sau nối lại `device_bash` thì làm được luôn phần này.

**Verify**: xem lại từng file ảnh trước khi chọn (đã liệt kê ở trên), đối chiếu tên biến thể với README/doc hiện có để chọn đúng kiểu chữ "BizTown". Chưa verify được bằng ảnh chụp máy thật/emulator (không có shell) — cần Dream hoặc session có `device_bash` verify lại hình ảnh hiển thị đúng tỉ lệ trên Splash/Login.

---

## 08:43 — Claude · Đồng bộ logo lên Figma: Dream tự làm, ghi nhận lại phần việc còn thiếu

**Bối cảnh**: Sau khi đổi logo trong code (mục 08:12 ở trên), Dream yêu cầu cập nhật luôn logo tương ứng trong file Figma (**BizTown Rent-Manager — MVP Wireframes**), và nhắc lại rõ: ở Splash, **logo thương hiệu (BizTown) nằm trên, logo/wordmark sản phẩm (Rent Manager) nằm dưới** — khớp đúng thứ tự đã code.

**Không tự làm được**: Kiểm tra thấy phiên Claude này chỉ có kết nối Figma dạng Dev Mode MCP (đọc thiết kế: `get_metadata`/`get_screenshot`/`get_design_context`...) — hiện **chưa bật** trên máy Dream, và bản chất chỉ đọc, **không có công cụ ghi/sửa node trong Figma**. Vì vậy không thể tự sửa file Figma trong phiên này dù bật lại kết nối.

**Quyết định**: Dream tự thao tác trực tiếp trong Figma. Việc cần làm khi Dream sửa, để khớp đúng với code đã đổi (mục 08:12):
- **S-00 Splash**: thay logo lockup cũ (gộp icon+"BizTown"+"RENT MANAGER" chung 1 khối) bằng **2 layer tách riêng, xếp dọc**: (1) logo thương hiệu BizTown ở trên — dùng file `design/Logo/biztown-rent-icon-wordmark-alt.png` (bản "Rent", chữ "BizTown" viết hoa/thường); (2) wordmark "RENT MANAGER" riêng ở dưới — dùng `design/Logo/rentmanager-wordmark-white.png` (chữ trắng, hợp nền navy của Splash).
- **S-01 Login** (hoặc màn tương đương): thay logo lockup cũ nền sáng bằng riêng logo thương hiệu (`biztown-rent-icon-wordmark-alt.png`), **không** kèm "RENT MANAGER" vì màn đã có tiêu đề riêng.
- Các màn khác trong Figma nếu có dùng lockup logo cũ (`biztown-rent-manager-lockup*.svg`) — rà lại và đổi tương tự theo logo thương hiệu mới nếu có xuất hiện icon BizTown ở đó.

**Việc còn mở**: Đồng bộ Figma ↔ code cho logo — **Dream tự làm trực tiếp trong Figma**, chưa có ai/session nào verify lại sau khi Dream sửa xong. `docs/DESIGN-SYSTEMS.md`/`docs/DESIGN.md` chưa có dòng nào xác nhận Figma đã khớp — nên cập nhật lại (bỏ dòng `NEEDS INPUT` này) sau khi Dream sửa xong trên Figma, để tài liệu phản ánh đúng trạng thái mới nhất.

---

## 14:40 — Claude · Dựng ẢNH hoá đơn (PNG) theo thiết kế MOCK-IMG, thay cho mã QR trơ trọi

**Câu hỏi của dungtv**: "liệu code có thể tạo ra cái ảnh hoá đơn như thế này không?" — trỏ vào frame `MOCK-IMG` (node `526:2485`).

**Trả lời: được.** Đã dựng hàm `invoice-image`: `GET /functions/v1/invoice-image/<ma_tra_cuu>` trả về PNG 750×~2000.

**Cách làm**: ghép chuỗi SVG rồi rasterise bằng `resvg` bản WASM, nạp font Be Vietnam Pro từ Google Fonts. PNG là thứ **duy nhất** cổng Supabase phục vụ được — HTML bị ép `text/plain`, SVG dính CSP `sandbox` nên mở ra trắng trơn (đã chứng minh hồi làm `invoice-qr`).

**Logo dùng vector thật**, trích thẳng từ `design/Logo/biztown-rent-manager-lockup-on-navy.svg` sang `functions/invoice-image/logo.ts`. Khác hẳn mẫu email (phải vẽ mô phỏng bằng ô bảng vì email không cho dùng SVG) — ở đây nền là SVG nên nhúng được vector gốc, sắc nét ở mọi cỡ.

**Toạ độ lấy từ metadata frame Figma, màu đọc pixel trực tiếp** trên bản render: navy `#23305E`, cam `#EF9F27`, nhãn `#6F7D99`, phí con `#5A6B8A`, đường kẻ `#EEF0F5`, hộp tổng nền `#FCEFE4` chữ `#C9662A`, hạn thanh toán `#B0402E`, viền QR đứt nét `#D6DAE6`, thẻ ngân hàng `#F3F4F8`, chân trang `#A4A9BD`.

**Hai lỗi tự phát hiện khi đối chiếu:**

1. **Chữ đậm không ra đậm.** Chỉ lấy link font ĐẦU TIÊN trong CSS của Google Fonts, tức nét 400, nên mọi `font-weight="700"` vẫn ra nét thường — resvg **không làm đậm giả**, thiếu file là thiếu luôn nét đậm. Sửa: nạp cả 2 file 400 và 700.
2. **Thừa khoảng trắng ở khối thông tin trên.** Figma có 3 dòng (Người thuê / Mã hợp đồng / Mã hoá đơn), ta chỉ có 2 vì schema không có trường mã hợp đồng (DECISIONS Đợt 60). Dồn đường kẻ và bảng tiền lên, giữ nguyên khoảng cách 22px/28px như Figma.

**Số đo thật**: lần gọi đầu ~4s (tải wasm + 2 file font), các lần sau ~0,8s, ảnh 133KB. Mã QR sinh lại từ **tài khoản hiện tại của chủ nhà**, không dùng chuỗi lưu sẵn trong hoá đơn — cùng lý do đã ghi ở `invoice-qr`.

**Verify**: gọi thật với hoá đơn `98A31YT6Q1W0`, ghép ảnh cạnh bản render Figma để so từng khối — khớp. Khác biệt còn lại chỉ là dữ liệu (dòng "Mã hợp đồng" không có, hoá đơn thật có 3 dòng phí thay vì 2).

**Chưa làm**: link trong SMS vẫn trỏ `invoice-qr` (chỉ mã QR). Đổi sang `invoice-image` thì tin nhắn dài thêm 3 ký tự — chờ dungtv chốt.

---

## 15:20 — Claude · SMS chuyển sang gửi link ẢNH hoá đơn, đổi tên hàm cho vừa giới hạn mẫu eSMS

dungtv chốt: link gửi trong SMS từ nay là ảnh hoá đơn đầy đủ, không còn là ô mã QR trơ trọi.

**Một ràng buộc suýt bỏ sót**: link lọt vào ô tham số `{P2,70}` của mẫu eSMS đi mượn — **tối đa 70 ký tự**, vượt là bị từ chối. Tên `invoice-image` cho ra link **72 ký tự**, tức gửi đi là hỏng. Phát hiện trước khi gửi nhờ tính lại độ dài chứ không phải sau khi tin nhắn thất bại.

Đã **đổi tên hàm `invoice-image` → `invoice`**: link còn 66 ký tự, dư 4. Ghi chú cảnh báo vào `supabase/config.toml` và `invoice_message.dart` để lần sau không ai đổi thành tên dài hơn khi chưa có tên miền riêng.

| | Độ dài link | Vừa ô 70 ký tự? |
|---|---|---|
| `invoice-qr` (cũ) | 69 | vừa |
| `invoice-image` | 72 | **không** |
| `invoice` (chốt) | 66 | vừa, dư 4 |

**Giữ nguyên `invoice-qr` trên server** — những tin nhắn đã gửi cho người thuê trước đây vẫn trỏ vào đó, xoá đi là chết link. Đã xoá `invoice-image` (tên trung gian, chưa từng gửi cho ai).

Đổi tên hàm Dart `invoiceQrLink` → `invoiceImageLink` cho khớp việc nó làm.

**Verify thật, đủ 4 điểm**:
- Link mới trả PNG 750×2006, HTTP 200.
- Link cũ `invoice-qr` vẫn HTTP 200 — tin nhắn đã gửi không chết.
- `invoice-image` trả 404 — đã dọn sạch.
- Mở app trên emulator, vào B-05 xem trước tin nhắn: hiện đúng link `.../functions/v1/invoice/2KAQN60MHAJ8`.

Độ dài tin nhắn: 199 ký tự, **2 đoạn SMS**, không có ký tự nào ngoài bảng GSM-7. Muốn xuống 1 đoạn phải có tên miền riêng — xem `docs/DECISIONS.md` Đợt 63.

---

## 15:50 — Claude · Chuyển toàn bộ SMS sang tài khoản eSMS của Dream

dungtv đưa key mới và chốt: *"nhớ dùng key sms này của dream nhé không dùng của tôi nữa"*.

**Bắt được một lỗi khi dán key**: chuỗi Secret Key gửi sang dính thêm `16:31` ở cuối — là dấu thời gian của khung chat, không phải phần của key. Thử cả 3 biến thể với API kiểm tra số dư của eSMS trước khi ghi vào secret:

| Secret thử | Kết quả |
|---|---|
| 30 ký tự (bỏ `16:31`) | `CodeResponse 100` — **đúng**, số dư 13.910đ, UserID 314094 |
| 35 ký tự (giữ nguyên cả `16:31`) | eSMS trả lỗi HTML vì dấu `:` trong đường dẫn |
| 34 ký tự (bỏ dấu hai chấm) | `Authorize Failed` |

Nếu cứ thế ghi vào secret thì toàn bộ SMS lẫn OTP chết im mà không rõ vì sao.

Đã đặt lại `ESMS_API_KEY` / `ESMS_SECRET_KEY`. Hai thứ này **dùng chung** cho cả hoá đơn lẫn OTP (qua `_shared/esms.ts`) nên đổi một lần là đổi cả hai.

**Verify bằng tiền, không bằng lời**: ghi số dư trước khi gửi (13.910đ) → gửi thật một tin hoá đơn vào số 0356123970 → số dư còn 12.080đ. **Trừ 1.830đ từ tài khoản Dream** ⇒ key mới đã thực sự có hiệu lực. eSMS nhận tin, trả `smsId 4ca54abc…`.

Tin gửi đi mang **link ảnh hoá đơn mới** (66 ký tự, 199 ký tự cả tin, 2 đoạn SMS). API tra trạng thái vẫn trả `CodeResponse 105` (chưa có dữ liệu) — như đã ghi từ 17/09, eSMS nhận tin **không** đồng nghĩa nhà mạng đã phát; chỉ máy dungtv nhận được mới kết luận được.

**Giá thực đo**: 915đ/đoạn SMS. Ghi vào `docs/SMS-HOA-DON.md` mục 6.5 mới.

---

## 16:25 — Claude · SMS hoá đơn cuối cùng cũng tới máy: chuyển đường GỬI sang brandname, và vá nốt đường gửi hàng loạt

dungtv báo không nhận được tin nào dù hàm trả về thành công và tài khoản bị trừ tiền.

**Nguyên nhân**: `_shared/esms.ts` vẫn gửi bằng `SmsType "1"` — đầu số dùng chung, nhà mạng không giao. Chính ghi chú trong file đã nói rõ điều này từ 17/09 nhưng **code thì chưa đổi**.

**Vì sao hôm qua "đã làm SmsType 2 rồi" mà hôm nay vẫn dính type 1** — tra git ra đúng trình tự:

| Commit | Giờ | Đã làm gì |
|---|---|---|
| `b2e7a71` | 17/09 11:44 | Sửa **mỗi phần ghi chú** của `esms.ts`, kết luận *"KHÔNG có đường vòng… không nhét được nội dung hoá đơn"* vào brandname mượn. Code vẫn `SmsType "1"`. |
| `689258d` | 17/09 15:11 | Sau khi dungtv bảo *"đã bảo dùng luôn cái mượn brandname mà"*, **nắn nội dung** theo mẫu Baotrixemay — nhưng chỉ đụng `invoice_message.dart`, **không đụng `esms.ts`**. |

Tức là nội dung đã nắn đúng mẫu brandname, còn chỗ gửi vẫn đi đầu số dùng chung. Ghi chú "không có đường vòng" viết lúc 11:44 vẫn nằm nguyên trong file khiến chỗ đó trông như đã chốt, không ai quay lại. `SmsType "2"` hôm qua chỉ đổi ở `send-otp-sms` — file khác hẳn, nên OTP tới còn hoá đơn thì không.

**Đã sửa `_shared/esms.ts`**: `SmsType "2"` + `Brandname "Baotrixemay"`, và `IsUnicode` từ `"1"` → `"0"`.

`IsUnicode` là lỗi thứ hai, âm thầm tốn tiền: nội dung cố tình viết **không dấu** để ở lại GSM-7 (160 ký tự/đoạn), nhưng lại khai là Unicode nên bị đẩy sang UCS-2 (70 ký tự/đoạn). Đo thật: cùng một tin, `IsUnicode 1` trừ **1.830đ**, `IsUnicode 0` trừ **1.640đ**.

**Vá luôn đường gửi HÀNG LOẠT (B-03)** — chỗ này còn hỏng nặng hơn mà chưa ai phát hiện vì chưa test: `_shared/invoice_message.ts` dựng một câu hoàn toàn khác ("BizTown Rent Manager: Hoá đơn phòng…"), hỏng hai đường cùng lúc:
1. Không khớp mẫu brandname → `CodeResult 146`, không tin nào ra khỏi hệ thống.
2. Viết **có dấu** và có dấu gạch ngang `–` (U+2013) → dính đúng cái bẫy GSM-7 đã ghi ở `docs/SMS-HOA-DON.md` mục 6.1, ngay trong chính dự án viết ra tài liệu đó.

Đã viết lại cho **giống hệt** bản gửi đơn lẻ, kèm link ảnh hoá đơn. Kiểm lại: không còn ký tự nào ngoài ASCII trong hàm dựng nội dung.

**Verify thật**: gửi qua đúng hàm `send-notification` đã sửa → dungtv **xác nhận nhận được cả 2 tin**. Một lần thử đổi dấu chấm thành chấm than bị trả `CodeResult 146 — Sai template` và **không bị trừ tiền** → mẫu mượn bắt khớp từng chữ, đừng sửa câu chữ.

Số dư tài khoản Dream: 13.910đ → 8.800đ sau các lần test hôm nay.

---

## 17:30 — Claude · Tin nhắn hoá đơn xuống 1 đoạn SMS: giảm nửa tiền, + báo kết quả sau khi bấm Gửi

### Lỗi 1 — Bấm "Gửi ngay" xong không báo gì

dungtv phát hiện khi xem tôi test: gửi xong sheet chỉ đóng lại im lìm. `showSendInvoiceSheet` **có** trả về kết quả nhưng **cả 2 nơi gọi đều vứt đi** (B-02 và màn tạo hoá đơn lẻ). Gửi lỗi thì có báo trong sheet, gửi thành công thì không — bất đối xứng. Trong khi màn gửi hàng loạt B-03 lại báo bình thường.

Đã đổi hàm trả về **câu thông báo** thay cho `bool`, hai nơi gọi hiện SnackBar: *"Đã gửi hoá đơn qua SMS tới 0356123970"* / *"…qua email tới …"*. Thêm 2 key vào cả 3 file ngôn ngữ, đối chiếu key-set khớp.

### Lỗi 2 — Tin nhắn tốn gấp đôi tiền không cần thiết

dungtv chốt: bỏ số tiền/hạn khỏi SMS, chỉ báo người thuê vào link xem hoá đơn.

Tính lại ngân sách ký tự thì thấy **phần chữ cố định của mẫu mượn đã ăn 93/160 ký tự**, chỉ còn 67 cho thông tin thật. Rút được 2 chỗ:

| | Trước | Sau |
|---|---|---|
| Tên hàm ảnh hoá đơn | `invoice` (link 66) | **`i`** (link 60) |
| Kỳ | `09/2026` (7) | `09/26` (5) |
| Ô cuối | `BizTown P101 2.748.000d han 15/09` (33) | `BT` (2) |
| **Cả tin** | **199 ký tự — 2 đoạn** | **160 ký tự — 1 đoạn** |

**Đo bằng tiền thật**: gửi qua đúng màn B-05 trong app, số dư 7.160đ → 6.340đ = **820đ**, đúng nửa của 1.640đ trước đây.

160 là con số **cố định**, không phải trung bình: tên miền Supabase luôn 32 ký tự, mã tra cứu luôn 12, kỳ luôn 5, ô cuối luôn 2. Không hoá đơn nào vọt lên 2 đoạn.

**Giữ sống cả 3 đường dẫn** `i` / `invoice` / `invoice-qr` — link đã gửi cho người thuê hôm nay trỏ vào 2 cái sau, xoá là chết link. Phần thân gom về `_shared/invoice_image.ts`, hai hàm `i` và `invoice` chỉ là file một dòng trỏ vào đó, không chép code.

**Khi mua được tên miền riêng** (link còn ~17 ký tự) thì dư ra ~43 ký tự — nhét lại số tiền và hạn thanh toán vào mà vẫn giữ 1 đoạn. Đã ghi rõ trong doc comment của `invoice_message.dart`.

### Ghi nhận một sai sót của tôi trong lúc test

Đang làm kênh SMS mà tôi lại verify bằng kênh Email cho đỡ tốn tiền — dungtv nhắc ngay, và việc đó còn làm một email hoá đơn rơi vào hộp thư của anh. Đã làm lại đúng kênh SMS. Câu báo thành công của kênh SMS thì kiểm tra tĩnh (chuỗi + tham số truyền vào) vì ảnh chụp lấy sau khi SnackBar đã tắt.

---

## 18:00 — Claude · Bump `0.1.0+12`, dựng bản build Android + iOS

Gồm toàn bộ việc hôm nay: gạch phân cách menu "⋮", đường kẻ nét chấm trong thẻ chi tiết, trạng thái khoá của nút, ảnh hoá đơn dựng bằng SVG, SMS gửi bằng brandname, tin nhắn rút xuống 1 đoạn, báo kết quả sau khi bấm Gửi.

**Kiểm tra dữ liệu trước khi build**: quét toàn bộ 73 người thuê — **không còn ai** mang số `0356123970` hay email `kianh247@gmail.com` của dungtv. Mọi hồ sơ đã trả về số/email gốc sau các lần test.

Android: `app-release.apk` 61.5MB. iOS: biên dịch + ký thành công, `Generated.xcconfig` mang đúng cấu hình Supabase, `CFBundleVersion 12`.

---

## 19:10 — Claude · Bổ sung 2 trạng thái chữ phụ còn thiếu ở B-01 so với thiết kế

dungtv đối chiếu màn Hoá đơn với frame B-01 và hỏi đã đủ các trạng thái chưa. Rà lại thì **dải chip đủ 5 trạng thái, màu khớp**, nhưng **chữ phụ thiếu 2 câu**:

| Thiết kế | Trước | Nay |
|---|---|---|
| `collected 02/09` | có | có |
| `period 08 is 6 days late` | có | có |
| `sent 05/09 **via Zalo**` | chỉ có ngày, **thiếu kênh** | **"đã gửi 18/09 qua SMS"** |
| `no reading for this period yet` | **không có** | **"chưa ghi chỉ số cho kỳ này"** |
| (app thêm) chưa tạo hoá đơn | có | có |

**Kênh gửi trước đây không lưu ở đâu cả.** Bảng `tb_invoice` chỉ có `sent_at`. Thêm migration `20260918100000_invoice_sent_channel.sql` (cột `sent_channel text`), ghi vào ở **cả 3 đường gửi**: SMS đơn lẻ, Email, và gửi hàng loạt phía backend. Hoá đơn gửi trước hôm nay có `sent_channel` rỗng nên app **lùi về câu chỉ có ngày**, không hiện "qua null".

**Thứ tự ưu tiên của chữ phụ có chủ ý**: thiếu chỉ số điện/nước báo **trước** trạng thái gửi, vì hoá đơn thiếu chỉ số là hoá đơn tính sai tiền. Đúng như Figma vẽ — hàng P.101 mang nhãn "Sent" nhưng chữ phụ vẫn là "no reading for this period yet".

**Bẫy gặp phải khi đẩy migration**: 9 migration từ 15/09 đã chạy trên database nhưng **chưa ghi vào bảng lịch sử**, nên `supabase db push` đòi chạy lại từ đầu và báo `relation "tb_notification" already exists`. Sửa bằng `supabase migration repair --status applied` cho 9 bản đó rồi push lại — chỉ migration mới được áp dụng. Lần sau gặp lỗi "already exists" thì kiểm tra `supabase migration list` trước, đừng sửa file migration.

**Verify thật, đủ 4 điểm**: "chưa ghi chỉ số" hiện đúng ở hàng thiếu chỉ số và không hiện ở hàng đủ; gửi SMS thật từ màn B-05 → DB ghi `sent_channel = "sms"`; SnackBar hiện "Đã gửi hoá đơn qua SMS tới 0356123970"; danh sách hiện "đã gửi 18/09 qua SMS". Dữ liệu test đã trả nguyên trạng, quét lại toàn bộ 73 người thuê không còn ai mang số/email của dungtv.

**Ghi nhận một điểm chưa khớp, chưa sửa**: chip dùng hạn thanh toán **tính từ điều khoản hợp đồng**, còn nhãn/chữ phụ dùng `due_date` **lưu trên hoá đơn**. Bình thường hai cái trùng nhau; nếu sau này cho sửa hạn riêng trên từng hoá đơn thì chip và nhãn sẽ lệch nhau. Phát hiện do lúc test tự tay đổi `due_date`.

---

## 19:40 — Claude · Icon OS mang chữ BizTown, tên app rút còn "Rent Manager"

Khách hàng Hàn Quốc báo qua dungtv: cài lên iPhone thì tên "BizTown Rent Manager" quá dài, đề nghị đưa tên thương hiệu vào icon và để tên app là "Rent Manager".

**Đây là đảo lại quyết định của chính sáng nay** (mục 08:12): lúc đó chốt giữ icon OS không chữ vì "chữ không đọc được ở size nhỏ". Kiểm chứng lại bằng cách kết xuất icon ở đúng cỡ hiển thị thật — 180×180 trên iPhone, 192×192 trên Android — thì **chữ "BizTown" đọc rõ**. Lập luận cũ dựa trên phỏng đoán, không phải trên ảnh thật.

**Đã làm**: thay `assets/icon/app_icon.png` bằng `biztown-rent-icon-wordmark-alt.png`, chạy lại `flutter_launcher_icons` (sinh 26 file icon cho iOS + 5 mật độ Android); đổi `CFBundleDisplayName` và `android:label` thành `Rent Manager`.

**Verify**: `aapt2 dump badging` trên APK thật trả `application-label: 'Rent Manager'`; mở icon ở cỡ thật bằng mắt, chữ đọc được ở cả hai nền tảng.

Bump `0.1.0+12` → `0.1.0+13`.

---

## 20:05 — Claude · Rà logo ở tài liệu gửi khách: chưa đồng bộ, dungtv chốt tạm chấp nhận

dungtv hỏi: đã đổi icon và tên app rồi thì hoá đơn/email gửi đi đã sửa cho đồng bộ chưa?

**Trả lời: chưa.** App dùng bộ logo mới, còn **ảnh hoá đơn và email gửi người thuê vẫn mang lockup cũ** — tức người thuê nhận hoá đơn mang logo khác với logo chủ trọ thấy trong app.

**Tài liệu ghi sai, đã sửa**: `docs/DESIGN-SYSTEMS.md` ghi 2 file lockup cũ *"không còn dùng trong code"*, nhưng lần rà đó chỉ quét `src/lib/` mà bỏ qua `supabase/functions/`. Thực tế `biztown-rent-manager-lockup-on-navy.svg` vẫn được nhúng vào hàm dựng ảnh hoá đơn.

**Vướng kỹ thuật khi định thay**: logo mới là **ô vuông bo góc nền navy `#23305E`** — trùng **đúng** màu nền header ảnh hoá đơn. Đo pixel xác nhận trùng khít, không phải ước lượng. Dán vào là ô vuông tan vào nền.

**dungtv chốt phương án 3: tạm giữ lockup cũ**, chấp nhận app và hoá đơn khác logo nhau.

**Đã cắm cảnh báo ngay trong code** ở `_shared/invoice_image_logo.ts` và `send-invoice-email/index.ts`, vì phiên sau chỉ đọc tài liệu sẽ tưởng bộ logo cũ đã bỏ hẳn và "sửa giúp" nhầm. Không đổi một dòng code chạy nào.

**Còn treo**: xin mr Han bản logo nền trong suốt; và chốt xem header hoá đơn có giữ dòng "RENT MANAGER" không — quy tắc thương hiệu mới nói bỏ, mockup của Hường (đã duyệt) nói giữ.
