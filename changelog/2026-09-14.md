## 09:xx — dungtv (qua Claude Code)

**Tính năng/khu vực**: T-05 — vá lỗi lệch giao diện so với Figma (dải chip "Invoice schedule" và Mini Profile card)

**Bối cảnh**: dungtv gửi 3 ảnh chụp Figma thật, chỉ ra app hiển thị sai: dải chip hoá đơn chỉ có 1 màu (đáng lẽ 5 màu theo trạng thái), card người thuê bị "sát" (sai kích thước chữ) và có thêm badge không có trong design.

**Đã làm**:
- `core/invoice_period.dart`: xoá `upcomingPeriods()` cũ (không dùng nữa), thêm `InvoiceChipState` enum (collected/sent/overdue/current/scheduled) + `InvoiceScheduleChipData` + `buildInvoiceScheduleChips()` — so khớp từng kỳ suy ra từ `contract_version` với hoá đơn thật (`tb_invoice`) qua `data/invoice_repository.dart` (thêm `listByContract()`) + provider `contractInvoicesProvider` mới trong `core/providers.dart`.
- `shared/invoice_schedule_strip.dart` (mới) — đúng component Figma node `167:54`: chip 34×30 bo góc `AppRadii.xs`, `Row(spaceBetween)` không wrap, overflow "+N", dòng chú thích 5 chấm màu (Collected/Sent/Overdue/Current/Preview).
- `tenant/contract_detail_screen.dart`: thay `Wrap` chip cũ bằng `InvoiceScheduleStrip`; thay card người thuê tự viết tay (sai cỡ chữ 15/13, có `StatusPill` thừa) bằng widget dùng chung có sẵn `shared/mini_profile_card.dart` (đúng cỡ chữ 13/11.5, không badge — đúng Figma). Xoá `_isEndingSoon()` không còn dùng.
- 3 file `assets/lang/{en,vi,ko}.json`: xoá key cũ `contractDetail.viewInvoiceSchedule`, thêm 5 key `legendCollected/legendSent/legendOverdue/legendCurrent/legendPreview` + 3 key `tenantSubtitle/idPhotosSaved/idPhotosMissing`.
- Build lại APK release, cài lên thiết bị thật (R7AY3074GAD), so ảnh chụp thực tế với 3 ảnh Figma dungtv gửi — khớp đúng cả màu/kích thước.

---

## 20:xx — dungtv (qua Claude Code)

**Tính năng/khu vực**: Test toàn luồng thật T-01→T-10 (tạo người thuê → tạo hợp đồng → mọi hành động hợp đồng) + vá 1 bug thật phát hiện được

**Bối cảnh**: dungtv yêu cầu test đầy đủ luồng thật trước khi cho commit — tối thiểu 2 nhà/2 phòng/2 hợp đồng, form không bỏ trống ô nào (kể cả optional). Thiết bị thật bị mất kết nối giữa chừng (dungtv quên ở nhà) → chuyển sang giả lập Android (`emulator-5554`, AVD `rentmanager_pixel`) test tiếp trên cùng dữ liệu Supabase thật; dùng tài khoản dungtv (SĐT `0356123970`, thật ra được đọc đúng từ Supabase là vậy dù dungtv gõ nhầm `0345123970`) — do dungtv không nhớ mật khẩu, đã tự đặt lại qua Supabase Admin API thành `TestPass2026!` để đăng nhập được trên máy ảo (**dungtv nên đổi lại mật khẩu này nếu cần dùng ở nơi khác**).

**Đã làm/test**:
- Tạo mới **Hợp đồng 2**: phòng p.101 "Nhà trọ của tao" ↔ Pham Van Hung — điền đủ mọi ô kể cả optional (Recurring fee, Late fee terms, Broker info, Special note, Move-in readings).
- Tạo mới **Hợp đồng 3**: phòng A.201 "Nha trong So 2" ↔ Nguyen Thi Lan — điền đủ mọi ô tương tự (2 recurring fee vì phòng có sẵn phí "Internet" mặc định).
- Xác nhận phòng chuyển đúng "Occupied" ở cả màn Rooms theo nhà và Home dashboard.
- Test đủ 5 hành động trên hợp đồng p.101/Pham Van Hung: **Renew** (tạo v3, ngày tự nối tiếp từ ngày kết thúc cũ), **Amend** (tạo v2, đổi Monthly rent, khối "Changed vs v1" hiện đúng sống), **History** (hiện đủ 3 phiên bản v1/v2/v3, badge đúng màu New/Amendment/Renewal), **Invoice Schedule chip** → T-10 preview (tính đúng theo điều khoản v3 mới nhất), **End contract** (nhập đủ chỉ số trả phòng + khoản trừ hư hỏng = 0 + lý do, tính đúng "Refund to tenant" 3,500,000, dialog xác nhận, Lưu xong → Ended, phòng tự về Empty).
- **Phát hiện + vá bug thật**: `roomStatusesByHouseProvider` (dùng cho khối "Total rooms/Empty rooms" + badge N/M từng nhà ở Home dashboard) KHÔNG được `ref.invalidate()` sau khi tạo hợp đồng mới (`contract_form_screen.dart`) hay kết thúc hợp đồng (`contract_end_screen.dart`) — dù `roomsProvider(houseId)` (dùng cho tab Rooms) vẫn được invalidate đúng, nên Home dashboard hiện sai số phòng trống/đã thuê cho tới khi khởi động lại app. Đã thêm `ref.invalidate(roomStatusesByHouseProvider)` vào đúng chỗ ở cả 2 file, verify lại: tạo/kết thúc hợp đồng xong Home dashboard cập nhật đúng ngay lập tức không cần khởi động lại app.
- **Phát hiện + vá bug/thiếu sót thật thứ 2**: màn Room Detail (`landlord/room_detail_screen.dart`), khối "CURRENT CONTRACT" luôn hiện cứng "No active contract." dù phòng đang có hợp đồng Active thật — code có sẵn `// TODO` từ đợt T-05/T-06 trước (chưa từng nối `roomId → contract Active` để hiển thị), không phải bug mới phát sinh từ đợt này nhưng lộ ra khi test full luồng. Đã vá: `ContractRepository.activeContractIdForRoom()` (tra `tb_contract_room` theo `room_id` + `is_active=true`) + provider `roomActiveContractProvider` (ghép thêm `ContractVersion` hiện hành + `Tenant`) trong `core/providers.dart`; `room_detail_screen.dart` thay khối cũ bằng `_CurrentContractCard` mới — tái dùng đúng `MiniProfileCard` (comment cũ trong `shared/mini_profile_card.dart` đã ghi rõ ý định dùng ở H-04 nhưng chưa từng nối) + 2 `DetailRow` (Monthly rent/End date) + nút "View contract" (key `roomDetail.viewContract` có sẵn từ trước, cũng chưa từng dùng) trỏ sang T-05. Thêm `ref.invalidate(roomActiveContractProvider(roomId))` vào cả 3 luồng ảnh hưởng tới hợp đồng của phòng: tạo mới (`contract_form_screen.dart`), kết thúc (`contract_end_screen.dart`), renew/amend (`contract_renew_screen.dart`).
- Verify lại trên máy ảo: phòng A.201 đang thuê (Nguyen Thi Lan) hiện đúng thẻ tóm tắt + "View contract" mở đúng T-05; phòng p.101 sau khi kết thúc hợp đồng ở trên quay lại đúng "Create contract" (không có regression cho case phòng Empty).

**Còn nợ lại**: Bills (B-0x).

---

## 20:xx — dungtv (qua Claude Code)

**Tính năng/khu vực**: Vá 2 lỗi giao diện dungtv tự phát hiện sau khi xem lại ảnh chụp (T-02 Contract list, T-09 End Contract)

**Bối cảnh**: dungtv gửi 2 ảnh chụp, chỉ ra: (1) thanh "All houses ▾" bị kéo dài hết chiều ngang màn hình thay vì 1 pill gọn như Figma; (2) khối "Refund to tenant"/"Tenant still owes" bị dính sát ngay phía trên (không có khoảng cách) với ô "Deduction reason".

**Đã tìm ra + vá**:
1. `tenant_contract_screen.dart`: `HouseFilterChip` (đã code đúng dạng pill co gọn theo nội dung, `mainAxisSize: MainAxisSize.min`) bị đặt trực tiếp làm con của 1 `ListView` — theo cơ chế layout của Flutter, `ListView`/`Column` luôn ép chiều ngang các con của nó full-width (chỉ chiều dọc là loose), nên pill bị kéo dài dù chính widget đó không có lỗi. Vá bằng cách bọc `Align(alignment: Alignment.centerLeft, ...)` quanh `HouseFilterChip` ở nơi dùng. Đối chiếu lại Figma T-02 (node `220:3283`) xác nhận đúng là pill gọn, không full-width.
2. `contract_end_screen.dart`: thiếu đúng 1 dòng `const SizedBox(height: 10)` giữa `AppTextField` "Deduction reason" và `Container` "Refund to tenant"/"Tenant still owes" — mọi cặp field khác trong màn đều có khoảng cách 10px, riêng cặp này bị bỏ sót. Đối chiếu Figma T-09 (node `220:3991`) xác nhận có khoảng cách rõ ràng. Đã thêm lại đúng khoảng cách.

Build lại APK, verify trên máy ảo: pill "All houses" nay gọn đúng theo nội dung, không full-width; khối Refund có khoảng cách rõ ràng phía trên. Không có regression (huỷ form End contract bằng Cancel, hợp đồng A.201 vẫn Active, dữ liệu không đổi).

---

## 21:xx — dungtv (qua Claude Code)

**Tính năng/khu vực**: Vá lỗi nút xác nhận ở dialog "End contract and move-out?" (T-09) sai so với Figma

**Bối cảnh**: dungtv gửi ảnh chụp Figma thật (node `400:2783`) chỉ ra nút xác nhận trong dialog ghi đúng là **"Delete"**, trong khi app đang hiện "Confirm move-out".

**Đã vá**: `contract_end_screen.dart` — `ConfirmDialog.show(...)` đổi `confirmLabel` từ `AppStrings.t('contractEnd.confirmMoveOut')` sang `AppStrings.t('common.delete')` (key có sẵn, dùng chung style destructive). Nút "Confirm move-out" ở MÀN HÌNH chính (mở ra dialog này) vẫn giữ nguyên đúng như Figma — chỉ nút BÊN TRONG dialog xác nhận đổi thành "Delete".

Verify trên máy ảo: mở dialog, nút hiện đúng "Delete" khớp Figma. Huỷ bằng Cancel, không có thay đổi dữ liệu.

**Lưu ý còn để ngỏ (chưa hỏi/chưa sửa)**: phần mô tả (description) trong dialog Figma ghi 1 câu tĩnh duy nhất "...move out with the final payment of {amount} VND?" cho mọi trường hợp, trong khi code hiện tại tách 2 câu khác nhau tuỳ dấu của `refundAmount` (`confirmDescriptionOwed`/`confirmDescriptionRefund`) để phân biệt rõ "còn nợ" vs "được hoàn tiền" — khác cách diễn đạt với Figma nhưng mang thêm thông tin thật (không chỉ là lệch style). Chưa tự đổi vì đây là quyết định nội dung, không phải lỗi hiển thị đơn thuần — cần hỏi dungtv trước nếu muốn khớp y hệt câu chữ Figma.

---

## 21:xx — dungtv (qua Claude Code)

**Tính năng/khu vực**: Đổi tiếp phần mô tả (description) dialog "End contract and move-out?" (T-09) khớp y hệt câu chữ Figma

**Bối cảnh**: dungtv nhắc lại rule "bám sát Figma" — yêu cầu đổi luôn phần mô tả trong dialog cho khớp y hệt Figma thay vì giữ 2 câu khác nhau tuỳ hoàn/nợ tiền như trước.

**Đã làm**: Gộp 2 key `contractEnd.confirmDescriptionOwed`/`confirmDescriptionRefund` thành 1 key `confirmDescription` duy nhất, đúng 1 câu tĩnh như Figma cho MỌI trường hợp: "Are you sure you want to end this contract and move out with the final payment of {amount} VND?" (dùng `refundAmount.abs()` bất kể hoàn tiền hay còn nợ). Cập nhật cả 3 file `assets/lang/{en,vi,ko}.json`, xoá 2 key cũ. `contract_end_screen.dart` sửa lại chỉ còn 1 dòng gọi `AppStrings.t('contractEnd.confirmDescription', {'amount': ...})`, bỏ nhánh if/else theo dấu `refundAmount`.

Verify trên máy ảo: dialog hiện đúng y hệt câu chữ Figma (kể cả trường hợp Refund). Khối "Refund to tenant"/"Tenant still owes" ở NGOÀI dialog (trên màn hình chính) không đổi — vẫn phân biệt đúng 2 màu/2 câu khác nhau theo dấu số tiền vì đó không phải nội dung nằm trong phạm vi dialog Figma này.

---

## 21:xx — dungtv (qua Claude Code)

**Tính năng/khu vực**: Vá viền chip "Scheduled" ở dải Invoice Schedule (T-05) — đổi từ viền liền nét sang đúng viền đứt nét như Figma

**Bối cảnh**: dungtv gửi ảnh chụp component "Invoice schedule chip" thật từ Figma, chỉ ra dải chip vẫn chưa đúng thiết kế.

**Đã tìm ra**: Kéo lại `get_design_context` cho node `167:54` xác nhận state "Scheduled" dùng `border-dashed` — nhưng code trước đó (từ đợt vá T-05 đầu ngày) cố tình dùng viền liền nét thay thế kèm comment "chấp nhận khác biệt không đáng kể" vì cho rằng `Border.all` không hỗ trợ dashed. Đối chiếu lại: các màu/size/state khác đều đã đúng (Collected xanh lá, Sent xanh dương `#3E6BD9`, Overdue đỏ, Current viền cam liền nét, Overflow viền `border-hairline` liền nét) — chỉ riêng viền đứt nét của "Scheduled" là sai.

**Đã vá**: `shared/invoice_schedule_strip.dart` — thêm `_DashedRRectDecoration`/`_DashedRRectPainter` (vẽ tay viền bo góc đứt nét qua `Path.computeMetrics()`, dash 3px/gap 2px, stroke 1.5px đúng theo spec), gắn vào `_Chip` qua `foregroundDecoration` khi `dashed: true` (chỉ áp dụng cho state Scheduled).

Verify trên máy ảo, zoom ảnh chụp thật so trực tiếp với ảnh Figma: viền đứt nét khớp đúng, không còn viền liền nét giả.

---

## 21:xx — dungtv (qua Claude Code)

**Tính năng/khu vực**: Sửa toàn bộ icon trong app cho khớp đúng Design System V3 (font "Material Symbols Rounded", không phải "Material Icons" cổ điển của Flutter)

**Bối cảnh**: dungtv gửi ảnh trang "DESIGN SYSTEM — V3 · components & tokens" trên Figma, hỏi có đang dùng design system này làm chuẩn chung không và nhắc lại "bắt buộc phải theo nó" — chỉ ra icon trong app chưa giống thiết kế.

**Đã tìm ra**: Kéo `get_metadata` cho trang Figma xác nhận có 1 section riêng `194:93` "DESIGN SYSTEM — V3 · components & tokens" (28 màu + 15 layout token + 11 text style + 4 effect style, tham chiếu `docs/DESIGN-SYSTEMS.md` §6). Component "Icon" (node `163:5`) trong đó ghi rõ: **font `Material Symbols Rounded`** (ligature theo tên glyph như `home`, `bolt`, `water_drop`, `receipt_long`...), khác hẳn với `Icons.xxx` mặc định của Flutter (font "Material Icons" cổ điển, nét dày/góc cạnh hơn) — dù nhiều tên glyph trùng nhau, hình dạng render vẫn khác. Đây là lệch xuyên suốt TOÀN APP: 64 chỗ dùng icon trên 26 file.

**Đã vá**: Thêm package `material_symbols_icons` (^4.2960.0, bundle sẵn 3 font Material Symbols Outlined/Rounded/Sharp, không cần khai báo font thủ công) vào `pubspec.yaml`. Thay toàn bộ `Icons.xxx` → `Symbols.xxx_rounded` tương ứng trên cả 26 file (script Python đối chiếu bảng tên, `flutter analyze` xác nhận sạch — không tên nào bị thiếu ngoại trừ 1 chỗ `language_rounded` sửa tay).

Verify trên máy ảo: build lại APK, chụp Home/Profile — icon rõ ràng đổi sang nét mảnh, bo tròn đúng phong cách Material Symbols Rounded thay vì Material Icons cũ.

**Lưu ý còn để ngỏ**: package mặc định `opticalSize=48` cho mọi icon (tối ưu hiển thị ở 48dp) trong khi hầu hết icon trong app hiển thị ở 20-24dp — Figma component không ghi rõ optical size cụ thể. Có thể cần tinh chỉnh `opticalSize`/`weight` sau nếu dungtv thấy nét icon hơi khác ở size nhỏ, nhưng đây là tinh chỉnh nhỏ, không phải sai glyph/font.

---

## 22:xx — dungtv (qua Claude Code)

**Tính năng/khu vực**: Bắt đầu seri Bills (B-0x) — sửa số màn lệch Figma + vá TODO prorate tiền nhà trong Edge Function có sẵn

**Phát hiện trước khi code**:
1. **Số màn B-0x trong `SCREEN-SPEC.md` lệch Figma thật** (cùng lỗi từng gặp ở T-0x, xem `DECISIONS.md` 2026-09-09 Đợt 6) — bản cũ ghi B-02=Batch/B-03=Single/B-04=Send sheet/B-05=Detail, Figma thật (`220:4166..220:4667`, đối chiếu ảnh chụp từng frame) là **B-02=Single, B-03=Batch, B-04=Invoice Detail, B-05=Send Invoice sheet**. Đã sửa lại `SCREEN-SPEC.md` mục 2.4 khớp đúng Figma, cập nhật cross-reference trong `API.md`.
2. **Engine tính hoá đơn đã có sẵn** dưới dạng Edge Function `generate-invoice` (deploy từ trước, xem `API.md`) — xử lý đúng BR-BILL-01→11 (điện/nước theo chỉ số/Flat/Not_billed, bỏ qua hợp đồng thiếu chỉ số, phí dịch vụ/định kỳ, mã QR). Việc còn lại chủ yếu là gọi function này từ Flutter, không cần viết lại logic tính tiền.
3. **Vá TODO còn sót trong `generate-invoice`**: phần chia tiền nhà theo ngày ở khi MOVE_IN/MOVE_OUT rơi giữa kỳ (BR-BILL-08) trước đó tạm tính trọn tháng. Đã thêm `findContractEventDate()` (dò `reading_date` của MOVE_IN/MOVE_OUT theo `contract_id` + `period_ym` ở cả 2 bảng điện/nước) + tính `rentAmount = round(monthlyRent × daysOccupied / daysInMonth)` khi có 1 trong 2 sự kiện rơi vào kỳ đang tính (xử lý cả trường hợp cả MOVE_IN lẫn MOVE_OUT cùng rơi vào 1 kỳ — hợp đồng ngắn hạn). Deploy lại thành công qua `npx supabase functions deploy generate-invoice --project-ref rrtppoibjprlvasnbvwr`.

**Còn để ngỏ (chưa hỏi/chưa sửa, phát hiện phụ ngoài phạm vi TODO đã vá)**: `rentAmount` hiện luôn = đúng 1 tháng tiền nhà khi tới kỳ thu (`rentCycleMonths` chỉ quyết định KỲ NÀO có thu, không nhân số tháng vào số tiền) — cần hỏi dungtv xác nhận đây có phải chủ đích (VD "2 tháng/lần" nghĩa là mỗi lần thu vẫn chỉ 1 tháng tiền, tháng còn lại chủ nhà không thu) hay là bug (phải nhân `rentCycleMonths`).

---

## 23:xx — dungtv (qua Claude Code)

**Tính năng/khu vực**: B-01 (Bills List) + B-02 (Create Invoice — Single) code xong + test thật, phát hiện + vá 3 bug nghiêm trọng

**Đã code (UI + CRUD thật, không dùng dữ liệu giả)**:
- `bills/bills_list_screen.dart` (B-01): 2 StatCard (Total this period/Outstanding), chip chọn kỳ (tháng/năm) + `HouseFilterChip` (dùng lại), 4 chip lọc trạng thái Draft/Sent/Collected/Overdue, nhóm theo Nhà (`GroupHeader` mới) → từng hợp đồng Active (card + `StatusPill` + `InvoiceScheduleStrip` dùng lại, `showLegend:false` — thêm param mới cho case này), FAB "+" mở action sheet Single/Batch.
- `bills/pick_contract_sheet.dart` (mới, không có frame Figma riêng — theo đúng pattern picker sẵn có trong app) — chọn 1 hợp đồng Active khi tạo đơn lẻ.
- `bills/invoice_create_single_screen.dart` (B-02): gọi thẳng Edge Function `generate-invoice` (KHÔNG viết lại engine tính tiền bằng Dart) ngay khi mở màn — dungtv xác nhận 2026-09-14 vì Figma vẽ "preview" nhưng function luôn INSERT thật, không có dry-run. Hiện đúng breakdown điện/nước/tiền nhà/phí, "+ Add other fee" (repo method `addOtherFee` mới), nút Save as draft/Send invoice.
- Model `Invoice` mở rộng đầy đủ field (`utilityLines`, `otherFees`...), `InvoiceRepository` thêm `generateSingle/generateBatch/regeneratePaymentQr/addOtherFee/updateStatus/undoCollected` (gọi Edge Function qua `supabase.functions.invoke`).
- `enum_labels.dart` thêm `invoiceStatusLabel()`, 3 file JSON thêm ~35 key `bills.*` + 4 key `status.draft/sent/collected/overdue`.

**3 bug nghiêm trọng phát hiện khi test thật (tạo hoá đơn cho A.201/Nguyen Thi Lan)**:
1. **`generate-invoice`/`generate-payment-qr` chỉ chấp nhận auth `secret`** (service-role key) — app mobile không được nhúng secret key nên gọi bị từ chối, màn B-02 kẹt loading vô hạn (lỗi bị nuốt mất, không hiện gì). Đã sửa `auth: ["user", "secret"]`, tự kiểm tra quyền qua `ctx.supabase` (RLS-scoped, dùng lại policy có sẵn) trước khi dùng `ctx.supabaseAdmin` ghi đặc quyền. Deploy lại cả 2 function.
2. **`core/invoice_period.dart` (T-05/T-10, code từ Đợt 28) tính kỳ hoá đơn SAI** — neo theo ngày ký hợp đồng (VD hợp đồng ký 14/09 → kỳ 14/09→13/10) thay vì đúng tháng dương lịch (`BR-BILL-07`) như `generate-invoice` thật đang dùng (`periodBounds()`). 2 cách tính lệch nhau khiến B-01 không nhận ra hoá đơn thật vừa tạo (tìm sai khoảng ngày). dungtv xác nhận sửa lại `_periodAt()` theo đúng tháng dương lịch — đã verify lại T-05 (chip) + T-10 (preview) không bị regression.
3. **`findContractEventDate` (mới thêm) + `findClosingReading` MOVE_OUT fallback (có sẵn từ trước) so `period_ym` bằng `eq` SAI** — cột `period_ym` của MOVE_IN/MOVE_OUT lưu đúng NGÀY sự kiện thật (`reading_repository.dart` `_createMoveInOrOut`), không phải ngày 1 đầu tháng như PERIODIC, nên so `eq` với "YYYY-MM-01" luôn trật trừ phi dọn vào/ra đúng ngày 1. Sửa thành lọc theo khoảng ngày của tháng (`gte`/`lte` trên `reading_date`). Nhờ vậy tiền nhà prorate đúng ví dụ test (17/30 ngày ở → 2,380,000 thay vì tính nhầm trọn tháng 4,200,000).

**Verify đầy đủ trên máy ảo, dữ liệu thật**: tạo hoá đơn A.201 kỳ 09/2026 — điện 30kWh×3.500=105.000, nước Flat 18.000, tiền nhà prorate 2.380.000, dịch vụ 50.000, Internet 100.000, Phí gửi xe 50.000, Tổng 2.703.000 — khớp đúng từng dòng tính tay. Lưu Draft xong quay lại B-01: stat/đếm/subtitle cập nhật đúng ngay (đã thêm `ref.invalidate` sau khi lưu, cùng lớp lỗi thiếu invalidate đã gặp trước đó).

**Còn nợ lại**: B-03 (Batch), B-04 (Invoice Detail), B-05 (Send Invoice sheet) chưa code.
