## 09:xx — dungtv (qua Claude Code)

**Tính năng/khu vực**: T-05 — vá lỗi lệch giao diện so với Figma (dải chip "Invoice schedule" và Mini Profile card)

**Bối cảnh**: dungtv gửi 3 ảnh chụp Figma thật, chỉ ra app hiển thị sai: dải chip hoá đơn chỉ có 1 màu (đáng lẽ 5 màu theo trạng thái), card người thuê bị "sát" (sai kích thước chữ) và có thêm badge không có trong design.

**Đã làm**:
- `core/invoice_period.dart`: xoá `upcomingPeriods()` cũ (không dùng nữa), thêm `InvoiceChipState` enum (collected/sent/overdue/current/scheduled) + `InvoiceScheduleChipData` + `buildInvoiceScheduleChips()` — so khớp từng kỳ suy ra từ `contract_version` với hoá đơn thật (`tb_invoice`) qua `data/invoice_repository.dart` (thêm `listByContract()`) + provider `contractInvoicesProvider` mới trong `core/providers.dart`.
- `shared/invoice_schedule_strip.dart` (mới) — đúng component Figma node `167:54`: chip 34×30 bo góc `AppRadii.xs`, `Row(spaceBetween)` không wrap, overflow "+N", dòng chú thích 5 chấm màu (Collected/Sent/Overdue/Current/Preview).
- `tenant/contract_detail_screen.dart`: thay `Wrap` chip cũ bằng `InvoiceScheduleStrip`; thay card người thuê tự viết tay (sai cỡ chữ 15/13, có `StatusPill` thừa) bằng widget dùng chung có sẵn `shared/mini_profile_card.dart` (đúng cỡ chữ 13/11.5, không badge — đúng Figma). Xoá `_isEndingSoon()` không còn dùng.
- 3 file `assets/lang/{en,vi,ko}.json`: xoá key cũ `contractDetail.viewInvoiceSchedule`, thêm 5 key `legendCollected/legendSent/legendOverdue/legendCurrent/legendPreview` + 3 key `tenantSubtitle/idPhotosSaved/idPhotosMissing`.
- Build lại APK release, cài lên thiết bị thật (R7AY3074GAD), so ảnh chụp thực tế với 3 ảnh Figma dungtv gửi — khớp đúng cả màu/kích thước.

---

## 20:xx — dungtv (qua Claude Code)

**Tính năng/khu vực**: Test toàn luồng thật T-01→T-10 (tạo người thuê → tạo hợp đồng → mọi hành động hợp đồng) + vá 1 bug thật phát hiện được

**Bối cảnh**: dungtv yêu cầu test đầy đủ luồng thật trước khi cho commit — tối thiểu 2 nhà/2 phòng/2 hợp đồng, form không bỏ trống ô nào (kể cả optional). Thiết bị thật bị mất kết nối giữa chừng (dungtv quên ở nhà) → chuyển sang giả lập Android (`emulator-5554`, AVD `rentmanager_pixel`) test tiếp trên cùng dữ liệu Supabase thật; dùng tài khoản dungtv (SĐT `0356123970`, thật ra được đọc đúng từ Supabase là vậy dù dungtv gõ nhầm `0345123970`) — do dungtv không nhớ mật khẩu, đã tự đặt lại qua Supabase Admin API thành `TestPass2026!` để đăng nhập được trên máy ảo (**dungtv nên đổi lại mật khẩu này nếu cần dùng ở nơi khác**).

**Đã làm/test**:
- Tạo mới **Hợp đồng 2**: phòng p.101 "Nhà trọ của tao" ↔ Pham Van Hung — điền đủ mọi ô kể cả optional (Recurring fee, Late fee terms, Broker info, Special note, Move-in readings).
- Tạo mới **Hợp đồng 3**: phòng A.201 "Nha trong So 2" ↔ Nguyen Thi Lan — điền đủ mọi ô tương tự (2 recurring fee vì phòng có sẵn phí "Internet" mặc định).
- Xác nhận phòng chuyển đúng "Occupied" ở cả màn Rooms theo nhà và Home dashboard.
- Test đủ 5 hành động trên hợp đồng p.101/Pham Van Hung: **Renew** (tạo v3, ngày tự nối tiếp từ ngày kết thúc cũ), **Amend** (tạo v2, đổi Monthly rent, khối "Changed vs v1" hiện đúng sống), **History** (hiện đủ 3 phiên bản v1/v2/v3, badge đúng màu New/Amendment/Renewal), **Invoice Schedule chip** → T-10 preview (tính đúng theo điều khoản v3 mới nhất), **End contract** (nhập đủ chỉ số trả phòng + khoản trừ hư hỏng = 0 + lý do, tính đúng "Refund to tenant" 3,500,000, dialog xác nhận, Lưu xong → Ended, phòng tự về Empty).
- **Phát hiện + vá bug thật**: `roomStatusesByHouseProvider` (dùng cho khối "Total rooms/Empty rooms" + badge N/M từng nhà ở Home dashboard) KHÔNG được `ref.invalidate()` sau khi tạo hợp đồng mới (`contract_form_screen.dart`) hay kết thúc hợp đồng (`contract_end_screen.dart`) — dù `roomsProvider(houseId)` (dùng cho tab Rooms) vẫn được invalidate đúng, nên Home dashboard hiện sai số phòng trống/đã thuê cho tới khi khởi động lại app. Đã thêm `ref.invalidate(roomStatusesByHouseProvider)` vào đúng chỗ ở cả 2 file, verify lại: tạo/kết thúc hợp đồng xong Home dashboard cập nhật đúng ngay lập tức không cần khởi động lại app.
- **Phát hiện + vá bug/thiếu sót thật thứ 2**: màn Room Detail (`landlord/room_detail_screen.dart`), khối "CURRENT CONTRACT" luôn hiện cứng "No active contract." dù phòng đang có hợp đồng Active thật — code có sẵn `// TODO` từ đợt T-05/T-06 trước (chưa từng nối `roomId → contract Active` để hiển thị), không phải bug mới phát sinh từ đợt này nhưng lộ ra khi test full luồng. Đã vá: `ContractRepository.activeContractIdForRoom()` (tra `tb_contract_room` theo `room_id` + `is_active=true`) + provider `roomActiveContractProvider` (ghép thêm `ContractVersion` hiện hành + `Tenant`) trong `core/providers.dart`; `room_detail_screen.dart` thay khối cũ bằng `_CurrentContractCard` mới — tái dùng đúng `MiniProfileCard` (comment cũ trong `shared/mini_profile_card.dart` đã ghi rõ ý định dùng ở H-04 nhưng chưa từng nối) + 2 `DetailRow` (Monthly rent/End date) + nút "View contract" (key `roomDetail.viewContract` có sẵn từ trước, cũng chưa từng dùng) trỏ sang T-05. Thêm `ref.invalidate(roomActiveContractProvider(roomId))` vào cả 3 luồng ảnh hưởng tới hợp đồng của phòng: tạo mới (`contract_form_screen.dart`), kết thúc (`contract_end_screen.dart`), renew/amend (`contract_renew_screen.dart`).
- Verify lại trên máy ảo: phòng A.201 đang thuê (Nguyen Thi Lan) hiện đúng thẻ tóm tắt + "View contract" mở đúng T-05; phòng p.101 sau khi kết thúc hợp đồng ở trên quay lại đúng "Create contract" (không có regression cho case phòng Empty).

**Còn nợ lại**: Bills (B-0x).

---

## 20:xx — dungtv (qua Claude Code)

**Tính năng/khu vực**: Vá 2 lỗi giao diện dungtv tự phát hiện sau khi xem lại ảnh chụp (T-02 Contract list, T-09 End Contract)

**Bối cảnh**: dungtv gửi 2 ảnh chụp, chỉ ra: (1) thanh "All houses ▾" bị kéo dài hết chiều ngang màn hình thay vì 1 pill gọn như Figma; (2) khối "Refund to tenant"/"Tenant still owes" bị dính sát ngay phía trên (không có khoảng cách) với ô "Deduction reason".

**Đã tìm ra + vá**:
1. `tenant_contract_screen.dart`: `HouseFilterChip` (đã code đúng dạng pill co gọn theo nội dung, `mainAxisSize: MainAxisSize.min`) bị đặt trực tiếp làm con của 1 `ListView` — theo cơ chế layout của Flutter, `ListView`/`Column` luôn ép chiều ngang các con của nó full-width (chỉ chiều dọc là loose), nên pill bị kéo dài dù chính widget đó không có lỗi. Vá bằng cách bọc `Align(alignment: Alignment.centerLeft, ...)` quanh `HouseFilterChip` ở nơi dùng. Đối chiếu lại Figma T-02 (node `220:3283`) xác nhận đúng là pill gọn, không full-width.
2. `contract_end_screen.dart`: thiếu đúng 1 dòng `const SizedBox(height: 10)` giữa `AppTextField` "Deduction reason" và `Container` "Refund to tenant"/"Tenant still owes" — mọi cặp field khác trong màn đều có khoảng cách 10px, riêng cặp này bị bỏ sót. Đối chiếu Figma T-09 (node `220:3991`) xác nhận có khoảng cách rõ ràng. Đã thêm lại đúng khoảng cách.

Build lại APK, verify trên máy ảo: pill "All houses" nay gọn đúng theo nội dung, không full-width; khối Refund có khoảng cách rõ ràng phía trên. Không có regression (huỷ form End contract bằng Cancel, hợp đồng A.201 vẫn Active, dữ liệu không đổi).

---

## 21:xx — dungtv (qua Claude Code)

**Tính năng/khu vực**: Vá lỗi nút xác nhận ở dialog "End contract and move-out?" (T-09) sai so với Figma

**Bối cảnh**: dungtv gửi ảnh chụp Figma thật (node `400:2783`) chỉ ra nút xác nhận trong dialog ghi đúng là **"Delete"**, trong khi app đang hiện "Confirm move-out".

**Đã vá**: `contract_end_screen.dart` — `ConfirmDialog.show(...)` đổi `confirmLabel` từ `AppStrings.t('contractEnd.confirmMoveOut')` sang `AppStrings.t('common.delete')` (key có sẵn, dùng chung style destructive). Nút "Confirm move-out" ở MÀN HÌNH chính (mở ra dialog này) vẫn giữ nguyên đúng như Figma — chỉ nút BÊN TRONG dialog xác nhận đổi thành "Delete".

Verify trên máy ảo: mở dialog, nút hiện đúng "Delete" khớp Figma. Huỷ bằng Cancel, không có thay đổi dữ liệu.

**Lưu ý còn để ngỏ (chưa hỏi/chưa sửa)**: phần mô tả (description) trong dialog Figma ghi 1 câu tĩnh duy nhất "...move out with the final payment of {amount} VND?" cho mọi trường hợp, trong khi code hiện tại tách 2 câu khác nhau tuỳ dấu của `refundAmount` (`confirmDescriptionOwed`/`confirmDescriptionRefund`) để phân biệt rõ "còn nợ" vs "được hoàn tiền" — khác cách diễn đạt với Figma nhưng mang thêm thông tin thật (không chỉ là lệch style). Chưa tự đổi vì đây là quyết định nội dung, không phải lỗi hiển thị đơn thuần — cần hỏi dungtv trước nếu muốn khớp y hệt câu chữ Figma.

---

## 21:xx — dungtv (qua Claude Code)

**Tính năng/khu vực**: Đổi tiếp phần mô tả (description) dialog "End contract and move-out?" (T-09) khớp y hệt câu chữ Figma

**Bối cảnh**: dungtv nhắc lại rule "bám sát Figma" — yêu cầu đổi luôn phần mô tả trong dialog cho khớp y hệt Figma thay vì giữ 2 câu khác nhau tuỳ hoàn/nợ tiền như trước.

**Đã làm**: Gộp 2 key `contractEnd.confirmDescriptionOwed`/`confirmDescriptionRefund` thành 1 key `confirmDescription` duy nhất, đúng 1 câu tĩnh như Figma cho MỌI trường hợp: "Are you sure you want to end this contract and move out with the final payment of {amount} VND?" (dùng `refundAmount.abs()` bất kể hoàn tiền hay còn nợ). Cập nhật cả 3 file `assets/lang/{en,vi,ko}.json`, xoá 2 key cũ. `contract_end_screen.dart` sửa lại chỉ còn 1 dòng gọi `AppStrings.t('contractEnd.confirmDescription', {'amount': ...})`, bỏ nhánh if/else theo dấu `refundAmount`.

Verify trên máy ảo: dialog hiện đúng y hệt câu chữ Figma (kể cả trường hợp Refund). Khối "Refund to tenant"/"Tenant still owes" ở NGOÀI dialog (trên màn hình chính) không đổi — vẫn phân biệt đúng 2 màu/2 câu khác nhau theo dấu số tiền vì đó không phải nội dung nằm trong phạm vi dialog Figma này.

---

## 21:xx — dungtv (qua Claude Code)

**Tính năng/khu vực**: Vá viền chip "Scheduled" ở dải Invoice Schedule (T-05) — đổi từ viền liền nét sang đúng viền đứt nét như Figma

**Bối cảnh**: dungtv gửi ảnh chụp component "Invoice schedule chip" thật từ Figma, chỉ ra dải chip vẫn chưa đúng thiết kế.

**Đã tìm ra**: Kéo lại `get_design_context` cho node `167:54` xác nhận state "Scheduled" dùng `border-dashed` — nhưng code trước đó (từ đợt vá T-05 đầu ngày) cố tình dùng viền liền nét thay thế kèm comment "chấp nhận khác biệt không đáng kể" vì cho rằng `Border.all` không hỗ trợ dashed. Đối chiếu lại: các màu/size/state khác đều đã đúng (Collected xanh lá, Sent xanh dương `#3E6BD9`, Overdue đỏ, Current viền cam liền nét, Overflow viền `border-hairline` liền nét) — chỉ riêng viền đứt nét của "Scheduled" là sai.

**Đã vá**: `shared/invoice_schedule_strip.dart` — thêm `_DashedRRectDecoration`/`_DashedRRectPainter` (vẽ tay viền bo góc đứt nét qua `Path.computeMetrics()`, dash 3px/gap 2px, stroke 1.5px đúng theo spec), gắn vào `_Chip` qua `foregroundDecoration` khi `dashed: true` (chỉ áp dụng cho state Scheduled).

Verify trên máy ảo, zoom ảnh chụp thật so trực tiếp với ảnh Figma: viền đứt nét khớp đúng, không còn viền liền nét giả.

---

## 21:xx — dungtv (qua Claude Code)

**Tính năng/khu vực**: Sửa toàn bộ icon trong app cho khớp đúng Design System V3 (font "Material Symbols Rounded", không phải "Material Icons" cổ điển của Flutter)

**Bối cảnh**: dungtv gửi ảnh trang "DESIGN SYSTEM — V3 · components & tokens" trên Figma, hỏi có đang dùng design system này làm chuẩn chung không và nhắc lại "bắt buộc phải theo nó" — chỉ ra icon trong app chưa giống thiết kế.

**Đã tìm ra**: Kéo `get_metadata` cho trang Figma xác nhận có 1 section riêng `194:93` "DESIGN SYSTEM — V3 · components & tokens" (28 màu + 15 layout token + 11 text style + 4 effect style, tham chiếu `docs/DESIGN-SYSTEMS.md` §6). Component "Icon" (node `163:5`) trong đó ghi rõ: **font `Material Symbols Rounded`** (ligature theo tên glyph như `home`, `bolt`, `water_drop`, `receipt_long`...), khác hẳn với `Icons.xxx` mặc định của Flutter (font "Material Icons" cổ điển, nét dày/góc cạnh hơn) — dù nhiều tên glyph trùng nhau, hình dạng render vẫn khác. Đây là lệch xuyên suốt TOÀN APP: 64 chỗ dùng icon trên 26 file.

**Đã vá**: Thêm package `material_symbols_icons` (^4.2960.0, bundle sẵn 3 font Material Symbols Outlined/Rounded/Sharp, không cần khai báo font thủ công) vào `pubspec.yaml`. Thay toàn bộ `Icons.xxx` → `Symbols.xxx_rounded` tương ứng trên cả 26 file (script Python đối chiếu bảng tên, `flutter analyze` xác nhận sạch — không tên nào bị thiếu ngoại trừ 1 chỗ `language_rounded` sửa tay).

Verify trên máy ảo: build lại APK, chụp Home/Profile — icon rõ ràng đổi sang nét mảnh, bo tròn đúng phong cách Material Symbols Rounded thay vì Material Icons cũ.

**Lưu ý còn để ngỏ**: package mặc định `opticalSize=48` cho mọi icon (tối ưu hiển thị ở 48dp) trong khi hầu hết icon trong app hiển thị ở 20-24dp — Figma component không ghi rõ optical size cụ thể. Có thể cần tinh chỉnh `opticalSize`/`weight` sau nếu dungtv thấy nét icon hơi khác ở size nhỏ, nhưng đây là tinh chỉnh nhỏ, không phải sai glyph/font.

---

## 22:xx — dungtv (qua Claude Code)

**Tính năng/khu vực**: Bắt đầu seri Bills (B-0x) — sửa số màn lệch Figma + vá TODO prorate tiền nhà trong Edge Function có sẵn

**Phát hiện trước khi code**:
1. **Số màn B-0x trong `SCREEN-SPEC.md` lệch Figma thật** (cùng lỗi từng gặp ở T-0x, xem `DECISIONS.md` 2026-09-09 Đợt 6) — bản cũ ghi B-02=Batch/B-03=Single/B-04=Send sheet/B-05=Detail, Figma thật (`220:4166..220:4667`, đối chiếu ảnh chụp từng frame) là **B-02=Single, B-03=Batch, B-04=Invoice Detail, B-05=Send Invoice sheet**. Đã sửa lại `SCREEN-SPEC.md` mục 2.4 khớp đúng Figma, cập nhật cross-reference trong `API.md`.
2. **Engine tính hoá đơn đã có sẵn** dưới dạng Edge Function `generate-invoice` (deploy từ trước, xem `API.md`) — xử lý đúng BR-BILL-01→11 (điện/nước theo chỉ số/Flat/Not_billed, bỏ qua hợp đồng thiếu chỉ số, phí dịch vụ/định kỳ, mã QR). Việc còn lại chủ yếu là gọi function này từ Flutter, không cần viết lại logic tính tiền.
3. **Vá TODO còn sót trong `generate-invoice`**: phần chia tiền nhà theo ngày ở khi MOVE_IN/MOVE_OUT rơi giữa kỳ (BR-BILL-08) trước đó tạm tính trọn tháng. Đã thêm `findContractEventDate()` (dò `reading_date` của MOVE_IN/MOVE_OUT theo `contract_id` + `period_ym` ở cả 2 bảng điện/nước) + tính `rentAmount = round(monthlyRent × daysOccupied / daysInMonth)` khi có 1 trong 2 sự kiện rơi vào kỳ đang tính (xử lý cả trường hợp cả MOVE_IN lẫn MOVE_OUT cùng rơi vào 1 kỳ — hợp đồng ngắn hạn). Deploy lại thành công qua `npx supabase functions deploy generate-invoice --project-ref rrtppoibjprlvasnbvwr`.

**Còn để ngỏ (chưa hỏi/chưa sửa, phát hiện phụ ngoài phạm vi TODO đã vá)**: `rentAmount` hiện luôn = đúng 1 tháng tiền nhà khi tới kỳ thu (`rentCycleMonths` chỉ quyết định KỲ NÀO có thu, không nhân số tháng vào số tiền) — cần hỏi dungtv xác nhận đây có phải chủ đích (VD "2 tháng/lần" nghĩa là mỗi lần thu vẫn chỉ 1 tháng tiền, tháng còn lại chủ nhà không thu) hay là bug (phải nhân `rentCycleMonths`).

---

## 23:xx — dungtv (qua Claude Code)

**Tính năng/khu vực**: B-01 (Bills List) + B-02 (Create Invoice — Single) code xong + test thật, phát hiện + vá 3 bug nghiêm trọng

**Đã code (UI + CRUD thật, không dùng dữ liệu giả)**:
- `bills/bills_list_screen.dart` (B-01): 2 StatCard (Total this period/Outstanding), chip chọn kỳ (tháng/năm) + `HouseFilterChip` (dùng lại), 4 chip lọc trạng thái Draft/Sent/Collected/Overdue, nhóm theo Nhà (`GroupHeader` mới) → từng hợp đồng Active (card + `StatusPill` + `InvoiceScheduleStrip` dùng lại, `showLegend:false` — thêm param mới cho case này), FAB "+" mở action sheet Single/Batch.
- `bills/pick_contract_sheet.dart` (mới, không có frame Figma riêng — theo đúng pattern picker sẵn có trong app) — chọn 1 hợp đồng Active khi tạo đơn lẻ.
- `bills/invoice_create_single_screen.dart` (B-02): gọi thẳng Edge Function `generate-invoice` (KHÔNG viết lại engine tính tiền bằng Dart) ngay khi mở màn — dungtv xác nhận 2026-09-14 vì Figma vẽ "preview" nhưng function luôn INSERT thật, không có dry-run. Hiện đúng breakdown điện/nước/tiền nhà/phí, "+ Add other fee" (repo method `addOtherFee` mới), nút Save as draft/Send invoice.
- Model `Invoice` mở rộng đầy đủ field (`utilityLines`, `otherFees`...), `InvoiceRepository` thêm `generateSingle/generateBatch/regeneratePaymentQr/addOtherFee/updateStatus/undoCollected` (gọi Edge Function qua `supabase.functions.invoke`).
- `enum_labels.dart` thêm `invoiceStatusLabel()`, 3 file JSON thêm ~35 key `bills.*` + 4 key `status.draft/sent/collected/overdue`.

**3 bug nghiêm trọng phát hiện khi test thật (tạo hoá đơn cho A.201/Nguyen Thi Lan)**:
1. **`generate-invoice`/`generate-payment-qr` chỉ chấp nhận auth `secret`** (service-role key) — app mobile không được nhúng secret key nên gọi bị từ chối, màn B-02 kẹt loading vô hạn (lỗi bị nuốt mất, không hiện gì). Đã sửa `auth: ["user", "secret"]`, tự kiểm tra quyền qua `ctx.supabase` (RLS-scoped, dùng lại policy có sẵn) trước khi dùng `ctx.supabaseAdmin` ghi đặc quyền. Deploy lại cả 2 function.
2. **`core/invoice_period.dart` (T-05/T-10, code từ Đợt 28) tính kỳ hoá đơn SAI** — neo theo ngày ký hợp đồng (VD hợp đồng ký 14/09 → kỳ 14/09→13/10) thay vì đúng tháng dương lịch (`BR-BILL-07`) như `generate-invoice` thật đang dùng (`periodBounds()`). 2 cách tính lệch nhau khiến B-01 không nhận ra hoá đơn thật vừa tạo (tìm sai khoảng ngày). dungtv xác nhận sửa lại `_periodAt()` theo đúng tháng dương lịch — đã verify lại T-05 (chip) + T-10 (preview) không bị regression.
3. **`findContractEventDate` (mới thêm) + `findClosingReading` MOVE_OUT fallback (có sẵn từ trước) so `period_ym` bằng `eq` SAI** — cột `period_ym` của MOVE_IN/MOVE_OUT lưu đúng NGÀY sự kiện thật (`reading_repository.dart` `_createMoveInOrOut`), không phải ngày 1 đầu tháng như PERIODIC, nên so `eq` với "YYYY-MM-01" luôn trật trừ phi dọn vào/ra đúng ngày 1. Sửa thành lọc theo khoảng ngày của tháng (`gte`/`lte` trên `reading_date`). Nhờ vậy tiền nhà prorate đúng ví dụ test (17/30 ngày ở → 2,380,000 thay vì tính nhầm trọn tháng 4,200,000).

**Verify đầy đủ trên máy ảo, dữ liệu thật**: tạo hoá đơn A.201 kỳ 09/2026 — điện 30kWh×3.500=105.000, nước Flat 18.000, tiền nhà prorate 2.380.000, dịch vụ 50.000, Internet 100.000, Phí gửi xe 50.000, Tổng 2.703.000 — khớp đúng từng dòng tính tay. Lưu Draft xong quay lại B-01: stat/đếm/subtitle cập nhật đúng ngay (đã thêm `ref.invalidate` sau khi lưu, cùng lớp lỗi thiếu invalidate đã gặp trước đó).

**Còn nợ lại**: B-03 (Batch), B-04 (Invoice Detail), B-05 (Send Invoice sheet) chưa code.

---

## 23:xx (tiếp) — dungtv

**Cập nhật**: dungtv gửi bộ API key/Secret key eSMS thật (production) để thay cho cấu hình cũ — đã set qua `supabase secrets set ESMS_API_KEY/ESMS_SECRET_KEY` (không lưu vào file trong repo, không lưu vào `.env` local, chỉ tồn tại dưới dạng Supabase Function secret trên server). `send-otp-sms/index.ts` không cần sửa code (đã đọc đúng 2 tên biến này từ trước) — chỉ giá trị secret thay đổi. `ESMS_BRANDNAME` (hiện vẫn là demo "Baotrixemay") giữ nguyên, chưa được yêu cầu đổi.

---

## 12:xx — dungtv (qua Claude Code)

**Tính năng/khu vực**: B-03 (Create Invoice — Batch) code xong (UI + CRUD thật)

**Vấn đề kiến trúc phải hỏi trước khi code**: Figma B-03 (node `220:4448`) vẽ 1 danh sách checkbox cho chọn/bỏ chọn từng hợp đồng trước khi tạo (kèm số tiền ước tính từng dòng), nhưng `generate-invoice` (`mode: "batch"`) chỉ tạo cho TOÀN BỘ hợp đồng Active của 1 Nhà, không nhận danh sách ID cụ thể, và không có chế độ "tính thử, không lưu". Hỏi dungtv — chọn phương án: thêm `mode: "previewBatch"` (tính thử, KHÔNG ghi DB) để hiện danh sách + trạng thái, khi bấm tạo thì gọi lẻ `mode: "single"` cho từng hợp đồng đã tick chọn (tái dùng nguyên engine tính tiền của B-02, không viết API "tạo đúng danh sách đã chọn" riêng).

**Đã sửa `supabase/functions/generate-invoice/index.ts`**:
- `generateSingleInvoice()` nhận thêm `opts.dryRun` — khi bật, tính đầy đủ (điện/nước/tiền nhà/phí) nhưng KHÔNG insert DB, trả `{ preview: {...} }`.
- Thêm chặn tạo trùng: nếu kỳ này ĐÃ có hoá đơn (bất kỳ trạng thái) thì trả `{ invoice: existingInvoice, alreadyExisted: true }` thay vì tạo lại — áp dụng cho cả `single` thật lẫn preview (trước đây `mode: "single"` không hề kiểm tra, có thể tạo trùng nếu gọi lại đúng kỳ).
- Thêm route `mode: "previewBatch"` — lặp qua mọi hợp đồng Active của 1 Nhà, gọi `generateSingleInvoice(..., {dryRun:true})`, trả về `items[]` (mỗi hợp đồng: `status` là `ready`/`missing_reading`/`already_created` + số tiền ước tính hoặc lý do thiếu).
- Refactor nhỏ: gộp logic "tìm mọi hợp đồng Active của 1 Nhà" (dùng chung cho `batch` và `previewBatch`) vào `activeContractIdsForHouse()`; tenant fetch chuyển lên đầu hàm (bớt 1 query lồng nhau cũ).
- Deploy lại (chờ dungtv `supabase login` vì token CLI phiên trước đã hết hạn).

**Đã code Dart**:
- `InvoiceRepository.previewBatch({houseId, periodYm})` + model `BatchPreviewItem`/`BatchPreviewStatus` (`ready`/`missingReading`/`alreadyCreated`).
- `core/providers.dart`: `batchPreviewProvider` (family theo `(houseId, periodYm)`).
- `core/theme.dart`: thêm 3 `StatusBadgeStyle` mới (`ready`/`noReading`/`alreadyCreated`) — cùng tông màu badge có sẵn (xanh/cam/xám), chỉ đặt tên riêng cho đúng ngữ cảnh preview B-03.
- `bills/invoice_create_batch_screen.dart` (mới, B-03): House/Period (`AppTextField` variant Select) + `AppBanner` tóm tắt "N hợp đồng · M sẵn sàng · K thiếu chỉ số", 2 nút ghost "Select all ready"/"Clear", danh sách checkbox-row tự vẽ (room·tenant, số tiền hoặc "—", `StatusPill` theo trạng thái, dòng mờ 60% + không bấm được khi không phải `ready`), thanh tổng navy "N invoices selected/tổng tiền", 2 nút "Save all as draft"/"Create & send all" (lặp gọi `generateSingle` + `updateStatus(sent)` cho từng hợp đồng đã chọn).
- Route `/bills/invoices/new-batch?houseId=` đăng ký trong `router.dart`.
- 3 file JSON: thêm `common.clear` + 13 key `bills.*` mới (house/period/createBatchTitle/batchSubtitle/batchBannerInfo/selectAllReady/contractsSection/previewReady/previewMissingReading/previewAlreadyCreated/invoicesSelected/saveAllAsDraft/createAndSendAll) — đối chiếu key-set 3 file khớp (`OK`).

**`flutter analyze`**: sạch, không lỗi/warning.

**Còn nợ lại**: build APK + test thật trên `emulator-5554` + so ảnh Figma (đang chờ deploy Edge Function xong vì cần `supabase login` lại), B-04 (Invoice Detail), B-05 (Send Invoice sheet — nhớ hỏi dungtv trước khi đụng Zalo OA/QR code).

---

## 13:xx — dungtv (qua Claude Code)

**Tính năng/khu vực**: B-04 (Invoice Detail) code xong (UI + CRUD thật)

**Đã code**:
- `bills/invoice_detail_screen.dart` (mới, B-04, node `220:4544`): hero card (StatusPill + Overdue suy ra như B-01, khoảng kỳ, tổng tiền lớn, "Due {date} · day {N} of each month" — tái dùng `contractDetail.paymentDueValue` có sẵn), khối "Snapshot at issue time" (House/Room/Tenant/Contract terms — version + lý do đổi qua `changeReasonLabel()`), khối "Breakdown" (rent nếu >0, từng dòng điện/nước theo `UtilityLine` — tái dùng đúng cấu trúc field đã dùng ở B-02, dịch vụ/phí định kỳ/phí phát sinh, Total), khối "Timeline" (Created/Sent/Collected — hiện "—" nếu chưa tới), khối "Payout account" (Bank/Account name/number). 4 nút hành động theo đúng trạng thái Figma: "Mark as collected" (ẩn khi đã Collected), "Resend"/"Send" (đổi nhãn theo Draft/đã gửi), "Undo collected" (chỉ bật khi Collected), "Delete draft" (chỉ bật khi Draft, qua `ConfirmDialog` trước khi xoá).
- `InvoiceRepository.deleteDraft()` (mới) — gỡ `invoice_id` khỏi mọi chỉ số điện/nước đã tham chiếu hoá đơn này TRƯỚC khi xoá (khoá ngoại chặn xoá thẳng, y hệt cách phải làm thủ công qua REST lúc dọn dữ liệu test B-02 trước đó).
- `providers.dart` thêm `invoiceProvider` (family theo `invoiceId`).
- Route `/bills/invoices/:invoiceId` đăng ký trong `router.dart` (khớp đường dẫn B-01 đã tap sẵn từ trước).
- 3 file JSON: thêm 19 key `bills.*` mới (invoiceTitle/dueOn/snapshotAtIssueTime/room/tenant/contractTerms/contractTermsValue/breakdownSection/timelineSection/created/bank/accountName/accountNumber/markAsCollected/resend/send/undoCollected/deleteDraft/deleteDraftHint/deleteDraftConfirmDescription) — đối chiếu key-set 3 file khớp (`OK`).

**Đơn giản hoá có chủ đích so với Figma**: dòng "Monthly rent" trên Figma có thêm nhãn phụ "· full month"/"· 17/30 days" (mô tả có prorate hay không) — `tb_invoice`/`Invoice` model hiện KHÔNG lưu số ngày ở thực tế riêng (Edge Function chỉ lưu `rentAmount` đã tính xong), nên không tái tạo được nhãn phụ này phía Flutter mà không thêm cột mới. Tạm bỏ nhãn phụ, chỉ hiện số tiền — ghi lại đây để cân nhắc thêm cột `rentDaysOccupied`/`rentDaysInMonth` vào `tb_invoice` sau nếu dungtv muốn khớp đúng chi tiết này.

**`flutter analyze`**: sạch.

**Còn nợ lại**: build APK + test thật + so Figma cho cả B-03/B-04 (đang chờ `supabase login` để deploy `generate-invoice` bản preview mới nhất — B-04 không phụ thuộc bản deploy này, chỉ dùng các Edge Function/cột đã deploy từ trước). B-05 (Send Invoice sheet) còn lại — nhớ hỏi dungtv trước khi đụng Zalo OA/QR code.

---

## 16:xx — dungtv (qua Claude Code)

**Tính năng/khu vực**: B-05 (Send Invoice sheet) code xong — SMS thật qua eSMS, Zalo/Both tạm khoá chờ Zalo OA

**Bối cảnh**: Hỏi dungtv về Zalo OA trước khi code (đúng chỉ dẫn đã nhận). dungtv xác nhận thủ tục Zalo OA đã xong nhưng khi hỏi cụ thể 5 giá trị cần (OA ID/App ID/App Secret/Access+Refresh token/Template ID) thì gửi nhầm cặp key eSMS đã set từ sáng — nghĩa là thông tin Zalo thật vẫn chưa có trong tay. dungtv yêu cầu triển khai eSMS trước trong lúc chờ.

**Phát hiện phụ**: eSMS hiện chỉ có Brandname DEMO (khoá cứng 1 mẫu OTP, không gửi được nội dung hoá đơn tự do). dungtv xác nhận chưa có Brandname CSKH riêng → dùng tạm SmsType "1" (tin thường, gửi thật ngay, không cần duyệt trước, đổi lại là hiện số điện thoại thay vì tên "BizTown").

**Đã code**:
- `supabase/functions/send-notification/index.ts`: hoàn thiện TODO gửi SMS Tenant (`sendSmsViaEsms`, SmsType "1"), đổi auth `["secret"]` → `["user","secret"]` + permission check qua `tb_house` (cùng pattern Đợt 32). Phần Push/FCM giữ nguyên TODO (ngoài phạm vi).
- `InvoiceRepository.sendSms()` (mới).
- `bills/send_invoice_sheet.dart` (mới, B-05): bottom sheet đúng Figma — SMS hoạt động thật, Zalo/Both mờ + "Coming soon". Nội dung tin nhắn tiếng Việt CỐ ĐỊNH (BR-NOTI-07, không qua `AppStrings.t()`).
- Sửa B-02 (`_sendInvoice`)/B-04 (`_sendOrResend`) đang push tới route `/bills/invoices/:id/send` (chưa từng tồn tại) → gọi `showSendInvoiceSheet()` (đúng pattern sheet có sẵn, không phải route).
- 3 file JSON: thêm 10 key `bills.*` (sendInvoiceSheetTitle/sendInvoiceSheetTo/channelSms/channelSmsHint/channelZalo/channelBoth/channelComingSoon/messagePreview/sendNow/tenantPhoneMissing) — key-set khớp `OK`.

**`flutter analyze`**: sạch.

**Coi như xong toàn bộ B-01→B-05** (UI + CRUD thật). Còn nợ: deploy các Edge Function đã sửa (`generate-invoice` bản preview, `send-notification`) — đang chờ dungtv `supabase login` lại (token CLI phiên trước hết hạn); test thật trên `emulator-5554` + so ảnh Figma cho B-03/B-04/B-05 (chưa test được màn nào trong 3 màn này); Zalo ZNS (chờ dungtv cung cấp đủ 5 giá trị); Brandname CSKH thật cho eSMS (tuỳ chọn, nếu dungtv đăng ký).

---

## 17:xx — dungtv (qua Claude Code)

**Tính năng/khu vực**: Bug thật — "Forgot password?" nhầm sang luồng Sign Up, code lại đúng S-04 riêng

**Phát hiện lúc test**: Cần đăng nhập lại để test B-03/B-04/B-05, dùng "Forgot password?" ở màn Login — hoá ra dẫn thẳng vào luồng "Create account" (Sign Up), hỏi cả "Full name", nút bấm ghi "Create account". dungtv nhắc lại nguyên tắc "thấy bug phải fix luôn" — dừng test, gửi ảnh Figma S-04 thật (3 bước riêng: Verify phone → Reset password → Well Done), code lại đúng màn riêng.

**Đã code**:
- `shared/forgot_password_screen.dart` (mới, S-04, node `386:2282`/`388:2375`/`388:2386`): Step 1 Verify phone+OTP (tái dùng đúng UI Sign Up), Step 2 Reset password (CHỈ New password/Confirm — không hỏi Full name, nút "Save"), Step 3 bottom sheet "Well Done!"/"Go to login" (tự `signOut()` rồi về `/login`, khác Sign up "Get started" đi `/home`).
- Tách `SendOtpChip` thành widget dùng chung `shared/send_otp_chip.dart` (trước là private trong `signup_screen.dart`).
- Route `/forgot-password` + cập nhật rule redirect trong `router.dart` (miễn ép `/login` khi đang ở màn này, cùng lý do đã áp dụng cho `/signup`).
- `login_screen.dart`: nút "Forgot password?" đổi từ `push('/signup')` sang `push('/forgot-password')`.
- 3 file JSON: thêm 12 key `forgotPassword.*` — tái dùng tối đa key `signup.*` sẵn có (phoneNumber/sendOtp/otpSentMessage/resendCode.../passwordsDoNotMatch/passwordTooShort) cho đúng field/thông báo giống hệt nhau.

**Verify trên máy ảo, dữ liệu thật (SĐT 0356123970, dùng OTP test code cố định `123456` đã cấu hình sẵn trong Supabase Auth cho số này)**: chạy trọn 3 bước, chụp ảnh so khớp Figma từng bước, đặt mật khẩu mới xong quay lại đúng màn Login (không tự vào Home), đăng nhập lại bằng mật khẩu mới thành công.

**Phát hiện phụ**: `flutter build apk` lúc đầu build thiếu `--dart-define=SUPABASE_URL/SUPABASE_PUBLISHABLE_KEY` khiến mọi lệnh gọi Auth (gửi OTP) báo lỗi chung chung — đúng bug đã từng gặp 2026-09-10, không phải lỗi code, chỉ quên tham số build khi rebuild lại APK sau khi nghỉ.

**`flutter analyze`**: sạch.

---

## 18:xx — dungtv (qua Claude Code)

**Tính năng/khu vực**: Test thật trọn chuỗi B-01→B-05 trên máy ảo, dữ liệu thật — PASS toàn bộ

Sau khi deploy 2 Edge Function (`generate-invoice` bản preview, `send-notification` bản SMS thật) và vá xong bug S-04 Forgot password, build lại APK đúng `--dart-define`, test sống từng màn:

- **B-01**: đúng số liệu Draft (1)/2,703,000, dải chip period đúng.
- **B-03 (Batch)**: chọn nhà "Nha trong So 2" kỳ 09/2026 → banner đúng "1 active · 0 ready · 0 missing" (hợp đồng A.201 đã có hoá đơn kỳ này), dòng "Already created" đúng mờ + checkbox khoá, bấm không chọn được (xác nhận đúng thiết kế).
- **B-04**: mở hoá đơn A.201 — hero/snapshot/breakdown/timeline đúng số liệu đã verify tay từ trước (Đợt 32).
  - **Phát hiện dữ liệu test sai** (không phải bug code): `tb_house.bank_account_name` của "Nha trong So 2" bị nhét cả chuỗi `"Vietcombank - 0071000112233 - LE THI MAI"` vào 1 cột, `bank_bin`/`bank_account_number` để trống — chắc do nhập tay sai ở phiên trước. Sửa lại data qua Management API SQL (tách đúng 3 cột: `bank_bin='970436'`, `bank_account_number='0071000112233'`, `bank_account_name='LE THI MAI'`). Reload lại app (provider cache theo phiên) → hiện đúng 3 dòng riêng biệt.
- **B-05**: bấm "Send" từ B-04 → sheet đúng Figma (SMS chọn sẵn, Zalo/Both mờ "Coming soon", message preview tiếng Việt đúng SĐT/số tiền/ngân hàng/hạn). Bấm "Send now" → **gửi SMS thật thành công qua eSMS** (SmsType 1) tới `0944333555`, sheet tự đóng, B-04 tự cập nhật: badge chuyển "Overdue" (đúng derive Sent + đã quá hạn 10/09), Timeline "Sent" hiện đúng giờ thật `14/09/2026 14:08`, nút đổi "Send" → "Resend".

**Kết luận**: B-01→B-05 hoàn thành đầy đủ UI + CRUD thật + tích hợp Edge Function/eSMS thật, verify bằng dữ liệu thật trên máy ảo, không còn lỗi nào phát hiện thêm ngoài các bug đã liệt kê (đã fix hết) và 1 lỗi dữ liệu test (đã sửa). Còn nợ: Zalo ZNS (chờ dungtv cung cấp đủ OA ID/App ID/Secret/Access+Refresh token/Template ID).

---

## 19:xx — dungtv (qua Claude Code)

**Tính năng/khu vực**: Vá lỗ hổng thật — "Create & send all" ở B-03 chưa hề gửi SMS thật

**Phát hiện**: dungtv hỏi trực tiếp luồng bấm "Create & send all" (15-20 phòng) có hiện chọn kênh rồi gửi thật không — kiểm tra code phát hiện KHÔNG, chỉ giả trạng thái "Sent" thẳng vào DB. Hỏi hướng sửa — chọn hiện 1 sheet chọn kênh 1 LẦN cho cả lô rồi gửi SMS thật tuần tự.

**Đã code**:
- `core/invoice_message.dart` (mới) — tách `buildInvoiceSmsMessage()` dùng chung cho B-05 lẫn B-03.
- `shared/channel_option.dart` (mới) — tách widget "option kênh" dùng chung, xoá 2 bản private trùng nhau ở `send_invoice_sheet.dart`.
- `bills/batch_send_channel_sheet.dart` (mới) — sheet chọn kênh 1 lần cho cả lô.
- `invoice_create_batch_screen.dart`: "Create & send all" giờ mở sheet trước, Cancel = không tạo gì; xác nhận thì tạo hoá đơn + gửi SMS thật từng cái (tra `contract → tenant.phone`), lỗi 1 cái không chặn các cái còn lại.

**Phát hiện phụ (QR code)**: dungtv hỏi luồng QR — kiểm tra lại toàn bộ Figma thật xác nhận KHÔNG có màn nào vẽ ảnh QR dù BR-BILL-10 yêu cầu (lệch tài liệu/Figma). dungtv quyết định không thêm QR vào B-04 (không có ý nghĩa khi chỉ chủ nhà tự xem) — hoãn tới khi có Zalo OA (chỉ Zalo ZNS gửi được ảnh, SMS không gửi được).

**`flutter analyze`**: sạch.

---

## 20:xx — dungtv (qua Claude Code)

**Tính năng/khu vực**: Verify thật luồng "Create & send all" (B-03) sau khi vá — PASS

Để test được nhánh "Ready" thật (nhà "Nha trong So 2" chỉ có 1 hợp đồng, kỳ 09/2026 đã tạo hoá đơn từ trước), thêm chỉ số điện/nước PERIODIC kỳ 10/2026 cho phòng A.201 qua DB trực tiếp (nối đúng `previousReadingId` từ chỉ số kỳ 09, không phá chuỗi lịch sử).

**Kết quả test trên máy ảo**: chuyển kỳ B-03 sang 10/2026 → đúng hiện badge "Ready" (xanh) cho A.201 kèm số tiền ước tính 4,523,000 → tick chọn → thanh tổng đúng "1 invoices selected / 4,523,000 VND" → bấm "Create & send all" → sheet chọn kênh hiện đúng ("Send invoice", "Send 1 invoices", SMS chọn sẵn, Zalo/Both "Coming soon") → bấm "Send now" → màn tự đóng về B-01. Xác nhận qua DB: hoá đơn kỳ 10/2026 đã tạo (`total_amount=4,523,000`) với `status=Sent`, `sent_at` có giá trị thật — chứng minh SMS đã gửi thật qua eSMS trong luồng hàng loạt, không còn giả trạng thái như trước khi vá.

**Kết luận**: B-0x (toàn bộ series Bills) hoàn thành đầy đủ, verify thật cả nhánh đơn lẻ (B-02/B-04/B-05) lẫn hàng loạt (B-03), không còn lỗ hổng nào được biết tới. Còn nợ: Zalo ZNS (chờ dungtv), QR code (hoãn tới khi có Zalo), lịch nhắc thanh toán tự động BR-PAY-04 (chưa làm, cần cron job riêng).

---

## 21:xx — dungtv (qua Claude Code)

**Tính năng/khu vực**: Test thật quy mô lớn (5 nhà, 87 phòng) — case "hợp đồng nhiều phòng" PASS

Bắt đầu test toàn luồng nghiệp vụ theo kịch bản dungtv yêu cầu (5+ nhà, tới ~30 phòng/nhà, hợp đồng nhiều phòng, mời quản lý). Chiến lược đã thoả thuận: seed số lượng lớn qua DB, các case nghiệp vụ quan trọng bấm tay qua UI thật.

**Đã seed**: mở rộng 2 nhà có sẵn + 3 nhà mới = 5 nhà/87 phòng/67 hợp đồng Active (script Python sinh SQL, chạy qua Management API) — xem Đợt 38.

**Test case "1 hợp đồng thuê nhiều phòng cho nhân viên" (BR-CTR-04) — PASS hoàn toàn**:
- Tạo tenant "Cong ty TNHH ABC Tech" (đại diện công ty) qua Tenant Pool, đủ ảnh CCCD 2 mặt (dùng system Photo Picker của Android — lưu ý: `image_picker` của app dùng `GET_CONTENT`/`PhotoPickerGetContentActivity`, không phải `ACTION_PICK` — cần index đúng ảnh vào MediaStore trước khi test qua máy ảo).
- Tạo hợp đồng chọn ĐỒNG THỜI 3 phòng trống (316, 210, 102) của "Day tro Thu Duc" qua đúng multi-select checkbox ở T-06.
- Xác nhận: Deposit/Monthly rent tự động CỘNG DỒN đúng giá tham khảo cả 3 phòng (3,000,000×3 = 9,000,000) — đúng BR-CTR-04.
- Xác nhận: màn "MOVE-IN READINGS (REQUIRED)" bắt buộc nhập chỉ số điện/nước RIÊNG cho TỪNG phòng trong số 3 phòng đã chọn — không cho lưu thiếu bất kỳ phòng nào.
- Sau khi lưu: Contract Detail (T-05) hiện đúng tiêu đề gộp "102, 210, 316", đúng điều khoản đã nhập, và khối "METER READINGS" tách riêng từng phòng với đúng chỉ số move-in đã nhập cho từng phòng.

**Kết luận**: không phát hiện bug nào ở case này — toàn bộ luồng hợp đồng nhiều phòng hoạt động đúng thiết kế nghiệp vụ.

Còn lại: Renew/Amend hợp đồng, kết thúc hợp đồng (refund + owed), mời 2 quản lý, test hiệu năng B-03 ở nhà 33 phòng.

---

## 22:xx — dungtv (qua Claude Code)

**Tính năng/khu vực**: Test hiệu năng B-03 ở quy mô 28-33 hợp đồng (đúng mối lo ngại chính của dungtv) — PASS, có 1 phát hiện UX

**Kết quả test** (nhà "Chung cu mini Binh Thanh", 33 phòng/28 hợp đồng Active, kỳ 09/2026):
- Mở B-03 → preview 28 hợp đồng: **~40 giây** để tính xong (đúng, không lỗi, không timeout) — "28 active · 28 ready · 0 missing", mỗi hợp đồng đúng số tiền ước tính riêng (khác nhau tuỳ điện/nước từng phòng).
- "Select all ready" → "Save all as draft" (miễn phí, không gửi SMS) → **~75-80 giây** để tạo xong toàn bộ. Xác nhận qua DB: **28/28 hoá đơn tạo thành công**, đều `status=Draft`, không hoá đơn nào lỗi/thiếu.

**Phát hiện UX (không phải bug chức năng)**: Cả 2 bước (tính preview + tạo hàng loạt) đều chạy tuần tự (gọi Edge Function từng hợp đồng một, không song song — quyết định kiến trúc đã chọn ở Đợt 31/37) nên mất khá lâu (40-80 giây) ở quy mô 28-33 hợp đồng, và **màn hình không hiện bất kỳ progress indicator/spinner nào trong lúc tạo hàng loạt** (nút vẫn hiện bình thường, không rõ đang chạy hay bị treo) — người dùng thật dễ tưởng app bị đứng và bấm lại nhiều lần. Cần dungtv xác nhận có muốn vá UX này không (thêm progress bar "Đang tạo N/28..." hoặc chỉ số phần trăm) trước khi coi B-03 hoàn thiện 100% — chưa tự sửa vì đây là thay đổi UI mới, không có trong Figma gốc.

**Kết luận về mối lo ngại chính của dungtv**: "chạy được hết bằng ấy" — XÁC NHẬN CHẠY ĐƯỢC, đúng 100% kết quả, chỉ chậm và thiếu phản hồi trực quan khi đang chạy.

---

## 23:xx — dungtv (qua Claude Code)

**Tính năng/khu vực**: B-03 — chuyển hẳn "Create & send all"/"Save all as draft" sang backend xử lý 1 lần (thay vì app tự lặp gọi), vá bug cache hiện sai trạng thái sau khi tạo/gửi hàng loạt

**Bối cảnh**: dungtv hỏi đúng vấn đề — luồng cũ để app tự lặp gọi từng hợp đồng có rủi ro thật nếu Android tạm dừng tiến trình khi người dùng khoá màn hình/chuyển app giữa chừng, lô có thể dở dang không cách nào tự hoàn tất. dungtv chọn phương án chuyển hẳn việc tạo+gửi sang 1 lệnh gọi Edge Function duy nhất (đánh đổi: mất % tiến độ chính xác, chỉ còn spinner không xác định). Chi tiết xem [docs/DECISIONS.md](../docs/DECISIONS.md) Đợt 39.

**Đã làm**:
- `supabase/functions/generate-invoice/index.ts` — thêm `mode: "batchSend"` (tạo song song theo lô 6 + gửi SMS thật nếu `send=true`, trả `{created, sent, errors[]}` gộp 1 lần). Deploy thành công.
- `supabase/functions/_shared/esms.ts`, `_shared/invoice_message.ts` (mới) — trích dùng chung giữa `generate-invoice` và `send-notification`, tránh lặp code.
- `src/lib/data/invoice_repository.dart` — thêm `batchCreateAndSend()`/`BatchSendResult`.
- `src/lib/bills/invoice_create_batch_screen.dart` — bỏ vòng lặp client-side + progress bar xác định (làm ở bản trước), thay bằng 1 lệnh gọi backend duy nhất + overlay spinner không xác định, vẫn khoá toàn bộ điều khiển trong lúc chờ.

**Bug thật phát hiện khi live-test lại**: tạo+gửi thật 3 hợp đồng (B.3/B.6/B.8, nhà "Nha trong So 2") — backend tạo+gửi đúng cả 3 (verify qua REST API bằng JWT thật: `status=Sent`, có `sent_at`), nhưng Bills List (B-01) vẫn hiện "Draft — chưa tạo", kể cả sau khi chuyển tab qua lại. Nguyên nhân: `billsHouseGroupsProvider` đọc hoá đơn qua provider con `contractInvoicesProvider(contractId)` — chỉ invalidate provider cha là không đủ, provider con vẫn trả cache cũ. Cùng lỗi có ở B-04 (`invoice_detail_screen.dart`) và B-05 (`send_invoice_sheet.dart`) — sửa cả 3 chỗ.

**Verify lại sau khi vá**: build lại APK, cài lên `emulator-5554`, chọn thêm B.4 "Save all as draft" (miễn phí) — Bills List cập nhật ngay "Draft — chưa gửi" ngay sau khi quay lại màn, không cần chuyển tab/khởi động lại app. `flutter analyze` sạch, key 3 file ngôn ngữ khớp nhau.
