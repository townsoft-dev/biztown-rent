// Tên cũ, GIỮ LẠI để những link đã gửi cho người thuê ngày 18/09/2026 không
// chết. Link mới dùng `i` cho ngắn — xem `_shared/invoice_image.ts`.
import { invoiceImageHandler } from "../_shared/invoice_image.ts";
export default invoiceImageHandler;
