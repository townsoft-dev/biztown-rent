<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Bắt buộc cho mọi lời gọi Supabase (Auth/DB/Storage) — chỉ có sẵn ở
         src/debug và src/profile theo mặc định của Flutter template, KHÔNG có
         ở đây (src/main, áp dụng cho cả bản release) — thiếu dòng này khiến
         bản release không truy cập mạng được, phát hiện 10/09/2026 lúc test
         CRUD thật (xem changelog/2026-09-10.md). -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Chụp ảnh Nhà/Phòng (H-02/H-05) qua image_picker -->
    <uses-permission android:name="android.permission.CAMERA" />
    <application
        android:label="Rent Manager"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Push (FCM) — 3 khai báo dưới đây quyết định thông báo TRÔNG thế nào
             khi app đang tắt/nền, lúc đó chính Android vẽ thông báo chứ không
             phải code Dart. Thiếu chúng thì (phát hiện khi test push thật
             17/09/2026): icon bẹt thành ô vuông đặc, màu xám mặc định, và log
             cảnh báo "Missing Default Notification Channel metadata" vì Android
             phải tự chế một kênh dự phòng — âm báo/độ ưu tiên khi đó mỗi hãng
             máy một kiểu. Kênh `biztown_default` được tạo thật trong
             MainActivity.kt. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_notification" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/notification_color" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="biztown_default" />
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
